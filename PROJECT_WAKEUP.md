# MusicForArt 项目唤醒与当前状态

最后更新时间：2026-04-01

## 新线程唤醒指令
请先读取并严格按此文件恢复上下文：
`/Users/demo/Documents/New project/musicForArt/PROJECT_WAKEUP.md`

然后先执行：
1. 总结当前用户端、运营端、Neo4j 三条主链路；
2. 检查并启动 Neo4j、后端、前端；
3. 优先继续当前未收尾的核心 bug 与业务功能；
4. 不做无关重构。

## 项目路径与端口
- 项目根目录：`/Users/demo/Documents/New project/musicForArt`
- 后端：`/Users/demo/Documents/New project/musicForArt/backend`
- 前端：`/Users/demo/Documents/New project/musicForArt/frontend`
- 用户端：`http://127.0.0.1:5200`
- 后台：`http://127.0.0.1:5200/admin.html`
- 后端：`http://127.0.0.1:8010`
- Neo4j HTTP：`http://127.0.0.1:7474`
- Neo4j Bolt：`127.0.0.1:7687`

## 当前系统分层

### 用户端
- 手机号 + 验证码模拟登录
- 当前已升级为测试期用户体系：
  - 用户首页标题改为 `有声书创作台`
  - 非登录状态下，所有主功能按钮都做前端禁用和后端强校验
  - 测试期间默认启用“邀请码激活制”
  - 登录区新增：
    - 邀请码（测试期必填）
    - UID（可选）
    - 推荐码（可填写 UID / 手机号 / 为空）
- 用户首页现有主功能均要求登录成功后才能执行，旧 session 也会被邀请码测试制复核
- 创建项目
- 用户端功能区已重排为：
  - 基础准备
  - 动作模块
  - 场景模块
  - 文本与执行
  - 导出与汇总
- 关键文案已调整为：
  - `文本演绎分析`
  - `文本匹配音乐`
- 人物动作提取
- 动作图谱推荐
- 场景搭建分析
- 场景音效推荐
- 场景补充单
- 动作补充单
- 音乐分析页误混入动作提取区块的问题已修：
  - 只有动作结构结果才会渲染动作面板
- 动作提取分段明细（测试态）已增加：
  - 该段缩略原文
  - Contract
  - Fallback
- 后期执行单页已去掉：
  - `对白/音乐` 固定比例展示
  - `推荐音效` 展示
- 用户端主流程按钮已增加“当前步骤”视觉态：
  - 当前点击的步骤按钮会显示压下态
  - 右上角角标文案为 `now`
  - 仅作轻提示，不影响原功能
- 用户端已隐藏 `Qwen` 切换入口：
  - 当前前台只显示默认 `DeepSeek`
  - Qwen 接线代码保留，未来可直接恢复 UI
- 用户端已新增“今日用量”面板：
  - 文本分析字符：已用 / 限额 / 剩余
  - 音效下载数量：已用 / 限额 / 剩余
  - 当前账号 / 用户级别 / 统计日期
- 用户端已新增前台热度榜区域：
  - 动作音效热度榜
  - 场景搭建热度榜
  - 支持按 `过去1天 / 过去10天 / 过去30天 / 过去3个月` 切换
  - 两个区域并排展示，移动端自动改为纵向
- 文本演绎分析与生成后期执行单结果页，已去掉无关的动作提取卡片：
  - `人物动作判定规则`
  - `提取结果概览`
  - `候选动词数`
  - `需做动作音效`
  - `功能标题`
  - `人物动作动词提取`
- 动作图谱推荐已修复两个前端体验问题：
  - 空结果也能正常渲染，不再报错
  - 单个音效下载后页面不跳走，仍停留在当前推荐页继续浏览
- 单个音效下载与 `SFX ZIP` 导出后，会立即刷新“今日用量”中的下载计数
- 下载音效时不再在右侧结果区显示“正在执行：下载 xxx.mp3”
- 用户端“额度协助路牌”当前口径：
  - 正面只保留 `额度协助路牌`
  - 反面补充文案：
    - `聪明如你，观字在。`
    - `如今日用量达到上限，请联系管理员协助调整额度。`
- 用户端首页标题后已增加小提示：
  - `Beta版页面有彩蛋`
- 用户端内容赛道当前规则：
  - 默认值为 `请选择作品赛道`
  - 内容赛道为必选
  - 提示文案：
    - `准确赛道选择与推荐音效的内容息息相关。`
- 用户端上传音频当前已做前后端双重安全校验：
  - 单个音频文件不得超过 `80MB`
  - 仅允许音频格式：
    - `mp3`
    - `wav`
    - `m4a`
    - `aac`
    - `flac`
    - `ogg`
  - 拒绝视频、文本等非音频格式
  - 后端会校验扩展名 / MIME / 文件名非法路径符号 / 空文件 / 大小上限

### 用户端：音乐分析与文本匹配音乐的近期质量收口

- 音乐分析 fallback 已修复“4 个乐段重复”问题：
  - 原因是 LLM `sections empty` 后落到统一模板兜底
  - 现已改成按真实锚点、段位、峰值/回落类型生成差异化乐段
  - `project_id=2` 已验证不再出现四段几乎相同的说明

- 音乐分析 `结构规律` 区块已提升质量：
  - 不再把 `未明确识别 / 无 / 建议复核` 这类机械占位结果直接展示给用户
  - 后端会把弱结构结果识别为低质量，再基于真实音乐锚点、分段、时长、标签重建结构说明
  - 前端增加展示门槛：若结构规律仍只是弱占位内容，则不展示该区块

- 文本匹配音乐（原生成文本音乐执行单）规则路径已升级：
  - 即使走 `rules-only`，也必须输出有真实依据的：
    - `key_points`
    - `structure_logic`
    - `sections`
    - `music_entry_plan`
    - `hit_points`
    - `risks`
    - `export_hints`
  - 不凭空捏造配器、剧情或重复段
  - `音乐与文本适配结论` 已改为证据型输出，不再只有简单的 `部分适配 / 60分`
  - `判断是否适合作品` 中已去掉重复的 `关键结论` 分项分数字段

- 文本匹配音乐结果页近期前端收口：
  - 右侧结果区已补 `min-width: 0` 与内部滚动/换行约束
  - 避免 `文本匹配音乐` 结果撑宽后挤压左侧功能按钮区
  - `文字-旁白-音乐对位执行单` 当前展示口径：
    - 时间区间去掉 `秒` 字
    - 每条记录为独立边框卡片
    - `场景 / 文字区间 / 旁白区间 / 音乐区间 / 锚点` 为上层信息
    - 文本内容单独放在下层，但仍属于同一条记录
    - 已去掉顶部 `文字片段` 提示文案
    - 已持续压缩 `场景` 与 `文字区间` 两列占宽，把空间让给 `锚点`
    - 文本内容展示时需要带上句尾标点，避免用户误解断句
    - 句尾标点优先从 `clause_timeline` 的真实标点证据中补回展示，不凭空捏造

- 动作图谱推荐口径已统一：
  - 绿色词只表示“最终仍有可下载素材的词”
  - `已覆盖 X 个音效词`
  - `当前命中 X 个词、共 Y 条素材候选`
  - 与下载表格保持同一口径
  - 修复前存在“两个绿色词，但只有一个可下载”的误导

### 运营端
- 手机号 + 验证码模拟登录
- 用户体系新增后台能力（当前先以 API 为主，后续再做后台页面）：
  - 测试期开关：
    - `GET/POST /api/admin/beta-access`
  - 邀请码列表：
    - `GET /api/admin/invite-codes`
  - 用户列表已扩展字段：
    - `uid`
    - `invite_activated`
    - `invite_code_used`
    - `referred_by_phone`
    - `referred_by_uid`
    - `referral_user_count`
- `admin.html` 当前已补到可运营使用：
  - 用户列表按每页 `6` 个分页
  - 可查看指定日期的文本已用 / 下载已用 / 剩余 / 邀请激活状态
  - 可修改前台等级默认额度
  - 可按手机号手动重置当日用量
- 动作后台已新增“前台排行榜管理”：
  - 动作榜按真实下载统计生成
  - 支持时间范围切换：`1d / 10d / 30d / 90d`
  - 支持手动覆盖：
    - 前台展示名
    - 前台副标题
    - 前台显示数量
    - 前台排序位
    - 是否展示
  - 支持调整用户端：
    - 动作榜标题
    - 场景榜标题
    - 动作榜 / 场景榜左右位置
- 场景后台已复用同一套“前台排行榜管理”能力：
  - 场景榜按真实下载统计生成
  - 与动作后台共享前台区域顺序与标题配置
- `运营行为审计` 已改成运营可读视图：
  - 行为总览
  - 行为分布
  - 最近行为时间线
  - 原始 JSON 折叠区
- 行为分布当前显示：
  - 中文业务功能名
  - 英文动作码默认折叠，点击展开 / 再点回缩
- 最近行为时间线当前规则：
  - 时间格式统一显示为 `YYYY-MM-DD HH:MM:SS`
  - 每页显示 `4` 条
  - 支持上下页翻页
- `运营漏斗` 当前明确解释为：
  - `AI 推理链路漏斗`
  - 数据源来自 `ReasoningLog`
  - 不是用户增长漏斗
- `优化建议` 当前明确解释为：
  - `图谱与素材补库建议`
  - 数据源来自 `ReasoningLog`
- 动作补充单运营
- 动作图谱 Neo4j
- 父节点浏览器
- 父节点详情：列表视图 / 图谱视图
- 场景后台独立入口：
  - `/scene.html`（已具备手机号登录、场景补充单筛选、摘要统计、卡片/表格视图、逐词上传入库、通知用户；后续继续扩成场景图谱维护与场景模板后台）
- 后台 `文件下载（本地归档）` 右侧已新增 `系统请求压力区`：
  - 当前定位是“基于真实日志的观测与建议面板”
  - 数据源：
    - `user_operation_log`
    - `llm_trace_digest`
  - 当前可展示：
    - 最近 `10分钟 / 1小时 / 24小时` 的重请求量
    - 活跃用户数
    - 项目数
    - P95 耗时
    - 并发峰值估算
    - LLM 异常数
    - 质量异常数
    - 最慢样本
    - 高频异常原因
    - 排队机制建议参数
  - 重要说明：
    - 目前这里只是“建议面板”
    - 还不是已经接入执行链路的真实排队控制器
    - 页面上显示的：
      - `建议全局槽位`
      - `单用户运行上限`
      - `单用户排队上限`
      - `建议超时`
      - `排队提醒阈值`
    - 当前都只是基于日志推导出来的建议值，系统尚未真正执行这些限制

### 下一阶段明确约定：从“建议面板”升级到“真实可执行机制”

- 已确认后续要做的方向：
  - 将后台 `系统请求压力区` 从“观测与建议”升级为“真实可执行机制”

- 已达成的最稳落地线：
  1. 先新增统一任务表，例如 `analysis_job`
  2. 先接入 `音乐分析` 和 `文本匹配音乐`
  3. 真正实现：
     - `全局槽位`
     - `单用户运行上限`
     - `单用户排队上限`
  4. 再给后台补“参数可修改入口”

- 这些参数未来真正落地时的维度：
  - `全局槽位`
    - 作用在“重任务调度器”层
    - 表示同一时刻允许执行的重任务总数
  - `单用户运行上限`
    - 作用在“用户维度的运行中任务计数”层
    - 建议先按 `user_phone` 落
  - `单用户排队上限`
    - 作用在“用户维度的排队中任务计数”层
    - 防止单一用户把队列塞满

- 当前明确不应误解为已完成的部分：
  - 现在系统里还没有真正的：
    - `job queue`
    - `queued/running/succeeded/failed/timeout` 状态机
    - 后台调度器
    - 参数控制入口
  - 因此后台当前看到的排队参数，不能被理解为已在系统中生效

### 图谱层
- 本地动作图谱 JSON
- Neo4j 同步
- Neo4j 边已显式拆分：
  - `HAS_DIRECT_SFX_CHILD`
  - `HAS_COMPOSITE_SFX_CHILD`

## 关键业务规则

### 测试期邀请码 / 推荐码规则
当前用户体系已切到“测试期邀请码激活制”：

1. 系统默认生成 `200` 个邀请码并写入数据库
2. 默认测试期开关为开启：
   - `beta_invite_only_enabled = true`
3. 测试期间：
   - 无邀请码的用户即使拿到验证码，也不能完成激活登录
   - 邀请码一旦激活成功，立即失效
4. 测试期开关关闭后：
   - 用户可正常注册/登录使用
   - 不再强制要求邀请码
5. 推荐码支持三种输入：
   - UID
   - 手机号
   - 留空
6. 推荐关系写入用户表：
   - `referred_by_user_id`
   - `referred_by_phone`
   - `referred_by_uid`
   - `referral_input`
7. 种子用户的拉新人数通过 `referral_user_count` 统计

### 用户级别与后台角色
当前已拆成两条维度，避免同一个手机号“既是后台管理员，又是前台种子用户/普通用户”时相互冲突：

1. 后台角色 `ops_role_code`
   - `00`：超级管理员
   - `11`：管理员
   - `22`：预留，不作为后台角色主用
   - `33`：普通后台无权限

2. 前台用户级别 `user_tier_code`
   - `22`：种子用户（测试期间可正常使用用户端功能）
   - `33`：普通用户（正式上线后的正常用户级别）

3. 当前超级管理员手机号
   - `15914141177`
   - 会在系统启动时自动写成：
     - `ops_role_code = 00`
     - `is_admin = 1`
     - `invite_activated = 1`

4. 同一手机号允许：
   - 后台角色是 `00/11`
   - 同时前台用户级别仍然是 `22/33`

5. 当前动作后台 `admin.html` 已新增：
   - 测试期开关
   - 邀请码池查看
   - 用户 UID
   - 后台角色码
   - 前台用户级别码

### 每日额度与去重计数规则
当前测试期已接入真实额度规则：

1. 种子用户 `22`
   - 每日文本分析总字符数：`10000`
   - 每日音效下载总数：`200`
2. 普通用户 `33`
   - 每日文本分析总字符数：`5000`
   - 每日音效下载总数：`100`
3. 授权用户 / 管理员
   - 当前默认不扣每日额度
4. 文本额度已真实接入这些链路：
   - `text_analysis`
   - `action_verb_analysis`
   - `scene_building_analysis`
   - `text_narration_analysis`
5. 下载额度已真实接入这些链路：
   - 单个音效下载 `/api/sfx/file`
   - `SFX ZIP` 导出下载
6. 同一用户、同一天、完全相同文本内容：
   - 只在第一次计入字符额度
   - 后续重复提交相同文本不再重复扣字符
7. 后台可按手机号手动重置指定日期额度：
   - `POST /api/admin/users/reset-usage`

### 前台热度榜规则
当前已新增“用户端热度榜”功能，供用户直接查看最近下载热度：

1. 动作榜与场景榜都支持时间维度：
   - `过去1天`
   - `过去10天`
   - `过去30天`
   - `过去3个月`
2. 动作榜当前统计：
   - 下载最多的动作音效 Top10
   - 下载最多的通用音效 Top10
   - 下载最多的玄幻赛道音效 Top10
   - 下载最多的言情赛道音效 Top10
   - 下载最多的科幻赛道音效 Top10
   - 下载最多的悬疑赛道音效 Top10
3. 场景榜复用同样的统计结构，但数据只来自场景音效下载
4. 当前不把场景榜混进动作后台逻辑里，而是动作后台 / 场景后台共用一套排行榜接口
5. 排行榜默认以真实下载数据为准，但后台允许对单条展示做手动覆盖
6. 当前排行榜下载数据源：
   - 动作音效下载：`action_sfx_asset_download`
   - 场景音效下载：`scene_sfx_asset_download`
7. 用户端展示读取：
   - `GET /api/home/leaderboards`
8. 后台管理读取：
   - `GET /api/admin/leaderboards/config`
   - `POST /api/admin/leaderboards/layout`
   - `POST /api/admin/leaderboards/override`

### 双核心模块方向
当前系统后续不再只围绕“动作分析”扩展，而是明确转向两个并列核心模块：

1. 动作分析
2. 场景搭建

规则：

1. 场景搭建不是动作图谱推荐的附属区块
2. 场景搭建必须作为用户端独立模块存在
3. 两个模块后续可以在结果页做联动，但不混模块
4. 当动作分析与场景搭建都稳定后，后续再进入第三阶段：
   - 导演思维融合
   - 近景 / 中景 / 远景
   - 混响 / 延迟 / 空间调度等创作建议

注意：

当前阶段，场景搭建不承担导演思维分析，不替用户做创作决策，只针对文本内容做场景识别、场景构成、图谱命中、素材召回与下载支持。

### 模块解耦约束
从当前开始，必须遵守：

1. 动作分析后台继续保留在 `admin.html`
2. 场景搭建后台独立建设在 `scene.html`
3. 场景模块后端服务、数据表、Neo4j 关系均单独建设
4. 不再把 scene 逻辑继续堆进 action 的后台页面与业务表
5. 第三模块“导演思维融合”未来作为上层整合，不反向要求底层耦合

### 命名规则
统一展示名：
- `{音效名}（{类型}-{赛道}）`

例子：
- `吐（组合-玄幻）`
- `手势摩擦声（直达-玄幻）`

### 音效分类
- `直达音效`：如 `摩擦声`、`挥手声`
- `组合音效`：如 `掐诀`、`吐`

### 同一素材可跨赛道复用
- 物理文件不要求重复存两份
- 业务关系允许同时挂到多个赛道
- 用户下载时仍按赛道展示不同 display name

### 状态逻辑
业务状态：
- `pending`：待合并
- `partial`：部分补齐
- `ready_to_notify`：已补齐待通知
- `merged`：全补齐视图

提醒状态：
- `未通知`
- `已通知`

注意：
- `notified` 不是业务状态终点
- 当前 UI 视图要求：
  - 已通知的数据不再出现在“已补齐待通知”视图中

## 当前已完成功能

### 用户端
- 动作图谱推荐支持：
  - 列表视图
  - 图谱视图
- 已去掉“报告模式”入口，用户端不再区分该模式
- “为什么推荐这些音效”解释区
- 命中素材表只保留表格展示
- `待补子级音效` 使用 `? / ✓` 状态展示
- 人物动作提取表格已去掉“判定原因”
- 用户端首页已增加场景后台独立入口提示
- 用户端首页已增加场景搭建独立主链路按钮：
  - 场景搭建分析
  - 场景音效推荐
  - 场景补充单
- 用户端下载行为已改为“静默下载”：
  - 不再覆盖当前结果页
  - 不再额外显示下载中的执行日志
- 用户端结果区已增加“今日用量”可视面板，便于复测额度逻辑

### 运营端
- 动作补充单按父节点分组
- 逐词上传
- 动作图谱维护：
  - 两级下拉选择
  - 通用层 / 赛道层维护
  - 通用层继承屏蔽 / 恢复继承
  - 继承屏蔽池 / 屏蔽后命中复核
  - 节点级清理
  - 通用层与赛道层双向迁移
- 场景后台独立壳子已建立：
  - `/Users/demo/Documents/New project/musicForArt/frontend/scene.html`
- 场景图谱主数据已建立：
  - `/Users/demo/Documents/New project/musicForArt/backend/assets/scene/scene_graph.json`
- 场景图谱管理服务已建立：
  - `/Users/demo/Documents/New project/musicForArt/backend/app/services/scene_graph_manage.py`
- 场景图谱 Neo4j 骨架已建立：
  - `/Users/demo/Documents/New project/musicForArt/backend/app/services/scene_graph_neo4j.py`
- 顶部通知目标改为下拉选择（手机号 + 补充单 ID）
- 用户与测试期管理已支持：
  - 前台等级与后台角色分离
  - 每日文本 / 下载额度编辑
  - 指定日期用量查看
  - 按手机号手动重置当日用量
  - 用户列表每页 6 条分页
- 运营行为审计已支持：
  - 行为总览卡
  - 中文业务功能分布
  - 最近行为时间线分页
  - 原始 JSON 折叠查看
- Neo4j 父节点浏览器：
  - 筛选
  - 排序
  - 摘要卡
  - 今日优先补充/通知
  - 列表 / 图谱双视图
- 父节点详情支持：
  - 子级关系
  - 已补 / 待补
  - 补充单状态
  - 节点内通知

### Neo4j
- 本地 Neo4j 已安装并可运行
- ActionNode 已写入业务状态：
  - `supplement_item_count`
  - `covered_count`
  - `pending_count`
  - `completion_ratio`
  - `ready_to_notify_count`
  - `notified_count`

## 当前最关键的已修 bug

### 1. 已通知仍出现在 ready_to_notify
已修：
- `ready_to_notify` 视图只显示：
  - 业务状态为 `ready_to_notify`
  - 且 `notified_at` 为空
- 已通知只出现在：
  - `全部状态`
  - `已通知用户`

### 2. 用户端命中素材但 `待补组合音效` 仍是问号
已修：
- 用户端 `? / ✓` 改为读取全局覆盖结果
- 不再只看当前一次补充单

### 3. 第二次同文复测时历史已补素材失效
已修：
- 核心原因：之前只按当前 supplement item 看素材，未按父节点全局复用
- 现在改成：
  - 按 `赛道 + target_head` 生成父节点 key
  - 从历史 `ActionSupplementAsset` 全局聚合已入库素材
  - 用户端、运营端、Neo4j 三边统一读这套覆盖结果

例子：
- `玄幻::吐` 现在能全局读到历史 `吐.mp3`
- 第二次同文复测不应再把 `吐（组合）` 变回问号

### 4. 同音效词跨父节点复用
已修：
- 如果 `玄幻::飞身` 已补过 `快速移动声`
- 而 `玄幻::躲闪` 也挂了 `快速移动声`
- 那么 `躲闪` 现在也应直接命中这份素材

### 5. 无明确音效词的动作自动生成保底组合音效
已修：
- 对 `骑`、`挥`、`操纵` 这类前台已展示但图谱里暂无 `sfx_terms` 的动作
- 系统自动生成保底组合音效：
  - `骑（组合）`
  - `挥（组合）`
  - `操纵（组合）`
- 这样用户端有可下载/可匹配路径，运营端也有上传入口

### 6. 通知下拉框独立于主列表状态
已修：
- 顶部通知下拉框不再跟随 `pending / merged / 全部状态` 变化
- 只展示：
  - `ready_to_notify`
  - 且 `未通知`
  的单条补充单
- 同时保留表格勾选批量通知

## 当前需要继续重点盯的链路
如果用户继续反馈“第二次复测仍失效”，优先检查：
1. `action_sfx_graph.py`
   - `load_action_node_coverage`
   - `build_action_sfx_recommendation`
2. `main.py`
   - `ops_action_supplements`
   - `ops_action_supplements_merge`
   - `analyze_action_graph_draft_api`
3. `action_graph_neo4j.py`
   - `_business_stats_by_node_key`

## 2026-03-28 本地新增口径

### 动作图谱维护状态展示修正
- `结构维护：元数据与赛道分配` 中，同一个词如果真实同时存在于：
  - 通用元数据层
  - 任意赛道特化层
- 那么无论当前是从 `通用元数据` 视角进入，还是从某个赛道视角进入：
  - 状态卡都应如实显示两层已配置
- 当前实现方式：
  - 后端 `get_action_graph_node_layers(...)` 新增聚合字段：
    - `genre_layer_any_exists`
    - `genre_layer_any_genres`
  - 前端状态卡和来源解释优先按真实全局状态展示
- 注意：
  - 这次只修“状态展示口径”
  - 不改变赛道层的实际编辑入口
  - 赛道特化维护仍然只在明确赛道上下文下保存

## 新增：场景搭建模块的大框架

### 一、产品定位
场景搭建与动作分析同级，都是用户端核心功能，目标是解决有声书行业中的“声音世界搭建”痛点。

动作分析解决：

1. 人物做了什么
2. 需要哪些动作音效

场景搭建解决：

1. 这一段文本属于什么场景
2. 场景由哪些可听见的元素构成
3. 场景应该命中哪些场景音效
4. 当前有哪些素材可直接下载
5. 还缺哪些场景音效素材

### 二、明确边界
场景搭建当前只做：

1. 文本场景识别
2. 场景构成拆解
3. 场景图谱命中
4. 场景音效召回与下载
5. 场景补充缺口生成

场景搭建当前不做：

1. 导演思维分析
2. 镜头语言分析
3. 近景 / 中景 / 远景的创作决策
4. 混响、延迟、空间设计等创作者能力替代

这些属于后续“第三模块：导演思维融合”阶段再做。

### 三、系统主链路
场景搭建建议沿用当前动作分析的主架构：

1. 用户上传文本
2. LLM 产出场景结论
3. 系统基于结论命中 Neo4j / 场景图谱
4. 命中场景关联词
5. 命中场景音效素材
6. 输出可下载结果与缺口
7. 缺口进入后续运营补充链路

### 四、场景搭建模块的核心输出
用户端建议最少包含：

1. 场景主识别
2. 场景构成要素
3. 场景关联词
4. 场景音效来源（可下载）
5. 待补场景音效

### 五、最重要的设计原则：场景可复制性
场景搭建与动作分析不同，它天然更强调“可复用的大场景集合”。

原因：

1. 场景文本写法会变化
2. 但声音制作上会收敛到稳定的大场景类型
3. 所以系统后续必须逐步沉淀可复用场景库

例如：

1. 深夜山林
2. 雨夜街巷
3. 客栈大堂
4. 宫廷长廊
5. 战场边缘

这意味着场景搭建模块后续必须建设：

1. 通用场景集合
2. 赛道场景集合
3. 场景构成词集合
4. 场景音效大集合

目标：

1. 新文本优先命中已有大场景
2. 不要每次从零生成全新场景
3. 只有明显不属于现有集合时，才新增候选

### 六、Neo4j / 图谱层建议方向
场景搭建图谱后续建议包含：

1. 场景主节点
2. 场景构成词
3. 直达场景音效
4. 组合场景音效
5. 场景模板 / 场景集合层

场景模板 / 场景集合层是关键，它决定系统的复用性与稳定性。

### 七、提示词来源
场景搭建模块的 LLM 提示词，不由当前开发阶段拍脑袋编写。

后续以用户提供的“有声书老师教学逐字稿”为主输入，由系统从逐字稿中提炼：

1. 场景识别提示词
2. 场景构成拆解提示词
3. 场景图谱命中所需结构化字段

### 八、开发优先级
当前优先级是：

1. 先沉淀模块框架与数据结构
2. 再从老师逐字稿提炼提示词
3. 再开始场景搭建模块开发

不要直接跳过框架设计开始写代码。

### 九、当前已起的后端骨架
当前 scene 模块后端已经起壳：

1. 场景分析服务：
   - `/Users/demo/Documents/New project/musicForArt/backend/app/services/scene_building.py`
2. 场景音效推荐服务：
   - `/Users/demo/Documents/New project/musicForArt/backend/app/services/scene_sfx_graph.py`
3. Prompt：
   - `/Users/demo/Documents/New project/musicForArt/backend/app/prompts/V3-scene_building_task.txt`
   - `/Users/demo/Documents/New project/musicForArt/backend/app/prompts/V3-scene_building_task_retry.txt`
4. 场景分析接口：
   - `POST /api/analysis/<project_id>/scene-building`
5. 场景音效推荐接口：
   - `POST /api/analysis/<project_id>/scene-sfx`
6. 场景补充单生成接口：
  - `POST /api/analysis/<project_id>/scene-supplements`
7. 场景补充单运营查询接口：
  - `GET /api/ops/scene-supplements`
8. 场景补充单运营动作接口：
  - `POST /api/ops/scene-supplements/merge`
  - `POST /api/ops/scene-supplements/notify`
9. 场景图谱维护接口：
  - `GET /api/scene-graph/catalog`
  - `GET /api/scene-graph/node-layers`
  - `POST /api/scene-graph/node-layer`
  - `GET /api/scene-graph/collections`
  - `GET /api/scene-graph/templates`
  - `POST /api/scene-graph/template`
  - `GET /api/scene-graph/neo4j-status`
  - `POST /api/scene-graph/neo4j-sync`

### 十、当前已起的数据表
当前 scene 模块已新增独立表：

1. `scene_analysis`
2. `scene_supplement_task`
3. `scene_supplement_asset`

说明：

1. 这三张表与 action 模块分开
2. 当前先满足独立链路起壳
3. 后续再补 scene 侧完整运营功能、入库与通知

## 关键文件
- 后端主入口：
  - `/Users/demo/Documents/New project/musicForArt/backend/app/main.py`
- 动作图谱推荐：
  - `/Users/demo/Documents/New project/musicForArt/backend/app/services/action_sfx_graph.py`
- Neo4j 图谱服务：
  - `/Users/demo/Documents/New project/musicForArt/backend/app/services/action_graph_neo4j.py`
- 用户端页面：
  - `/Users/demo/Documents/New project/musicForArt/frontend/index.html`
- 运营后台页面：
  - `/Users/demo/Documents/New project/musicForArt/frontend/admin.html`
- 场景搭建策略文档：
  - `/Users/demo/Documents/New project/musicForArt/docs/SCENE_BUILDING_MODULE_STRATEGY.md`
- 场景搭建输出结构文档：
  - `/Users/demo/Documents/New project/musicForArt/docs/SCENE_BUILDING_OUTPUT_SCHEMA.md`
- 场景搭建 Prompt v1：
  - `/Users/demo/Documents/New project/musicForArt/docs/SCENE_BUILDING_PROMPT_V1.md`
- 场景模块解耦方案：
  - `/Users/demo/Documents/New project/musicForArt/docs/SCENE_MODULE_DECOUPLING_PLAN.md`
- 动作图谱数据：
  - `/Users/demo/Documents/New project/musicForArt/backend/assets/sfx/action_graph.json`
- 数据库：
  - `/Users/demo/Documents/New project/musicForArt/backend/music_for_art.db`

## 本地文件存储
当前开发环境音效统一存储目录：
- `/Users/demo/Documents/New project/musicForArt/backend/assets/sfx/`

当前测试数据清理策略：
- 保留 `user_account`
- 清空业务表：
  - `projects`
  - `audio_analysis`
  - `text_analysis`
  - `narration_analysis`
  - `fusion_plan`
  - `action_supplement_task`
  - `action_supplement_asset`
  - `user_operation_log`
  - `auth_code`
  - `auth_session`
- 清理测试音效文件，仅保留：
  - `README.md`
  - `semantic_lexicon.json`
  - `action_graph.json`

上线建议：
- 音效文件：腾讯云 COS
- 业务元数据：MySQL
- 图谱关系：Neo4j
- 本地磁盘：仅临时缓存

## Neo4j 本地运行
Neo4j 目录：
- `/Users/demo/Documents/New project/musicForArt/.local/neo4j/neo4j-install/neo4j/2026.02.2/libexec`

JRE 目录：
- `/Users/demo/Documents/New project/musicForArt/.local/jdks/zulu21-jre/zulu-21.jre/Contents/Home`

启动：
```bash
cd "/Users/demo/Documents/New project/musicForArt/.local/neo4j/neo4j-install/neo4j/2026.02.2/libexec"
export JAVA_HOME="/Users/demo/Documents/New project/musicForArt/.local/jdks/zulu21-jre/zulu-21.jre/Contents/Home"
./bin/neo4j start
./bin/neo4j status
```

## 标准启动命令

### 启动后端
```bash
cd "/Users/demo/Documents/New project/musicForArt/backend"
source .venv/bin/activate
pkill -f "python -m app.main" || true
nohup python -m app.main >/tmp/musicforart-backend.log 2>&1 &
sleep 3
curl -sS http://127.0.0.1:8010/health
```

### 启动前端
```bash
cd "/Users/demo/Documents/New project/musicForArt/frontend"
pkill -f "python3 -m http.server 5200" || true
nohup python3 -m http.server 5200 --bind 127.0.0.1 >/tmp/musicforart-frontend.log 2>&1 &
sleep 2
curl -sS http://127.0.0.1:5200/ | head -n 5
curl -sS http://127.0.0.1:5200/admin.html | head -n 5
```

### 验证 Neo4j
```bash
curl -sS http://127.0.0.1:8010/api/action-graph/neo4j-status
```

期待：
- `neo4j_configured: true`
- `neo4j_connected: true`

## 排障命令
```bash
tail -n 120 /tmp/musicforart-backend.log
tail -n 120 /tmp/musicforart-frontend.log
lsof -nP -iTCP:8090 -sTCP:LISTEN
lsof -nP -iTCP:5200 -sTCP:LISTEN
```

## 当前协作方式
- 用户通常会批量测试后统一反馈
- 回答时优先：
  1. 明确是否已修
  2. 说明根因
  3. 给最短部署命令
  4. 给复测步骤和期待值

## Scene 模块最新进展
- `scene` 已确立为与动作分析并列的独立核心模块，不再继续耦合进 `admin.html`
- 独立后台页：
  - `/Users/demo/Documents/New project/musicForArt/frontend/scene.html`
- 独立后端服务：
  - `scene_building.py`
  - `scene_sfx_graph.py`
  - `scene_graph_manage.py`
  - `scene_graph_neo4j.py`
- 独立主数据：
  - `/Users/demo/Documents/New project/musicForArt/backend/assets/scene/scene_graph.json`

### Scene 已落地链路
- 用户端：
  - `场景搭建分析`
  - `场景音效推荐`
  - `场景补充单`
- 后端接口：
  - `POST /api/analysis/<project_id>/scene-building`
  - `POST /api/analysis/<project_id>/scene-sfx`
  - `POST /api/analysis/<project_id>/scene-supplements`
  - `GET /api/ops/scene-supplements`
  - `POST /api/ops/scene-supplements/merge`
  - `POST /api/ops/scene-supplements/notify`

### Scene 图谱维护
- 已支持：
  - `GET /api/scene-graph/catalog`
  - `GET /api/scene-graph/node-layers`
  - `POST /api/scene-graph/node-layer`
  - `POST /api/scene-graph/delete-node`
  - `GET /api/scene-graph/collections`
  - `GET /api/scene-graph/templates`
  - `POST /api/scene-graph/template`
  - `GET /api/scene-graph/neo4j-status`
  - `POST /api/scene-graph/neo4j-sync`
  - `POST /api/scene-graph/promote`
  - `POST /api/scene-graph/demote`
- 节点级双向迁移已接通：
  - 赛道层节点 -> 通用层
  - 通用层节点 -> 目标赛道层
- `scene.html` 中已新增“节点级迁移”操作区：
  - `迁移到通用层，并保留赛道层`
  - `迁移到通用层，并从赛道层移除`
  - `迁移到赛道层，并保留通用层`
  - `迁移到赛道层，并从通用层移除`
- `scene.html` 中已新增“删除当前层节点”操作：
  - 通用层视角删除时，会明确提醒会影响所有继承该通用层的赛道
  - 赛道层视角删除时，只删除当前赛道下的真实节点
- `scene.html` 中已新增“新建空白场景节点”入口：
  - 可直接创建通用层或赛道层的空白节点
  - 运营填写内容后即可保存当前层，无需先等目录里已有节点
  - 图谱编辑区的两个写操作按钮现在也是“状态联动”的：
    - `新建空白节点`
      - 未输入节点名时禁用，并显示 `请先输入节点名`
      - 输入节点名后自动恢复可点击
    - `保存当前层`
      - 未选择节点、也未创建空白节点时禁用，并显示 `请先选择或新建节点`
      - 一旦已选节点或已创建空白节点，就自动恢复为 `保存当前层`
- 场景节点下拉目录现在会直接展示：
  - `通用+赛道`
  - `赛道真实节点`
  - `通用继承`
  - 以及模板名、场景集合名
- 目录下方会显示当前层/赛道的节点数量摘要，帮助运营在加载前先判断目录状态
- `scene.html` 已新增“节点来源解释”卡：
  - 会直接解释当前节点是 `通用层真实节点`、`赛道真实节点`、`通用层继承` 还是 `通用层 + 赛道层`
  - 同时给运营明确的维护提示，减少把“继承节点”误当成“赛道真实节点”的风险
- `scene_graph_manage.py` 的节点详情现在会返回 `inheritance_scope`
  - `inheriting_genres`
  - `real_genre_nodes`
- `scene.html` 的来源解释卡现在会进一步显示“影响范围”
  - 删除通用层节点前，能直接看到还会影响哪些赛道继承
  - 删除赛道层真实节点时，也会说明只影响当前赛道
- `scene.html` 的节点级迁移 / 删除区，现在也补成“状态联动”
  - 初始未加载节点时：
    - 迁移按钮统一显示 `请先加载节点`
    - 删除按钮统一显示 `请先加载节点`
  - 通用层视角：
    - 上迁按钮会禁用并显示 `通用层视角不可上迁`
    - 下迁按钮按通用层真实节点是否存在决定是否可用
  - 赛道层视角：
    - 下迁按钮会禁用并显示 `赛道层视角不可下迁`
    - 上迁按钮按赛道真实节点是否存在决定是否可用
  - 删除当前层节点后：
    - 迁移 / 删除区会自动回退到“请先重新加载场景节点”的空状态
- `scene.html` 已新增“节点摘要”区：
  - 汇总模板、场景集合、来源状态、影响继承赛道、已存在真实赛道节点
  - 让运营在顶部就能快速判断当前节点的大致维护状态

### Scene 集合维护
- `scene.html` 中的大场景集合不再只是预览，已支持：
  - 修改集合名称
  - 清空当前集合引用
- 后端接口：
  - `POST /api/scene-graph/collection-rename`
  - `POST /api/scene-graph/collection-delete`
- 当前规则：
  - 集合改名会同步更新引用它的节点与模板
  - 清空集合引用只会移除集合名，不会删除节点或模板本身
- `scene.html` 的节点维护现在已支持“模板 -> 场景集合”轻自动联动：
  - 节点模板命中已有模板名时，会自动带出该模板所属的场景集合
  - 保存节点时，如果集合为空，也会优先回填模板关联集合
- `scene.html` 现在会在集合/模板操作区直接提示影响范围：
  - 集合选择后，会提示当前集合引用了多少通用层节点、多少赛道层节点
  - 模板选择后，会提示当前模板被多少个节点引用
  - 让运营在改名、删除、清空引用前，就能明确知道会影响“节点 / 模板 / 两者”
- `scene.html` 的集合维护区，现在也补成“状态联动”：
  - 未选择集合时：
    - `修改集合名称` 显示 `请先选择集合`
    - `清空当前集合引用` 显示 `请先选择集合`
  - 已选集合但未输入新名称时：
    - `修改集合名称` 显示 `请先输入新名称`
  - 只有“已选集合 + 已填新名称”时，才恢复为真正的改名按钮

### Scene 模板维护
- 已支持：
  - 保存模板
  - 删除当前模板
- 模板删除不会自动删除已引用该模板的场景节点，但会返回受影响节点数量，供运营复核
- 模板维护区现在也补成“状态联动”：
  - 未输入模板名称时：
    - `保存模板` 显示 `请先输入模板名称`
  - 未选择模板、且当前也没有有效模板名称时：
    - `删除当前模板` 显示 `请先选择模板`
  - 进入有效状态后，按钮再恢复为 `保存模板 / 删除当前模板`
- 模板列表现在会直接展示：
  - `引用节点数`
  - `引用节点`
- 运营在选择模板时，会收到“当前模板已被多少个节点引用”的即时提示

### Scene 推荐展示
- 用户端场景模块现在会显示更明确的图谱来源：
  - `来源类型`
  - `模板命中`
  - `场景集合`
  - `含保底词`
- 用户端 `index.html` 的 scene 卡片现在增加了专用来源标签行：
  - `来源`
  - `模板`
  - `集合`
  - `含保底词`
- 目标是让场景搭建结果在视觉上更像独立核心模块，而不是动作分析旁边的一段普通文字
- 用户端 scene 总览现在还会直接统计：
  - 场景切换点
  - 模板命中场景数
  - 通用层命中场景数
  - 赛道层命中场景数
  - 保底生成场景数
  - 已有素材场景数（scene-sfx）
- 用户端第 `4.6` 步“场景补充单”现在也已有独立面板展示：
  - 场景补充单总览
  - 场景补充单明细
  - 第 4 / 4.5 / 4.6 三段关系说明
  - `4.6` 面板里现在还直接给了后续处理入口提示：
    - `打开 scene 后台`
    - 并明确说明后续应进入：
      - 场景补充单运营
      - 场景图谱维护
  - 三段关系说明里也补了“用户端到后台的闭环”：
    - 用户端负责识别、推荐、写入补充单
    - scene 后台负责补素材、改图谱、调模板与集合
  - 当前又补了一层“结果层级”：
    - `结果层级：可转后台补库`
    - `结果层级：待复核`
    - `结果层级：处理中`
    - `结果层级：已补齐`
  - 总览也新增了：
    - `可直接转后台补库`
    - `建议先复核图谱`
  - 目的是让用户在进入 scene 后台前，就能先判断：
    - 这条补充单是不是已经可以直接交给后台补素材
    - 还是应先回到 scene 图谱检查推荐方向
- 用户端按钮区已补 scene 模块链路提示：
  - 先 `4) 场景搭建分析`
  - 再 `4.5) 场景音效推荐`
  - 最后 `4.6) 场景补充单`
- 用户端 scene 三步按钮已改成“状态联动”：
  - `4) 场景搭建分析`：未创建项目时禁用
  - `4.5) 场景音效推荐`：只有完成 `4)` 后才解锁
  - `4.6) 场景补充单`：只有完成 `4.5)` 后才解锁
  - 按钮文案也会联动变化：
    - 未解锁时显示 `待第4步 / 待第4.5步`
    - 已完成一轮后显示 `可重跑`
  - 按钮下方提示会根据当前进度动态切换，不再主要依赖点击后报错来引导用户下一步
  - 新建项目、重做 `4)`、重做 `4.5)`、重做 `4.6)` 时，现在都会先清理对应的旧结果状态：
    - 重做 `4)`：会先清空 `4 / 4.5 / 4.6` 的旧结果
    - 重做 `4.5)`：会先清空 `4.5 / 4.6` 的旧结果
    - 重做 `4.6)`：会先清空 `4.6` 的旧结果
  - 目的：避免第 `4)` 或 `4.5)` 已经开始重跑时，页面还残留上一轮的下游结果，造成“看起来像还能沿用”的误解
  - 现在重跑时还会先给出明确的“重跑中占位提示”：
    - 重跑 `4)`：提示已清空上一轮的场景分析、场景音效推荐、场景补充单结果
    - 重跑 `4.5)`：提示已清空上一轮的场景音效推荐、场景补充单结果
    - 重跑 `4.6)`：提示已清空上一轮的场景补充单结果
  - 目的：让用户在请求返回前，能明确知道当前是“正在重跑该步骤”，而不是误以为页面短暂空白是异常
- 用户端 `scene` 三段结果又补了一轮“阶段感”：
  - `4) 场景搭建分析`：顶部会显示“当前阶段：场景识别”
  - `4.5) 场景音效推荐`：顶部会显示“当前阶段：图谱命中与素材召回”
  - `4.6) 场景补充单`：顶部会显示“当前阶段：缺口写入补充单”
  - 每段总览下方都补了很短的“这一步在回答什么 / 下一步去哪里”的说明，降低三段结果之间的跳跃感
- 用户端 `scene` 结果又补了两类风险提醒：
  - `保底生成提醒`
    - 当场景主要依赖系统保底结果、而非稳定图谱命中时，单独拎出来提示
  - `缺口提醒`
    - 当场景音效推荐仍存在明显缺口时，单独提示优先涉及哪些场景
  - 目的是让用户更容易区分：
    - 哪些是相对稳定的图谱结果
    - 哪些仍需要继续维护 scene 图谱或写入场景补充单
- 用户端 `scene` 卡片现在也已补“建议下一步”：
  - `4) 场景搭建分析` 的每个场景卡片，会根据：
    - 通用层命中
    - 赛道层命中
    - 模板命中
    - 是否保底
    给出下一步建议
  - `4.5) 场景音效推荐` 的每个场景卡片，会根据：
    - 是否已有素材
    - 是否仍有缺口
    - 是否同时存在保底
    给出下一步建议
  - 目标：让用户在看到单个场景时，就知道该去 scene 图谱维护、继续做音效推荐，还是写入场景补充单
- 用户端 `scene` 结果页现在还补了统一的“结果层级”：
  - `4) 场景搭建分析`
    - 每个场景卡片都会显示：
      - `结果层级：稳定图谱`
      - `结果层级：通用图谱`
      - `结果层级：模板过渡`
      - `结果层级：保底结果`
      - `结果层级：待完善`
    - 总览也新增了 `结果较稳定场景`
  - `4.5) 场景音效推荐`
    - 每个场景卡片都会显示：
      - `结果层级：可直接使用`
      - `结果层级：部分命中`
      - `结果层级：待补素材`
      - `结果层级：保底待补`
      - `结果层级：待确认`
    - 总览也新增了 `可直接使用场景`
  - 这层的作用，是把“来源标签 / 风险提醒 / 下一步建议”再往前收口成一套统一口径，让用户和运营更容易判断当前结果是否已经够稳定、该继续下载使用，还是应优先回到 scene 图谱维护
- 当前设计原则：
  - 场景分析与动作分析分开建设
  - 未来第三模块再做导演思维层的上层融合

### Scene 后台文案
- `scene.html` 已做一轮文案降噪：
  - 顶部与模块说明改短
  - “调试输出”改成“请求回显”
  - 多处开发态说明已压成更产品化的短句
- `scene.html` 已新增快捷导航：
  - 场景补充单
  - 场景图谱
  - 场景集合
  - 场景模板
  - Scene Neo4j
  - 请求回显
- “请求回显”已改成默认折叠，降低长页面滚动干扰
- `scene.html` 的 `Scene Neo4j` 区，现在也补成“状态联动”：
  - 未登录时：
    - `查看 Neo4j 状态` 显示 `请先登录后查看`
    - `同步场景图谱到 Neo4j` 显示 `请先登录后同步`
  - 已登录但还没读取状态时：
    - 同步按钮显示 `先查看 Neo4j 状态`
  - 读取状态后：
    - 若未配置：按钮显示 `Neo4j 未配置` 并禁用
    - 若已配置但未连通：按钮显示 `Neo4j 未连接，仍尝试同步`
    - 若已配置且连通：按钮显示 `同步场景图谱到 Neo4j`
  - 区块下方提示文案也会随状态同步变化，不再只依赖点击后的返回信息
- `scene.html` 的顶部登录区 / 场景补充单加载区，现在也补成“状态联动”：
  - 登录区：
    - 未输入有效手机号时，`发送验证码` 显示 `请先输入手机号`
    - 未输入有效手机号或验证码时，`登录后台` 显示 `请先输入验证码`
    - 输入满足条件后，按钮才恢复为 `发送验证码 / 登录后台`
  - 场景补充单运营区：
    - 未登录时，`加载场景补充单` 显示 `请先登录后加载`
    - 未输入天数时，会显示 `请先输入天数`
    - `清空当前筛选` 在未登录时也会禁用，并显示 `请先登录后操作`
    - 正在拉取补充单列表时，`加载场景补充单` 会显示 `加载中...`
- `scene.html` 现在还补了一层“函数入口登录兜底”：
  - 即使绕过按钮状态，下面这些核心加载函数也会先拦住未登录调用：
    - `loadSceneSupplements`
    - `loadSceneGraphCatalog`
    - `loadSceneGraphTopPriority`
    - `loadSceneGraphNode`
    - `loadSceneCollections`
    - `loadSceneTemplates`
    - `loadSceneTermSuggestions`
    - `loadSceneNeo4jStatus`
    - `syncSceneNeo4j`
  - 目的不是重复提示，而是避免在未登录状态下直接打出一串 `401`，把真正问题变成“页面坏了”的错觉
- `scene.html` 的写操作函数，也已补“函数入口登录兜底”：
  - 即使绕过按钮禁用，下面这些写操作也会先在函数入口拦住未登录调用，并给出明确提示：
    - `renameSceneCollection`
    - `deleteSceneCollection`
    - `saveSceneTemplate`
    - `deleteSceneTemplate`
    - `openSceneGraphFromSupplement`
    - `saveSceneGraphLayer`
    - `promoteSceneNode`
    - `demoteSceneNode`
    - `deleteSceneCurrentLayerNode`
    - `mergeSceneAsset`
    - `notifySceneSupplement`
  - 目的同样是减少未登录状态下的误操作和 `401` 噪音，让运营看到的是“请先登录后操作”，而不是误以为页面或接口损坏
- `scene.html` 的高频异步动作，现已补“请求中防连点”：
  - 登录区：
    - `发送验证码` 请求中会显示 `发送中...`
    - `登录后台` 请求中会显示 `登录中...`
  - 集合维护区：
    - 改名请求中会显示 `修改中...`
    - 清空集合引用请求中会显示 `清空中...`
  - 模板维护区：
    - 保存模板请求中会显示 `保存中...`
    - 删除模板请求中会显示 `删除中...`
  - 图谱维护区：
    - `保存当前层` 请求中会显示 `保存中...`
    - 节点级上迁 / 下迁请求中会显示 `迁移中...`
    - `删除当前层节点` 请求中会显示 `删除中...`
    - `加载当前最高优先节点` 请求中会显示 `加载中...`
    - `加载场景节点` 请求中会显示 `加载中...`
  - 场景补充单卡片区：
    - `上传并合并入库` 请求中会显示 `上传中...`
    - `标记已通知` 请求中会显示 `通知中...`
    - `查看该场景图谱 / 去图谱维护` 请求中会显示 `定位中...`
  - 请求完成后，按钮会回到当前应有的状态联动文案，减少网络慢时的重复点击和重复提交
- `scene.html` 的按钮旁提示（`inline-status`）现在也已统一状态样式：
  - 进行中：暖色提示
  - 成功完成：绿色提示
  - 失败 / 缺条件：红色提示
  - 普通说明：默认灰色
  - 这层不是新业务逻辑，而是把保存、迁移、删除、上传、通知这些动作的就地反馈统一成更容易扫读的视觉语言，方便后续集中复测时快速识别哪里成功、哪里失败、哪里还在处理中
- `scene.html` 的场景补充单卡片按钮，现在也已做“未登录状态显式表达”：
  - `查看该场景图谱 / 去图谱维护`
    - 未登录时：按钮直接显示 `请先登录后定位`
  - `上传并合并入库`
    - 未登录时：按钮直接显示 `请先登录后上传`
    - 同时函数层 `refreshSceneMergeState` 也会直接给出 `请先登录后上传并合并`
  - `通知用户`
    - 未登录时：按钮直接显示 `请先登录后通知`
    - 提示文案也会同步解释必须先登录 scene 后台

### Scene 图谱维护体验
- `scene.html` 的图谱维护表单已补“实时标签预览”：
  - 背景元素
  - 特征元素
  - 细节元素
  - 支撑场景音效
  - 补充层次音效
- 每个字段旁边都已加“清空”按钮
- 当前策略是不推翻文本输入，而是在文本输入基础上补实时拆词预览，降低运营对纯文本表单的阅读负担
- `scene.html` 现已支持“快速加入常用项”：
  - 根据当前模板 / 场景集合，从已有 scene 图谱中聚合同模板、同集合的常用元素与音效词
  - 点击即可加入对应字段
- 后端接口：
  - `GET /api/scene-graph/term-suggestions`
- `scene.html` 已把“场景补充单运营”与“场景图谱维护”接通：
  - 每条场景补充单卡片新增“查看该场景图谱”
  - 表格视图新增“去图谱维护”
  - 跳转逻辑：
    - 优先定位当前赛道真实节点
    - 其次回到通用层真实节点
    - 若图谱中尚无现成节点，则自动定位到对应赛道的新建节点入口
  - 运营提示也已同步补到卡片中：建议先核对场景图谱，再决定是直接补素材，还是先调整场景推荐
- `scene.html` 还补了图谱维护 -> 补充单的反向提示：
  - 当前节点若已有关联场景补充单，会在“相关场景补充单”区显示条数、待补词数、可通知数
  - 并提供“回到场景补充单区查看”按钮
- `scene` 场景补充单现在也已接入“来源驱动的素材版本选择”：
  - 后端 `/api/ops/scene-supplements` 已返回：
    - `sfx_term_items`
    - `missing_sfx_term_items`
    - 每个词的 `source / source_label / default_asset_scope / scope_hint`
  - 前端 `scene.html` 会把场景音效词、待补场景音效直接标出：
    - 通用层
    - 赛道层
    - 通用+赛道
    - 未标注
  - 上传卡片会根据来源自动默认：
    - 只来自通用层 -> 默认通用版
    - 只来自赛道层 -> 默认当前赛道版
    - 通用+赛道 -> 不自动替运营决定，明确提示先选素材版本
- `scene.html` 的上传入库交互已进一步收口为“防误操作模式”：
  - 上传按钮默认禁用
  - 必须同时满足：
    - 已选场景音效词
    - 已选音效文件
    - 若来源为通用+赛道 / 未标注，还必须已明确选择素材版本
  - 前端不再只是提示，而是直接阻止误传；后端 `mergeSceneAsset()` 也补了 `asset_scope` 必填校验
- `scene` 补充单上传还补了一层“减点击”：
  - 如果一条场景补充单当前只剩 1 个待补词
  - 前端会自动帮运营选中这个词
  - 并继续按来源规则自动同步素材版本提示与按钮状态
- `scene` 场景补充单卡片又做了一轮收口：
  - 待补场景音效区现在会显示“来源摘要”，快速汇总当前补充单里：
    - 通用层词数
    - 赛道层词数
    - 通用+赛道词数
    - 未标注词数
  - 现在又补了统一的“补充单层级”：
    - `补充单层级：已补齐`
    - `补充单层级：优先补素材`
    - `补充单层级：先复核图谱`
    - `补充单层级：先判定版本`
    - `补充单层级：继续补素材`
  - 卡片里除了来源摘要，还会直接显示 `当前建议`
  - 表格视图也新增了 `补充单层级` 列
  - 现在表格视图中的补充单层级也改成了和卡片视图一致的彩色标签
  - 目的：让运营不只是看到词和来源，还能更快判断这条单是应直接补素材，还是先回图谱核对，或者先判断通用/赛道版本
  - 后台顶部统计卡现在也已和这套口径打通：
    - `优先补素材`
    - `先复核图谱`
    - `先判定版本`
    - `继续补素材 / 已补齐`
  - 这样 scene 用户端 `4.6` 和 scene 后台补充单总览，就开始共享同一套“该先做什么”的判断语言
  - 若当前补充单已经没有待补词，则不再继续显示上传入口
  - 改为提示运营：
    - 可以直接检查通知状态
    - 或回到场景图谱继续优化推荐
  - 通知按钮现在也按状态分层：
    - 已通知：按钮显示 `已通知用户`，禁用
    - 可通知：按钮显示 `标记已通知`
    - 未达到条件：按钮显示 `暂不可通知`，禁用
  - 卡片右侧提示也会同步显示：
    - `当前可通知用户`
    - `当前未达到可通知状态`
    - 或具体的 `已通知时间`
- `scene.html` 的场景补充单运营区已补“仅看仍有待补词的补充单”：
  - 这是前端轻筛选，只影响当前页面展示
  - 不会修改补充单状态
  - 开启后，统计卡片、卡片视图、表格视图、原始数据视图都会同步切到过滤后的结果
  - 这层筛选现在还会影响场景图谱目录中的“补充单热度”统计口径
  - 也就是：如果当前只看仍有待补词的补充单，图谱目录里的 `补充单:N / 待补:M` 也会同步只按这批可见补充单来算
- `scene.html` 的场景补充单列表现在又补了一层“运营优先级排序 + 层级筛选”：
  - 列表会优先按 `补充单层级` 自动排序：
    - `先复核图谱`
    - `先判定版本`
    - `优先补素材`
    - `继续补素材`
    - `已补齐`
  - 同层级内再按：
    - `待补词数` 从多到少
    - `ready_to_notify 且未通知` 优先
    - 最后按场景名稳定排序
  - 目的：让最值得先处理、最容易出错的场景补充单自然浮到前面
  - 同时补充单工具栏新增了 `补充单层级` 筛选：
    - `全部层级`
    - `先复核图谱`
    - `先判定版本`
    - `优先补素材`
    - `继续补素材`
    - `已补齐`
  - 筛选摘要下方还新增了 `补充单层级` 快筛按钮：
    - `全部层级`
    - `先复核图谱`
    - `先判定版本`
    - `优先补素材`
    - `继续补素材`
    - `已补齐`
  - 快筛按钮现在会直接显示当前数量，如：
    - `先复核图谱 (3)`
    - `优先补素材 (5)`
  - 若某个层级当前数量为 `0`，对应按钮会直接置灰禁用
  - 目的：让运营不必每次都打开下拉框，可直接先看分布，再一键切到最常处理的层级
  - 这样运营除了按状态、天数、是否仍待补来筛，还能直接只看“先回图谱”或“先判版本”的单
  - 工具栏下方现在还会显示 `当前筛选摘要`：
    - 最近多少天
    - 当前状态筛选
    - 当前补充单层级筛选
    - 是否仅看仍有待补词
    - 当前展示模式
  - 并直接提醒：当前补充单列表已按处理优先级自动排序
  - 如果当前筛选后为空：
    - 页面会直接提示“当前勾选了仅看仍有待补词的补充单，所以列表为空”
    - 避免运营把空白区域误判成接口失败
- `scene.html` 的场景图谱目录选择层已接入“补充单热度”：
  - 场景节点下拉项现在会追加：
    - `补充单:N`
    - `待补:M`
  - 下拉框下方的目录提示也会显示：
    - 当前目录里“含补充单节点”数量
  - 目的是让运营在还没加载节点前，就能优先看到哪些场景已产生真实补充需求
- 这层目录逻辑又补了一次优先级排序：
  - 场景节点下拉现在会优先按：
    - 待补词数
    - 补充单数
    - 场景名
    排序
  - 也就是“补充压力越大”的节点会自然排在更前面
  - 目录提示文案里也会明确说明：当前已按待补优先级排序
- `scene.html` 图谱目录又补了一个轻筛选：
  - `仅看有补充单节点`
  - 适合运营在补充单压力较大时，先只看已产生真实补充需求的场景节点
  - 切换筛选时会尽量保留当前已选节点；目录提示也会显示“当前仅显示含补充单节点”
  - 如果该筛选下没有节点：
    - `sceneGraphQuickHint` 会直接提示当前没有“含补充单”的场景节点
    - 并建议取消该筛选后查看完整目录
- `scene.html` 图谱目录现已支持“搜索场景节点”：
  - 可按：
    - 场景名
    - 模板名
    - 场景集合名
    进行模糊筛选
  - 这层搜索与“仅看有补充单节点”“按待补优先级排序”可叠加使用
  - 目录提示会显示“当前已按关键词筛选”
  - 若关键词筛选结果为空：
    - `sceneGraphQuickHint` 会明确提示“当前关键词下没有匹配的场景节点”
- `scene.html` 图谱目录还补了一个快捷动作：
  - `加载当前最高优先节点`
  - 它会直接加载当前筛选结果里排在第一位的节点
  - 旁边会显示一行轻提示：
    - 当前最高优先节点是谁
    - 这条节点当前对应多少补充单 / 多少待补词
  - 图谱区两个核心按钮现在也是“状态感知”的：
    - 没有筛出节点时：
      - `加载当前最高优先节点` 会禁用并显示 `当前没有优先节点`
      - `加载场景节点` 会禁用并显示 `请先选择场景节点`
    - 一旦选中节点或筛出优先节点，按钮会自动恢复可点击状态
- `scene.html` 现已在两个长列表区都补了“清空当前筛选”：
  - 场景补充单运营：
    - 恢复 `最近 30 天 / 全部状态 / 卡片视图 / 不勾选仅看待补`
  - 场景图谱维护：
    - 清空关键词搜索
    - 取消“仅看有补充单节点”
  - 这两个“清空当前筛选”按钮现在也补成“状态联动”：
    - 如果当前已经是默认筛选口径，会禁用并显示 `当前无需清空`
    - 只有真的存在非默认筛选时，才恢复为 `清空当前筛选`
  - 这两个动作都只影响当前页面展示，不修改任何业务数据
- `scene.html` 这层联动又补稳了一次：
  - 每次刷新场景补充单列表后，会立即重绘场景图谱目录
  - 避免补充单数量 / 待补词数量变化后，节点下拉仍显示旧热度
- `scene` 补充单表格视图现在也与卡片视图对齐：
  - 场景音效词、待补词都直接显示来源标签
  - 新增“来源摘要”列
  - 避免运营在表格视图下丢失通用层 / 赛道层 / 通用+赛道的信息
- 上线前图谱策略已新增一条明确原则：
  - `action_graph.json` 与 `scene_graph.json` 不应完全等待用户文本被动生长
  - 应先带一版“行业基础种子图谱”上线
  - 目的：
    - 让系统一开始就具备基础命中能力
    - 让运营可先围绕基础种子补素材
    - 再用真实用户文本、补充单和图谱维护持续把图谱养大
- 本轮已开始执行第一批基础种子扩充：
  - 动作图谱：
    - 通用层动作由 `24` 个提升到 `37` 个
    - 新增高复用通用动作词：
      - `走`
      - `快走`
      - `慢走`
      - `跳`
      - `爬`
      - `飞`
      - `摩擦`
      - `蹲下`
      - `抬手`
      - `放下`
      - `挥手`
      - `触碰`
      - `拾起`
  - 场景图谱：
    - 通用场景由 `3` 个提升到 `12` 个
    - 场景模板由 `4` 个提升到 `16` 个
    - 新增通用场景包括：
      - `街道`
      - `雨夜街巷`
      - `房间`
      - `卧室`
      - `走廊`
      - `客栈`
      - `市集`
      - `山林`
      - `河边`
    - 同时新增少量赛道样板场景：
      - `玄幻::宗门大殿`
      - `言情::闺阁深夜`
      - `悬疑::废弃走廊`
      - `科幻::舰桥`
- 这批预置种子与当前系统能力是匹配的：
  - 通用层可后续迁到赛道层
  - 赛道层也可后续再提升回通用层
  - 所以先以“高复用通用层”为主上线，是合理且低风险的
- 后台也已把这条策略变成可见信息：
  - `admin.html` 的动作图谱维护区新增了“基础种子摘要”
    - 会显示当前动作通用层基础种子规模
    - 并直接提示上线前建议优先补哪类高复用基础动作素材
    - 现在还新增了“建议优先补库”快捷入口，可直接跳到：
      - `走`
      - `快走`
      - `跳`
      - `爬`
      - `飞`
      - `抓`
      - `推`
      - `拉`
      - `摩擦`
      - `蹲下`
    - 这些快捷入口现在还会直接显示覆盖状态：
      - `未建补充单`（含义：待用户命中验证）
      - `待补 N`
      - `已覆盖 N`
    - 这些快捷入口现在还会直接显示“优先补库原因”：
      - 例如：
        - `基础位移动作，复用频率高`
        - `近景互动动作，覆盖面很广`
        - `布料/表面类通用动效，高复用`
    - 这些动作种子快捷入口现在还会按当前覆盖状态自动重排：
      - `待补` 优先
      - `未建补充单` 其次
      - `已覆盖` 靠后
    - 这批优先动作种子现在还会直接显示覆盖摘要：
      - 优先种子里 `待补` 有多少个
      - `待用户命中验证` 有多少个
      - `已覆盖` 有多少个
    - 动作后台当前还新增了一行轻量状态总览卡：
      - `待补`
      - `待验证`
      - `已覆盖`
      让运营先看基础种子的整体补库压力，再决定点哪一个入口
    - 每个优先动作种子条目现在还会直接给出“下一步动作”：
      - 已有补充单：`先去补充单补素材`
      - 未建补充单：`先等待真实用户文本命中验证`
    - `未建补充单` 在动作种子语义里，当前已明确解释为：
      - 不是异常数据
      - 而是“上线前已预置、但还缺真实用户命中验证”的基础种子
    - 已有补充单的优先动作种子，现在可直接点 `去补充`
    - 从动作种子入口点进图谱维护后，当前维护反馈里还会直接显示：
      - `建议下一步：先去补充单补素材`
      - 或 `建议下一步：先等待真实用户文本命中验证`
    - 动作后台种子摘要区当前还会直接提示：
      - `待用户命中验证` 不是异常
      - 而是上线前已预置、等待真实文本触发的基础动作种子
  - `scene.html` 的场景图谱维护区新增了“场景基础种子摘要”
    - 会显示当前通用场景、模板、赛道样板规模
    - 并直接提示上线前建议优先补哪类高复用基础场景素材
    - 现在还新增了“建议优先补库”快捷入口，可直接跳到：
      - `街道`
      - `房间`
      - `卧室`
      - `走廊`
      - `海边`
      - `山林`
      - `雨夜街巷`
    - 这些快捷入口现在也会直接显示覆盖状态：
      - `未建补充单`（含义：待用户命中验证）
      - `待补 N`
      - `已覆盖 N`
    - 这些快捷入口现在还会直接显示：
      - `支撑音效项数`
      - `细节音效项数`
    - 这批优先场景种子现在还会直接显示覆盖摘要：
      - 优先种子里 `待补` 有多少个
      - `待用户命中验证` 有多少个
      - `已覆盖` 有多少个
    - 场景后台当前也新增了一行轻量状态总览卡：
      - `待补`
      - `待验证`
      - `已覆盖`
      让运营先看基础场景种子的整体补库压力，再决定点哪一个入口
    - 每个优先场景种子条目现在还会直接给出“下一步动作”：
      - 已有补充单：`先去补充单补素材`
      - 未建补充单：`先等待真实用户文本命中验证`
    - `未建补充单` 在场景种子语义里，当前也已明确解释为：
      - 不是异常数据
      - 而是“上线前已预置、但还缺真实用户命中验证”的基础场景种子
    - 已有补充单的优先场景种子，现在可直接点 `去补充单`
    - 从场景种子入口点进图谱维护后，保存状态旁会直接显示：
      - `建议下一步：先去补充单补支撑场景音效，再回看细节音效`
      - 或 `建议下一步：先等待真实用户文本命中验证`
    - 场景图谱维护里的“相关场景补充单”区块，现在也会直接给出：
      - 当前节点更适合先补素材
      - 还是已可回补充单区通知用户
    - 场景节点摘要与来源解释里也新增了“补库顺序提示”：
      - 先补支撑场景音效
      - 再根据场景是否已经站住，决定是否继续补细节音效
    - 这些场景种子快捷入口现在也会按当前覆盖状态自动重排：
      - `待补` 优先
      - `未建补充单` 其次
      - `已覆盖` 靠后
    - 场景后台种子摘要区当前也会直接提示：
      - `待用户命中验证` 不是异常
      - 而是上线前已预置、等待真实文本触发的基础场景种子
  - 目的：让运营上线前不必自己猜“先补什么”，而是有一个稳定的基础补库方向

## 新增：2026-03-20 场景搭建分析稳定性修复

- `scene-building` 近期出现过真实 500：
  - 根因不是前端，而是后端写库时把 `result['report_json'] = result` 自引用
  - 已修复为可序列化副本，避免 `Circular reference detected`

- `scene-building` 的 `Contract 不通过` 已明确定位：
  - 不是 JSON 解析失败
  - 而是 LLM 已返回 JSON，但顶层字段不稳定，缺过 `title`
  - 主 prompt 与 retry prompt 现已补强：
    - 强制顶层必须包含 `title / scene_items / summary / graph_model`
    - `title` 固定输出为 `场景搭建分析`

- `scene-building` 现已补上长文本分段逻辑，参考动作分析：
  - `SINGLE_PASS_SAFE_CHARS = 420`
  - `SEGMENT_TARGET_CHARS = 360`
  - `SEGMENT_MAX_CHARS = 420`
  - 长文本会先分段，再合并 `scene_items / summary / key_points / risks`

- `supporting_sfx_terms / detail_sfx_terms` 的 prompt 规则也已更新：
  - `supporting_sfx_terms` 用于“让听众立刻知道场景”
  - `detail_sfx_terms` 用于“让场景更丰满”，不能替代支撑音效
  - 若支撑音效已有 `2-3` 个明显标志声，细节音效仍建议补 `2-5` 个
  - 若支撑音效少于 `2` 个、无法明确场景，细节音效可补 `3-8` 个
  - 目标不是节约 token，而是给用户更宽的可选择空间

## 新增：2026-03-20 前端体验收口（用户端主题 / 文档锚点 / scene 补充单交互）

- 用户端 [index.html](/Users/demo/Documents/New%20project/musicForArt/frontend/index.html)
  - 曾临时切换到与 `scene.html` 接近的深蓝色 + 金色渐变主题
  - 现已按用户反馈回退到原来的红色系轻主题

- `scene.html`
  - “场景补充单运营”里的补充单卡片，不再只是静态长卡片
  - 卡片头现在可点击展开 / 收起详情，避免运营点击补充单时“看起来没有任何反应”
  - 卡片头新增了显式提示：`点击收起补充单详情 / 点击展开补充单详情`
  - 另修复了一个导致“统计有数但补充单列表空白”的真实 bug：
    - 原因：补充单渲染中使用了 `escAttr(...)`，但函数未定义
    - 结果：`renderSceneSupplements()` 在统计卡渲染完成后、列表写入前抛异常
    - 表现：顶部能看到补充单数/待补词数，下面没有任何补充单卡片
    - 现已补上 `escAttr()` 并补充 `scene-badge` 样式

- `scene.html` 的“本地文档锚点”
  - 已从写死的 `pre` 文本，改成动态文档清单
  - 每条会显示：
    - 本地绝对路径
    - 文档中文名称
    - 中文解释
  - 当前纳入 scene 相关文档：
    - `SCENE_BUILDING_MODULE_STRATEGY.md`
    - `SCENE_BUILDING_OUTPUT_SCHEMA.md`
    - `SCENE_BUILDING_PROMPT_V1.md`
    - `SCENE_MODULE_DECOUPLING_PLAN.md`
    - `OPS_ACTION_GRAPH_AND_SUPPLEMENTS_GUIDE.md`
    - `OPS_ACTION_GRAPH_AND_SUPPLEMENTS_QA.md`

- `admin.html`
  - 已复用“本地文档锚点”功能
  - 新增动作分析相关文档清单，同样展示：
    - 本地绝对路径
    - 文档中文名称
    - 中文解释

## 新增：2026-03-21 前端安全加固第一阶段

- 本轮已按“测试期可暴露调试、正式期收紧暴露面”的原则落地第一阶段安全加固：
  - 用户端默认不再长期裸露内部调试字段
  - 调试 JSON 改为受系统开关控制
  - 当前策略是：
    - 测试期可开启 `frontend_debug_expose_enabled`
    - 正式上线后应关闭该开关
    - 即使关闭，当前前端在结果异常时仍可临时显示一段折叠调试 JSON，便于复测定位 bug

- 后端已新增前端安全配置项：
  - `frontend_debug_expose_default`
  - `require_signed_downloads`
  - `download_token_ttl_sec`
  - `download_signing_key`

- 后端已新增前端安全设置接口：
  - `GET /api/admin/frontend-security`
  - `POST /api/admin/frontend-security`
  - 当前 `admin.html` 已补上“前端安全设置”卡片
    - 可直接查看：
      - 调试 JSON 是否开启
      - 当前环境是否要求签名下载
      - 下载 token 时效
    - 并可手动保存 `frontend_debug_expose_enabled`

- 用户端返回已开始最小化收口：
  - 用户端接口统一经过 `_user_json_response(...)`
  - 会递归清理不该继续暴露给普通用户的数据，例如：
    - 文件真实路径
    - 内部 `_source_path`
    - `asset_file_path`
    - `llm_trace`
    - `debug`
    - `raw_response`
    - `prompt_file`
    - `effective_prompt_file`
  - 目标是：
    - 普通用户端只拿到“展示必须字段”
    - 后台与排障链路再保留完整信息

- 下载安全已按“本地兼容 / 正式签名”分层实现：
  - 本地开发环境仍兼容现有下载方式，便于联调
  - 正式环境可通过 `require_signed_downloads=1` 切到签名下载
  - 当前签名下载机制：
    - 前端不必继续拿真实文件路径直接复用
    - 后端可下发短时效 `token`
    - `/api/sfx/file` 已支持通过 `token` 校验后下载
    - token 会绑定：
      - 目标文件
      - 过期时间
      - 当前登录手机号（若要求）

- 身份识别已开始切到“token/session 优先”：
  - 三个前端页面：
    - `index.html`
    - `admin.html`
    - `scene.html`
    当前都已去掉主动透传 `X-User-Phone`
  - 现在优先依赖：
    - `Authorization: Bearer <token>`
    - 服务端 session/token 解析出的当前用户
  - 这一步的意义是：
    - 降低前端伪造手机号请求的风险
    - 后续做额度、下载、审计绑定时更稳

- 运营端识别信息已补强：
  - `admin.html` 的用户列表不再只给单独手机号列
  - 现在显式展示：
    - `手机号`
    - `UID`
    - 前台等级
    - 是否授权不限额
  - “运营行为审计”的时间线卡片现在也会显示：
    - `UID`
    - `手机号`
  - 目的：方便运营在人工排查和白名单放开时做双重识别，不只靠手机号肉眼判断

- 关于系统唯一标识的当前口径：
  - 内部稳定主键：`user_account.id`
  - 外显识别：`uid`
  - 登录方式：`phone`
  - 原因：
    - 手机号适合登录，但不适合做永久业务主键
    - `id` 更稳定
    - `uid` 更适合后台与运营沟通时识别用户

## 新增：2026-03-21 用户端首页热榜压缩版

- 用户端首页热榜区域本轮已按“单屏尽量看全”重新收紧：
  - 动作榜与场景榜全部从 `Top10` 改为 `Top5`
  - 两大区块继续并排展示
  - 每个区块内 6 个榜单按双列排布，目标是在常见桌面视口下尽量接近三行看全

- 排榜内容已进一步做减法：
  - 去掉榜单行内的附说明，例如动作来源词、场景来源词
  - 去掉区块顶部的“过去10天 · N 次下载”
  - 去掉“按真实下载统计展示……”这类解释性文案
  - 现在榜单行只保留：
    - 排名
    - 音效名
    - 下载次数

- 排榜样式已继续压缩：
  - 时间切换按钮：
    - 更小字号
    - 更小内边距
    - 更低高度
  - 榜单卡片：
    - 更小标题区
    - 更小 badge
    - 更小列表项高度
    - 更窄数值列
  - 榜单边界统一：
    - 有数据的每一行也统一改成虚线边框
    - 无数据空态同样保持虚线边框，视觉节奏一致

- 用户端页头也同步压缩了一轮：
  - `有声书声音设计台` 标题高度缩小
  - 原来独立一整排的能力芯片已取消
  - 改成嵌入式功能提示：
    - `文本`
    - `动作音效`
    - `场景搭建`
    - `音乐分析`
    - `后期执行单`
  - 这些嵌入式芯片已做过基线和字号对齐修正，避免比正文明显偏小

## 新增：2026-03-21 动作图谱“保底生成监控卡”

- 动作后台新增了一张专门的“保底生成监控卡”，不是只做统计，而是把“发现问题”和“当场处置”合并到一起：
  - 后端接口：
    - `GET /api/action-graph/fallback-monitor?days=30`
    - `POST /api/action-graph/fallback-monitor/resolve`
    - `GET /api/action-graph/fallback-monitor/alerts?days=7&limit=4`
  - 前端入口：
    - `frontend/admin.html`
    - 卡片名称：`保底生成监控卡`

- 这个功能的业务目的已经明确：
  - 解决 LLM 把动作词拆得过碎的问题
  - 例如前台反复出现：
    - `惊叹`
    - `惊`
    - `叹`
  - 运营可以在后台把：
    - `惊叹` 设为正式词
    - `惊`、`叹` 作为 alias / 碎词收敛到 `惊叹`
  - 这样下一次前台再出现相似文本时：
    - 系统会优先命中正式词 `惊叹`
    - `惊`、`叹` 不再单独推荐给用户

- 当前实现口径：
  - “候选层”里高频保底词来自最近动作图谱推荐日志：
    - `UserOperationLog.action == action_sfx_graph`
  - 后端在动作图谱推荐接口里，已经把每次命中的 fallback cluster 写入日志：
    - `fallback_terms`
    - `suggested_formal_term`
    - `parent_head`
    - `raw_verbs`
    - `sentence_excerpt`
  - 监控卡就是按这些日志聚合出来的

- 正式收敛规则的真实落点：
  - 不是 SQL 表
  - 仍然写回动作图谱文件：
    - `backend/assets/sfx/action_graph.json`
  - 规则存储在：
    - `_meta.fallback_alias_map`
  - 结构分为：
    - `common`
    - `genres`

- 当前“保存为正式收敛规则”按钮做的事：
  1. 运营填写正式词，例如 `惊叹`
  2. 运营填写需要被收敛掉的碎词，例如：
     - `惊`
     - `叹`
  3. 运营决定这个正式词进入：
     - `通用层`
     - 或 `赛道层`
  4. 系统会：
     - 确保正式词节点存在
     - 把 alias 规则写入 `_meta.fallback_alias_map`
     - 自动同步 Neo4j

- 当前推荐层也已经接入这套规则：
  - `action_sfx_graph.py` 新增了 alias resolve / suppress 逻辑
  - 下一次动作图谱推荐时：
    - 原始动词会先走 `resolve_action_fallback_head()`
    - 语义词 / 音效词会再走 `_suppress_alias_fragments()`
  - 结果是：
    - `惊 / 叹` 这类已被收敛的碎词，会被自动压掉
    - 前台最终只保留正式词 `惊叹`

- 已做过的本地功能验证：
  - 用临时 action graph 直接验证：
    - `惊 -> 惊叹`
    - `叹 -> 惊叹`
    - `_suppress_alias_fragments('玄幻', '惊叹', ['惊叹', '惊', '叹'])`
      最终只保留：
      - `惊叹`

- 这张监控卡和“图谱维护”的分工：
  - 监控卡：
    - 发现高频保底词
    - 做 alias 收敛
    - 决定正式词进入通用层/赛道层
  - 图谱维护：
    - 查看正式节点
    - 补 semantic_terms / sfx_terms
    - 继续补素材与补边

- 本轮又补了一层“后台即时提醒/红点/待办推送”：
  - 不是浏览器原生通知
  - 而是后台页登录后自动拉取提醒接口，并每 60 秒轮询一次
  - 展示位置：
    - `admin.html` 登录卡下面
    - 一个独立的待办提醒横幅
  - 提醒内容：
    - 当前未处理保底词组数量
    - 最近几组待处理词集
    - 最近命中时间
  - 处理入口：
    - 可直接点“去处理保底生成”滚动到监控卡

- 这意味着当前已经具备两层入口：
  1. 自动沉淀：
     - 用户前台点击“动作图谱推荐”时，如果出现保底词，系统会自动写入 `UserOperationLog`
  2. 后台感知：
     - 后台登录后会自动看到待办提醒横幅
     - 也可以主动进入“保底生成监控卡”按最近 N 天扫描处理

## 新增：2026-03-22 动作后台动态优先级提醒区

- `动作图谱维护` 区域原先那组固定 10 个“基础种子词”已经不再作为固定展示逻辑保留。
- 现已改成 `动态优先级提醒区`，只给运营展示“现在最值得先处理的是什么”，不再重复下面的节点维护目录。
- 当前展示 3 类待办摘要：
  1. `最近高频保底生成词`
     - 数据来源：`保底生成监控卡` 当前未处理项
     - 入口动作：`去处理保底生成`
  2. `最近真实命中但还没建补充单的动作词`
     - 数据来源：最近真实 fallback 命中里，当前还没有进入补充单链路的动作节点
     - 入口动作：`去图谱维护`
  3. `已建补充单但长期未补齐的动作词`
     - 数据来源：动作补充单列表里，仍存在待补项且挂起时间较久的补充组
     - 入口动作：`去补充`
- 设计目的已经明确：
  - 上面这块只回答 `先处理什么`
  - 下面维护节点继续回答 `去哪里处理`
  - 两者不再语义重叠

## 新增：2026-03-22 同样文本不重复扣字符修复

- 用户复测发现：
  - 同一手机号
  - 同一段文本
  - 第一次执行 `动作提取`
  - 第二次再用完全相同文本执行 `动作提取`
  - “今日用量”中的文本字符数仍然继续增长
- 根因已确认：
  - `text_fingerprint` 判重逻辑本身存在
  - 但“当天”判重窗口此前按数据库里的 UTC `created_at` 去卡
  - 用户实际按北京时间复测时，会出现“同一天内重复提交，但日志还落在 UTC 前一天”的情况
  - 导致当天判重漏命中
- 修复方案已落地：
  - 后端新增本地时区辅助：
    - `LOCAL_TZ = ZoneInfo('Asia/Shanghai')`
    - `_local_today_ymd()`
    - `_local_day_start_utc_naive()`
  - 文本指纹判重改成按北京时间当天计算
  - 每日用量快照和日用量写入口径也统一成北京时间
- 已做本地最小复现验证：
  - 修复前：`638 -> 1276`
  - 修复后：`638 -> 638`

## 新增：2026-03-22 文本额度超额缓冲

- 当前文本类功能对普通计数用户已新增一层“单次超额缓冲”：
  - 当天还未超限时
  - 单次新提交文本允许把总用量最多推进到 `日限额 + 1000`
  - 但这个缓冲只能在“当天尚未超限”时触发
- 业务口径：
  - 例如限额 `5000`
  - 当前已用 `4980`
  - 再上传 `500` 字文本
  - 这次允许正常执行
  - 之后当天再次继续上传文本，则会被限制
- 用户端 `今日用量` 面板已同步增加提示：
  - 正常状态：提示超额后联系管理员
  - 达到上限后：会额外说明“文本分析支持单次最多超额 1000 字缓冲”

## 新增：2026-03-22 已确认的动作图谱双层命中产品口径（尚未开发）

- 当前已在产品讨论中明确确认以下口径，但这部分**还没有落代码**，后续开发必须严格按这个方向推进，不能再重新猜业务逻辑：
  - 当用户端在 `动作提取 -> 动作图谱推荐` 中，某个动作词同时命中 `通用层=是`、`赛道层=是` 时，这不应该只是一个“来源说明”。
  - 用户视角更合理的语义应当是：
    - 系统实际具备两个版本的推荐能力
    - 例如 `围住` 同时命中通用层和玄幻赛道层时，前台最终应展示为：
      - `围住（通用）`
      - `围住（玄幻）`
- 与此对应的后台补库口径也已经确认：
  - 如果系统当前两层都命中，但两层素材都还没补齐
  - 则动作补充单未来应拆成双版本任务，而不是只给一条模糊的待补项
  - 也就是运营后台应能看到两条明确入口：
    - `围住（通用）`
    - `围住（玄幻）`
  - 运营上传时不再靠人工猜“这次到底该传通用还是赛道”，而是系统直接给出对应版本入口
- 这项调整的业务目的已经确认：
  - 让“图谱层命中两层”的语义，和“素材补充层 / 用户下载层”的语义彻底一致
  - 避免当前这种：
    - 用户端看到 `通用+赛道`
    - 但后台补充单只给一条待补项
    - 运营只能靠经验判断上传到哪一层
- 当前阶段的判断结论：
  - 这是一个合理且必要的中型改造
  - 不是小修，但也不是推翻重做
  - 后续如果进入开发，应至少联动 4 块：
    1. 用户端动作图谱推荐展示
    2. 动作补充单生成逻辑
    3. 运营上传入口与素材版本归属
    4. 用户端下载展示与下载计数口径
- 当前状态必须记住：
  - **方向已确认**
  - **代码尚未实施**
  - 下个线程继续开发时，必须把它当成“待实现的已确认方案”，不能误说“系统已经支持双版本展示和双版本补充单”。

## 新增：2026-03-22 用户端音效下载收口

- 用户端已取消 `下载 SFX ZIP` 批量下载入口。
- 前端：
  - `frontend/index.html` 中已移除 `13) 下载 SFX ZIP`
- 后端：
  - `GET /api/analysis/<project_id>/export`
  - 当前只接受 `type=cue_csv`
  - `type=sfx_zip` 已被拒绝
- 当前业务口径恢复为：
  - 用户只允许按需逐个下载音效
  - 音效下载量继续按单日维度累计
  - 不再通过 ZIP 批量下载绕开单个下载计数逻辑

## 新增：2026-03-22 用户端动作补充单明细收紧

- 用户端 `动作补充单明细` 已不再展示运营维护字段：
  - `建议头词`
  - `写入位置`
  - `置信度`
  - `推荐音效词`
- 当前只保留：
  - `动作词`
  - `关联语义`
  - `待补充音效词`
- 表格下方已新增用户可读提醒：
  - `补充单已经提交系统，待系统完成补充后，会给您手机号发送补足提醒，敬请期待。`

## 新增：2026-03-22 动作图谱后台改造成“通用元数据 + 赛道分配 + 赛道特化”

- 运营对旧版“通用层继承 / 赛道层维护”心智负担过重，已确认改造成新的业务模型：
  - `通用元数据`：系统底座，不再强调“被继承”
  - `赛道分配 / 赛道引用`：决定某个赛道是否使用这条通用元数据
  - `赛道特化`：只维护当前赛道额外强化的内容
- 后台页面 [frontend/admin.html](/Users/demo/Documents/New%20project/musicForArt/frontend/admin.html) 已做第一轮重构：
  - 术语已切换：
    - `通用层` -> `通用元数据`
    - `赛道层` -> `赛道特化`
    - `继承` -> `赛道引用 / 赛道分配`
  - `动作图谱维护` 已重排为更接近运营任务流的结构
  - `赛道引用控制` 按钮已改名：
    - `恢复当前赛道引用` -> `赋予当前赛道引用`
  - `赛道引用控制` 的取消 / 赋予操作，已在页面区域内有明确成功/失败反馈
- 关键底层模型也已改造：
  - 旧逻辑：`通用层默认全赛道可见，再靠屏蔽做减法`
  - 新逻辑：`通用元数据只有被显式分配给某个赛道后，该赛道才会看到`
  - 图谱 JSON 中已开始使用：
    - `_meta.genre_assignments`
    - `_meta.inheritance_blocks`
- 当前复测口径必须记住：
  - 赛道是否能看到某个通用词，不再默认等于“有通用就全赛道可见”
  - 必须经过显式赛道分配

## 新增：2026-03-22 图谱数据重置口径

- 为了配合这轮“通用元数据 -> 赛道分配 -> 赛道特化”复测，已多次按统一口径重置本地数据：
  - 保留：
    - `user_account`
    - `auth_session`
    - `invite_code`
  - 清空：
    - `projects`
    - `action_supplement_task`
    - `action_supplement_asset`
    - `user_operation_log`
    - `reasoning_log`
    - `daily_usage`
    - 以及本轮相关业务临时表
- 当前图谱复测基线口径为：
  - `common = 55`
  - `玄幻 = 0`
  - `言情 = 0`
  - `悬疑 = 0`
  - `科幻 = 0`
  - `genre_assignments = {}`
- 这意味着：
  - 通用元数据底座保留
  - 四个赛道都从空白分配状态开始
  - 适合重新验证“赛道分配 / 赛道特化 / 保底生成”之间的逻辑

## 新增：2026-03-22 新增赛道动作词入口

- 旧版后台只适合“打开已有节点再维护”，不适合“赛道从 0 开始时直接建词”。
- 已在 [frontend/admin.html](/Users/demo/Documents/New%20project/musicForArt/frontend/admin.html) 中新增：
  - `新增赛道动作词`
- 当前正确操作路径是：
  1. 先选赛道
  2. 输入新的赛道动作词，例如 `结印`
  3. 点 `新建赛道动作词`
  4. 系统直接打开 `玄幻::结印`
  5. 再在下方 `赛道特化维护区` 里补语义词、直达音效、整体音效并保存
- 重要口径：
  - `新增自定义语义词` 只是给**当前已打开节点**补语义子级
  - 它不是新建赛道动作词父节点的入口

## 新增：2026-03-22 节点删除入口已从运营页面收口

- 运营明确确认：
  - 当前阶段后台不应该给运营“删除通用层 / 删除赛道层”的入口
  - 业务上只有“新增赛道”需求，没有常规“删除节点”需求
- 因此：
  - `节点级清理` 已收口成只读提示区
  - 不再向运营展示：
    - 删除通用元数据节点
    - 删除赛道特化节点
    - 删除当前层节点
- 也就是说，当前后台面向运营的口径是：
  - 有新增赛道动作词入口
  - 无删除节点入口

## 新增：2026-03-22 人工收敛设置改成“标准展示词 + 勾选碎词 + 可手动新增”

- 运营已明确确认：
  - 过去那种“给出一堆碎词 -> 让运营自己删文本框”的方式不够好
  - 也不能强迫运营“至少选一个碎词”
  - 正确的交互应该是：
    - 明确看到 `标准展示词`
    - 只勾选那些确认要被收敛到标准展示词的碎词
    - 高风险单字默认不勾选
- 当前 [frontend/admin.html](/Users/demo/Documents/New%20project/musicForArt/frontend/admin.html) 已改成：
  - `标准展示词` 单独显示
  - 候选碎词改成复选框，而不是文本框
  - `建议归并` 默认勾选
  - `高风险单字 / 泛词谨慎` 默认不勾选
  - 提供：
    - `全部勾选`
    - `全部取消`
  - 新增：
    - `手动增加碎词`
      - 用于处理前台没拆出完整碎词、但运营肉眼能判断出应该新增如 `惊恐道` 这类词的场景
- 当前已确认的正确业务口径：
  - 例如：
    - 标准展示词：`惊恐`
    - 手动新增碎词：`惊恐道`
    - 勾选 `惊恐道`
    - 保存
  - 最终得到的应该是：
    - `惊恐道 -> 惊恐`

## 新增：2026-03-22 收敛规则保存后端口径已修复

- 这是本轮非常重要的 bug 修复，必须记住：
  - 旧逻辑会把整组 `fallback_terms` 自动补回去
  - 即使运营没有勾选某些碎词，后端仍然可能偷偷写入规则
  - 会导致错误规则，比如：
    - `下 -> 劈下`
    - `劈 -> 劈下`
- 现已修复：
  - `POST /api/action-graph/fallback-monitor/resolve`
  - 后端只认运营实际勾选 / 手动新增后提交的 `alias_terms`
  - 不再自动把整组 `fallback_terms` 补回去
- 因此当前正确口径是：
  - 如果运营一个碎词都不勾选，也允许只把 `劈下` 作为标准展示词存在
  - 不会因为“一个都没勾”就被后端偷偷生成错误 alias 规则

## 新增：2026-03-22 标准展示词与高风险碎词词表已做联动

- 为减少高风险单字误收敛，两个功能已开始联动：
  - 收敛保存前，如果命中了高风险碎词，会先弹确认
  - `现有标准展示词收敛规则` 中支持：
    - `释放`
    - `释放并加入高风险词表`
  - 高风险碎词词表中支持：
    - 显示 `关联规则 N`
    - `查看关联规则`
- 目的：
  - 让高风险词表成为前置风控层
  - 让标准展示词收敛规则成为正式落规则层
  - 形成“释放错误规则 -> 顺手加入高风险词表”的纠错闭环

## 新增：2026-03-22 当前运营判断原则（非常重要）

- 这轮复测里，必须严格按以下原则理解标准展示词收敛规则：
  - `A -> B`
  - 表示：如果以后再次命中 `A`，系统会尽量按 `B` 来展示和维护
  - 不表示：`A` 和 `B` 会同时展示
- 因此：
  - 单字、泛词、短词规则风险很高
  - 例如：
    - `散 -> 散落`
    - `阴 -> 阴笑`
  - 这类规则容易误伤别的语义，例如 `散开`
- 当前运营建议已经确认：
  - 大量单字规则应优先考虑释放
  - 再用更完整的短语重建规则
  - 例如：
    - `惊恐道 -> 惊恐`
  - 这类比：
    - `惊 -> 惊恐`
    - `道 -> 惊恐`
    更合理、更稳定

## 新增：2026-03-23 用户端输入区与复测数据重置

- 用户端 [frontend/index.html](/Users/demo/Documents/New%20project/musicForArt/frontend/index.html) 已做三项收口：
  - `项目标题` 去掉默认示例值，改为空白输入，并在前端强制设为必填
  - `API 地址` 已从用户端页面隐藏，但底层 `apiBase()` 逻辑和本地默认地址仍保留
  - `邀请码 / UID / 推荐码` 改成小标签 + 短占位文案，避免标签或占位文字压住输入框
- 2026-03-23 这一轮复测前已清空业务脏数据，保留用户和登录相关数据：
  - 保留：`user_account`、`auth_session`、`invite_code`、`system_setting`、`action_fallback_risk_term`、`leaderboard_override`
  - 清空：`projects`、`audio_analysis`、`text_analysis`、`narration_analysis`、`scene_analysis`、`fusion_plan`、`action_supplement_task`、`action_supplement_asset`、`scene_supplement_task`、`scene_supplement_asset`、`user_operation_log`、`reasoning_log`、`daily_usage`、`auth_code`、`action_verb_analysis`、`reasoning_cache`、`draft_review`
- 本轮数据库备份：
  - `/Users/demo/Documents/New project/musicForArt/backend/music_for_art.db.backup_2026-03-23_110832_retest_reset5`

## 新增：2026-03-26 文本演绎分析“核心时间证据”Prompt 升级

- 用户提出：希望在“文本演绎分析”里，围绕“标点级语句时间线（核心时间证据）”通过 Prompt 升级，给出更优质、更可执行的建议。
- 评估结论：
  - 可行；
  - 但这块不是纯 `text_narration` 独立 Prompt 决定，实际上上游核心仍是 [V3-text_analysis_task.txt](/Users/demo/Documents/New%20project/musicForArt/backend/app/prompts/V3-text_analysis_task.txt) 里的 `clause_timeline` 设计，再叠加 [narration.py](/Users/demo/Documents/New%20project/musicForArt/backend/app/services/narration.py) 的规则映射。
  - 因此本轮优化优先升级文本分析 Prompt，不改现有 JSON 字段结构。
- 已先做备份：
  - `/Users/demo/Documents/New project/musicForArt/backend/app/prompts/V3-text_analysis_task.txt.backup_2026-03-26_time-evidence`
  - `/Users/demo/Documents/New project/musicForArt/backend/app/prompts/V3-text_analysis_task_retry.txt.backup_2026-03-26_time-evidence`
- 已完成的 Prompt 增强方向：
  - 明确 `clause_timeline` 是“核心时间证据”，不是普通分句列表；
  - 优先保留：
    - 动作真正发生句
    - 情绪转折句
    - 场景/节奏切换句
    - 音乐进入退出触发句
    - 有明确音效提示价值的句子
  - 明确要求 `clause_timeline.text` 尽量保留原文触发词，不要改写成总结句；
  - 明确要求 `start_sec/end_sec` 要体现朗读节奏差异，禁止机械平均切分；
  - 明确要求压缩时优先保留“节奏拐点”句，而不是为了凑数量塞满。
- 本轮尚未改动：
  - `narration.py` 的时长映射规则；
  - 前端“核心时间证据”展示结构。

## 新增：2026-03-26 后台“保底生成与收敛”第一轮重构

- 已按最新确认的运营口径开始重构：
  - 去掉“保底生成监控”的时间下拉框；
  - 点击加载后，按最新命中时间优先排序；
  - 页面目标从“碎词技术维护”改成“系统自动治理后的标准展示词映射确认”。
- 已做的后台交互调整：
  - `高风险碎词词表维护` 已从运营主视图移除，不再作为常用区块出现；
  - 当前页面聚焦：
    - 这组保底词
    - 前台原始动词
    - 最终进入保底词集
    - 标准展示词映射确认
    - 手动增加碎词
    - 保存为标准展示词
- 已做的后端第一层自动治理：
  - 系统会先自动给出更合理的 `suggested_formal_term`；
  - 会自动筛掉明显高风险单字 / 泛词，不再默认丢给运营逐个维护；
  - 类似 `惊恐道 -> 惊恐` 这类尾词型场景，系统建议会比以前更贴业务理解。
- 当前口径非常重要：
  - 这轮不是“让 LLM 全自动写死规则”；
  - 而是“系统先自动整理明显碎词，运营只确认仍有歧义的标准展示词映射”；
  - 高风险碎词机制仍保留在系统内部，但不再作为运营的主维护功能。

## 新增：2026-03-26 元数据与赛道分配第二轮重构

- `结构维护：元数据与赛道分配` 已继续按运营动作流重构，不再强调“继承/屏蔽”技术心智。
- 当前页面新增并明确了这些直接操作：
  - `新增通用动词`
  - `新增赛道动作词`
  - `删除通用动词`
  - `删除赛道动词`
- 赛道里的通用词管理，已改成运营更容易理解的语义：
  - `从当前赛道删除`
  - `恢复到当前赛道`
- 原来的：
  - `赛道取消引用池`
  - `屏蔽后命中复核`
  已改成更业务的话术：
  - `赛道删除池`
  - `删除后命中复核`
- 当前业务口径：
  - 赛道里的“删除通用词”不是物理真删，而是进入可恢复的删除池；
  - 如果后续前台再次高频命中，会在“删除后命中复核”里提醒恢复；
  - 真正建错的通用节点 / 赛道特化节点，则可以直接通过“删除通用动词 / 删除赛道动词”处理。

## 新增：2026-03-26 用户端“文本音乐执行单”命名与展示口径调整

- 用户端原 `10) 生成后期执行单` 已改成：
  - `10) 生成文本音乐执行单`
- 调整原因：
  - 原命名容易让用户误以为该功能会自动包含动作分析和场景搭建结果；
  - 实际该功能只依赖：
    - 文本分析
    - 音乐分析
    - 演绎时间轴 / 音频与文本对应关系
  - 不包含动作模块与场景模块结果。
- 用户端页面中已同步调整：
  - 首页能力芯片与流程说明；
  - 演绎音频上传说明；
  - 运行时动作名；
  - 登录后“建议下一步”等口径。
- `执行单证据来源` 中原英文链路：
  - `music+text+narration`
  已在前端映射为中文：
  - `音乐分析 + 文本分析 + 演绎时间轴`
- `补充说明` 默认不再直接向用户展示后端原始 `report_markdown`：
  - 避免继续出现 `音乐-文本融合执行单（规则版）`、`Cue Sheet` 这类内部或偏技术口径；
  - 默认改为面向实际后期工作的 `执行建议` 面板；
  - 原始 markdown 仅在后台开启前端调试开关后，作为 `内部调试说明` 展示。

## 新增：2026-03-26 音乐匹配与推荐方案 V1

- 已根据最新业务讨论新增方案文档：
  - `/Users/demo/Documents/New project/musicForArt/docs/MUSIC_MATCHING_AND_RECOMMENDATION_PLAN_V1.md`
- 当前结论：
  - 现有“音乐分析”应逐步升级为“音乐匹配与推荐”；
  - 用户真实诉求分为两类：
    - 我手里的音乐是否适合当前作品；
    - 如果没有合适音乐，系统推荐 1~3 首更适合的音乐。
- 方案建议的分期：
  - 一期先做：
    - `我的音乐是否合适`
    - 输出赛道匹配、文本匹配、节奏匹配、适配原因、剪辑建议；
  - 二期再做：
    - `帮我推荐更合适的音乐`
    - 基于音乐候选库给出 1~3 首推荐及理由。
- 架构建议：
  - 不建议只用 Neo4j 存全部音乐分析结果；
  - 更合适的是：
    - 关系库 / JSON 存结构化分析结果；
    - Neo4j 存赛道、情绪、风格、叙事功能之间的关系；
    - 后续如需要，再加向量或特征检索层做召回。
- 当前这份文档是“方案版”，尚未进入代码开发阶段。

## 新增：2026-03-26 音乐匹配一期开发规格

- 已新增一期可开发规格文档：
  - `/Users/demo/Documents/New project/musicForArt/docs/MUSIC_MATCHING_PHASE1_SPEC.md`
- 当前建议的一期目标：
  - 先做 `我的音乐是否适合作品`
  - 不急着先做“推荐 1~3 首音乐”
- 一期核心能力：
  - 赛道匹配
  - 文本匹配
  - 演绎节奏匹配
  - 综合结论
  - 剪辑建议
  - 不适合原因与更换建议
- 一期后端建议：
  - 新增服务：`backend/app/services/music_match.py`
  - 新增 Prompt：`V1-music_match_task.txt`
  - 新增接口：`POST /api/analysis/<project_id>/music-match`
  - 新增结果表：`music_match_result`
- 当前判断：
  - 一期不强依赖 Neo4j；
  - Neo4j 更适合在二期音乐推荐阶段，承接赛道/情绪/风格/叙事功能关系。

## 新增：2026-03-26 音乐匹配一期已落代码

- 已新增结果表：
  - `music_match_result`
  - 位置：`backend/app/models.py`
- 已新增服务：
  - `backend/app/services/music_match.py`
  - 当前实现口径：
    - 先走稳定的规则评分与用户可读建议；
    - 暂不依赖 Neo4j；
    - 暂不依赖音乐推荐库；
    - 重点先解决“这首音乐是否适合作品”。
- 已新增接口：
  - `POST /api/analysis/<project_id>/music-match`
  - 位置：`backend/app/main.py`
  - 当前依赖：
    - 项目赛道
    - 音乐分析结果
    - 文本分析结果
    - 演绎分析结果（可选）
- 当前接口输出重点：
  - 综合结论
  - 综合评分
  - 赛道匹配判断
  - 文本匹配判断
  - 演绎节奏匹配
  - 剪辑建议
  - 更换建议
- 用户端已新增入口：
  - `2.5) 判断是否适合作品`
  - 位置：`frontend/index.html`
- 用户端已新增结果展示：
  - `音乐是否适合作品`
  - `赛道匹配判断`
  - `文本匹配判断`
  - `演绎节奏匹配`
  - `剪辑建议`
  - `更换建议`
  - `关键结论`
  - `使用提醒`
- 当前阶段说明：
  - 这是一期可复测版本；
  - 已能回答“这首音乐是否适合作品”；
  - 还未进入“推荐 1~3 首音乐”的二期开发。

## 新增：2026-03-26 平台服务能力一期已接入

- 用户端已新增“平台服务区”，位置：`frontend/index.html`
  - `创作者成品展示`
    - 用户可提交成品作品；
    - 当前支持标题、赛道、角色定位、能力简介、技能标签、样片链接、样片文件；
    - 提交后状态为 `pending`，待运营审核；
    - 已审核通过的展示会在用户端公开区展示。
  - `版权书发布广告`
    - 当前第一阶段由运营后台发布；
    - 用户端公开展示广告标题、赛道、预算、保证金、合作说明。
  - `在线充值中心`
    - 当前为“申请单 + 运营审核”模式，不接真实支付网关；
    - 用户可以提交：
      - 文字额度包；
      - 音效下载次数包；
      - 保证金充值；
    - 支持在充值单里填写 `保证金抵扣`；
    - 审核通过后会增加：
      - `text_char_pack_balance`
      - `sfx_download_pack_balance`
      - `deposit_balance`
  - `甲方招聘需求`
    - 当前第一阶段由运营后台代发布；
    - 用户端公开展示招聘标题、赛道、预算、截止时间、需求说明。

- 后端已新增数据表，位置：`backend/app/models.py`
  - `creator_showcase`
  - `copyright_book_ad`
  - `recruitment_need`
  - `recharge_order`
- `user_account` 已新增：
  - `text_char_pack_balance`
  - `sfx_download_pack_balance`
  - `deposit_balance`

- 后端已新增公开接口，位置：`backend/app/main.py`
  - `GET /api/home/creator-showcases`
  - `POST /api/home/creator-showcases`
  - `GET /api/home/copyright-ads`
  - `GET /api/home/recruitment-needs`
  - `GET /api/home/recharge-summary`
  - `POST /api/home/recharge-orders`

- 后端已新增后台接口，位置：`backend/app/main.py`
  - `GET /api/admin/creator-showcases`
  - `POST /api/admin/creator-showcases/review`
  - `GET /api/admin/copyright-ads`
  - `POST /api/admin/copyright-ads`
  - `GET /api/admin/recruitment-needs`
  - `POST /api/admin/recruitment-needs`
  - `GET /api/admin/recharge-orders`
  - `POST /api/admin/recharge-orders/review`

- 后台管理页已新增四块业务区，位置：`frontend/admin.html`
  - `创作者展示审核`
  - `版权书广告发布`
  - `招聘需求发布`
  - `充值订单审核`

- 当前充值口径说明：
  - 仍为第一阶段人工审核版；
  - 不接支付网关；
  - 审核通过后余额立即入账；
  - 日额度不足时，会优先消耗已购买的文字包/下载包余额。

## 新增：2026-03-26 首页收口 + 用户中心独立页

- 用户主首页 `frontend/index.html` 已做减法：
  - `平台服务区` 已从首页移除；
  - `在线充值中心` 不再出现在首页；
  - 热门榜新增后台开关控制，默认可隐藏，不对外展示。

- 已新增后台开关：
  - `home_leaderboards_enabled`
  - 已接入：
    - `GET /api/admin/frontend-security`
    - `POST /api/admin/frontend-security`
    - `GET /api/auth/settings`
  - 当前后台“前端安全设置”中可直接控制：
    - 首页隐藏热门榜
    - 首页展示热门榜

- 已新增独立页面：
  - `frontend/user_center.html`
  - 当前作为“用户中心”承载以下平台能力：
    - `作品`
      - 对应 `creator_showcase`
      - 用户可在用户中心提交作品展示
      - 前台展示按四赛道浏览，Top8 分页
    - `书单`
      - 对应 `copyright_book_ad`
      - 用户可在用户中心提交书单
      - 当前提交后进入 `pending`，需后台审核
      - 前台展示按四赛道浏览，Top8 分页
    - `在线充值中心`
      - 与用户中心放在同一页，不在首页对外暴露

- 当前命名调整：
  - `版权书发布广告` 业务语义改为 `书单`
  - `创作者成品展示` 对外语义改为 `作品`
  - `甲方招聘需求` 这条线当前已从主用户展示流中移出，优先保留 `作品 / 书单 / 充值`

- 后台页面 `frontend/admin.html` 对应调整：
  - `创作者展示审核` -> `作品审核`
  - `版权书广告发布` -> `书单管理`
  - `招聘需求发布` 入口已从主业务卡中移出
  - `充值订单审核` 保留

- 书单新增用户提交接口：
  - `POST /api/home/copyright-ads`
  - 需登录
  - 当前提交状态为 `pending`
  - 后台审核接口：
    - `POST /api/admin/copyright-ads/review`

## 新增：2026-03-26 保底收敛逻辑重构为“问题词替代设置”

- 后台 `frontend/admin.html` 中，原先偏“标准展示词收敛”的逻辑已继续重构为：
  - `问题词替代设置`
  - 业务含义从：
    - `碎词 -> 标准展示词`
  - 改为：
    - `原始问题词 -> 一个或多个替代展示词`

- 当前运营目标口径：
  - 前台传来的不标准词，例如：
    - `惊恐道`
    - `感受着`
  - 不应继续作为用户端最终展示词；
  - 运营可在后台将其替换为更合理的展示词，例如：
    - `惊恐道 -> 惊恐`
    - `感受着 -> 感受 / 受着`

- 后端已新增新的持久化元数据结构，位置：
  - `backend/app/services/action_graph_manage.py`
  - `backend/app/services/action_sfx_graph.py`
  - `action_graph.json` `_meta.fallback_replacement_map`

- 当前替代规则结构为：
  - `common`: `问题词 -> [替代展示词...]`
  - `genres.<赛道>`: `问题词 -> [替代展示词...]`

- 用户端推荐链路已接入替代规则：
  - `backend/app/services/action_sfx_graph.py`
  - 当前如果某个 fallback 问题词命中替代规则：
    - 用户端动作图谱推荐会优先展示替代词；
    - 不再直接把原始问题词展示给用户。

- 后台接口已切换：
  - `POST /api/action-graph/fallback-monitor/resolve`
    - 现在保存的是：
      - `source_term`
      - `replacement_terms`
      - `ignored_terms`
      - `genre`
  - `POST /api/action-graph/fallback-monitor/release`
    - 现在释放的是：
      - `source_term`
      - `genre`

- 后台规则列表也已换口径：
  - 从“现有标准展示词收敛规则”
  - 改成“现有问题词替代规则”
  - 展示方式为：
    - `问题词 -> 替代展示词1 / 替代展示词2`

- 这一轮的重点是：
  - 不再把前台传入词误当成“标准展示词”；
  - 不再要求运营在保底收敛页里判断通用层/赛道层；
  - 先完成“问题词替代设置”，再去 `元数据与赛道分配` 做正式图谱建设。

- 2026-03-26 后续修正：
  - `问题词替代设置` 当前默认按通用规则保存，不再直接挂赛道；
  - 允许运营把“原始问题词”本身也作为替代展示词勾选保存；
  - 已知复测前会清空：
    - `fallback_alias_map`
    - `fallback_ignored_terms`
    - `fallback_replacement_map`
  - 业务数据清理默认保留：
    - `user_account`
    - `auth_session`
    - `invite_code`

## 新增：2026-03-26 用户上传更合适音效 -> 后台审核 -> 奖励下载次数

- 已新增用户音效投稿闭环，目标：
  - 当用户觉得系统当前推荐音效不够合适时，可以主动上传自己手里的更合适音效；
  - 后台运营审核；
  - 审核通过后，用户永久获得 `+3` 音效下载次数奖励；
  - 审核驳回时，删除投稿文件，不影响用户正常使用系统。

- 后端新增数据表：
  - `backend/app/models.py`
  - `UserSfxSubmission`
  - 核心字段：
    - `user_phone`
    - `project_id`
    - `genre`
    - `verb`
    - `display_term`
    - `sentence_excerpt`
    - `project_text_excerpt`
    - `file_name`
    - `file_path`
    - `status`
    - `review_note`
    - `reward_download_delta`
    - `reward_applied`

- 后端新增接口：
  - 用户侧：
    - `GET /api/home/sfx-submissions`
    - `POST /api/home/sfx-submissions`
  - 管理侧：
    - `GET /api/admin/sfx-submissions`
    - `POST /api/admin/sfx-submissions/review`

- 当前一期实现口径：
  - 用户上传后进入 `pending`
  - 运营在后台试听审核
  - `approved`：
    - `user_account.sfx_download_pack_balance += 3`
    - 奖励只发一次，避免重复审核重复加次数
  - `rejected`：
    - 删除本地投稿文件

- 用户端入口位置：
  - `frontend/index.html`
  - 已在 `动作图谱推荐` 每条动作卡片里新增：
    - `上传更合适的音效`
  - 这条入口不打断当前推荐页，只在卡片内给提交反馈

- 用户中心：
  - `frontend/user_center.html`
  - 新增：
    - `我的音效投稿`
  - 用于查看最近投稿、审核状态、以及累计获得的永久下载奖励

- 后台位置：
  - `frontend/admin.html`
  - `平台业务发布与审核`
  - 新增：
    - `用户音效投稿审核`
  - 支持：
    - 过滤状态
    - 按手机号/动作词/展示词搜索
    - 下载试听
    - 审核通过并奖励 +3
    - 驳回并删除投稿

- 本期特意不做：
  - 自动替换当前正式素材
  - 自动写回图谱资产
  - 自动并入补充单
  - 这些后续如果要做，应单独作为第二阶段

## 新增：2026-03-26 动作图谱后台继续重构与语义升级前的稳定锚点

- `结构维护：保底生成与收敛` 已收窄为：
  - `问题词替代设置`
  - 目标不是把问题词直接写进通用/赛道层
  - 目标是把前台传来的不稳定词，整理成后续更适合展示给用户的替代展示词
  - 当前逻辑是：
    - `原始问题词 -> 一个或多个替代展示词`
    - 例如：
      - `惊恐道 -> 惊恐`
      - `感受着 -> 感受 / 受着`

- `问题词替代设置` 已完成的重要交互调整：
  - 不再让运营在这里判断“提升到通用层 / 提升到赛道层”
  - 不再自动把前台原词当成“标准展示词”
  - 支持手动增加替代词
  - 释放现有替代规则后，会回流到下方 `待运营确认`
  - 已修复：
    - 某些规则第一次释放正常、第二次不回流的问题
    - 页面缓存导致“闪一下又回来”的问题
    - 勾选原始问题词时被错误判空的问题

- `结构维护：元数据与赛道分配` 当前已调整成更明确的运营工作台：
  - 新增与删除动作词
    - `新建通用元数据`
    - `新建赛道动作词`
  - `来自问题词替代的候选展示词`
    - 作为 `问题词替代设置 -> 元数据建设` 的候选来源区
    - 当前支持：
      - `补入通用元数据`
      - `去创建赛道动词`
    - 这块已经做了分页和多列卡片布局，减少页面高度

- `去创建赛道动词` 目前最终口径：
  - 不再走不稳定的“未保存草稿联动”方案
  - 现阶段已改成“像补入通用元数据一样的创建流程”方向
  - 但此功能近期多轮反复修补，稳定性是本轮需要重点继续复测的点
  - 若后续仍不稳，可考虑直接以“创建最小赛道节点 -> 立即打开维护页”的全新逻辑重做，不再沿用旧草稿态

- `赛道删除池 / 删除后命中复核` 已统一语义：
  - 当前页面中保留：
    - `赛道删除池`
    - `删除后命中复核`
  - 已修复的重要逻辑：
    - 从 `赛道特化维护区` 中把某个词移除并保存后，
      该词现在也会进入 `赛道删除池`
    - 后续可以从 `赛道删除池` 恢复到当前赛道
  - 这一步不再只覆盖“从当前赛道删除/恢复”的旧链路

- 已从后台主操作页面移除，但代码与后端能力保留：
  - `赛道分配与特化候选`
  - `重叠词清理`
  - `直接删除动作词`
  - `运营维护顺序`
  - 原因：
    - 复测下来对运营主流程帮助有限
    - 页面长期占位且多为空数据
    - 后续如有重叠需求，可复用现有代码重新启用

- 用户端动作图谱推荐近期稳定口径：
  - 已收敛/已忽略的碎词，不再继续作为用户可见推荐词
  - 用户侧增加了轻量 `系统整理能力` 提示：
    - 表达系统会自动整理不稳定动作表达
    - 但不直接暴露完整后台替代规则表

- 后续下一大功能主题：
  - `语义子级的推荐逻辑`
  - 当前共识方向：
    - `问题词替代设置` 负责把用户端问题词整理成更适合展示和命中的词
    - `语义子级` 应作为系统的扩展理解层，不直接等同于问题词替代
    - 后续需重点梳理：
      - LLM 原始词
      - 问题词替代
      - 通用元数据 / 赛道特化
      - 语义子级扩展
      - 推荐与补库
    - 目标是进一步降低运营手动维护压力，同时提升前台命中质量

## 新增：2026-03-26 语义子级 -> 语义扩展推荐（第一轮界面重构）

- 已先做第一轮前后端界面口径统一，目标：
  - 把原来的“语义子级”升级成更贴业务的“语义扩展推荐 / 语义推荐词”
  - 让用户看到的是推荐结果，不是维护术语
  - 让后台维护看到的是：
    - `核心扩展词`（通用元数据层）
    - `赛道扩展词`（赛道特化维护区）

- 当前已落地的前端/后台改动：
  - 用户端：
    - `语义扩展词` -> `语义推荐词`
    - 图谱视图中的 `语义子级` -> `语义推荐词`
    - `待补子级音效` -> `待补推荐音效`
  - 后台端：
    - 维护节点摘要里的 `语义子级` -> `语义扩展词`
    - `前台合并结果（方案 A）` -> `前台推荐结果预览`
    - `语义扩展词` -> `语义扩展推荐`
    - `通用元数据层` 内的语义维护改名为：
      - `核心扩展词`
    - `赛道特化维护区` 内的语义维护改名为：
      - `赛道扩展词`
    - 相关候选文案已改成：
      - `扩展推荐候选`
      - `音效候选`

- 这轮还没有改底层推荐算法，属于“口径与工作台第一轮重构”：
  - 当前还未实现：
    - `核心扩展词 / 赛道扩展词 / 系统建议扩展词` 三层真正分级算法
    - LLM 自动建议语义扩展词
    - 用户行为反馈反哺语义扩展词权重
  - 当前重点是先把：
    - 用户端展示语言
    - 后台维护语言
    - 元数据与赛道特化的职责
    统一到更清楚的业务口径上

## 新增：2026-03-26 语义扩展推荐（第二轮后端候选落地）

- 后端已开始把 `expand_term()` 产出的高质量扩展词，作为 `系统建议扩展词` 下发：
  - `action_sfx_graph.py`
    - 在动作图谱推荐里追加 `pending_semantic_term_items`
  - `action_graph_manage.py`
    - 在节点维护详情里，把 `expand_term()` 结果并入 `merged.semantic_term_items`

- 当前过滤口径：
  - 去空值
  - 去与原动词完全相同的词
  - 去长度小于 2 的词
  - 去已存在于当前语义扩展里的词

- 当前阶段的目标不是直接自动入库，而是：
  - 先把可用的扩展推荐候选明确下发给前后端
  - 前端不再把这些词显示成 `未标注`
  - 而是统一显示为 `系统建议扩展词`

- 这意味着当前系统已经从“只改界面叫法”，进入到“后端开始真实提供语义扩展候选”的阶段。

## 新增：2026-03-26 语义扩展推荐（第三轮多来源建议与来源标注）

- 后端 `semantic_graph.py` 新增：
  - `suggest_action_semantic_terms(term, genre, limit, provider_override)`
  - 当前已按“先收窄再验证”的策略，先只保留：
    - `LLM建议`
  - `系统词库 / 系统图谱` 这两层暂不继续参与用户端语义扩展推荐，避免状态膨胀和复测困扰。

- 新增 Prompt：
  - `backend/app/prompts/V3-semantic_expand_action_terms_task.txt`
  - 用于给动作词生成 3~6 个更适合声音设计检索的语义扩展建议

- 当前前后端都已增加“来源标注”能力，但已收窄为：
  - 用户端：
    - `语义推荐词`
    - `直达音效来源`
    - `整体音效来源`
    中仅对 `系统建议扩展词` 额外标注：
    - `LLM建议`
  - 后台端：
    - `核心扩展词`
    - `赛道扩展词`
    的候选词按钮也只同步显示：
    - `系统建议扩展词 / LLM建议`

- 本地最小自检结果（使用当前 DeepSeek 配置）：
  - `吸收 / 玄幻`
    - `吞噬能量`
    - `炼化灵气`
    - `汲取精华`
    - `纳入体内`
    - `融合力量`
    - `抽取元气`
  - `腾空而起 / 玄幻`
    - `凌空飞跃`
    - `纵身一跃`
    - `御风飞行`
    - `冲天而上`
    - `踏空而行`
    - `飞身掠起`
  - `结印 / 玄幻`
    - `掐诀念咒`
    - `凝聚法力`
    - `法印成型`
    - `凌空画符`
    - `灵力波动`
    - `施法前摇`

- 当前结论：
  - 语义扩展推荐已先按“LLM来源”跑通
  - 目标是先验证：只靠 LLM 建议，是否已经足够满足用户端的扩展推荐体验
  - 如果这一层不够，再考虑逐步加回其他来源，而不是一开始把状态做复杂

## 新增：2026-03-27 直达音效 / 整体音效接入 LLM 建议

- 用户反馈：
  - 当前 `直达音效 / 整体音效` 推荐能力明显偏弱
  - 语义推荐词已经足够丰富，但音效层仍主要依赖人工维护的 `sfx_terms`
  - 因此决定继续沿用“先只做 LLM 来源”的策略，把音效建议也先做成 `LLM建议`

- 新增 Prompt：
  - `backend/app/prompts/V3-semantic_expand_action_sfx_task.txt`
  - 作用：围绕 `动作词 + 语义推荐词 + 赛道`
    输出两类音效建议：
    - `direct_terms`
    - `composite_terms`

- `backend/app/services/semantic_graph.py`
  新增：
  - `SfxSuggestion`
  - `_cached_llm_action_sfx_suggestions(...)`
  - `suggest_action_sfx_terms(...)`
  当前策略：
  - 只接 `LLM建议`
  - 不混入系统词库 / 系统图谱 / 行为反馈

- `backend/app/services/action_sfx_graph.py`
  已接入：
  - `suggest_action_sfx_terms(...)`
  - 用户端动作图谱推荐里的：
    - `direct_sfx_term_items`
    - `composite_sfx_term_items`
    会开始追加：
    - `source_label = 系统建议音效`
    - `origin_label = LLM建议`
  同时：
  - 新增 `pending_sfx_term_items`
  - 让待确认音效建议在返回体里也有明确位置

- `backend/app/services/action_graph_manage.py`
  已接入：
  - `suggest_action_sfx_terms(...)`
  - 后台节点维护中的：
    - `直达音效`
    - `整体音效`
    候选区会开始出现：
    - `系统建议音效 / LLM建议`

- 前端调整：
  - `frontend/index.html`
    - `renderSourceBadge()` 支持优先显示后端传回的 `source_label`
    - 所以用户端直达/整体音效建议现在可显示为：
      - `系统建议音效 / LLM建议`
  - `frontend/admin.html`
    - 直达音效 / 整体音效候选按钮可显示：
      - `系统建议音效 / LLM建议`

- 当前复测口径：
  - 先验证“只靠 LLM 给直达音效 / 整体音效做建议”是否已经足够
  - 如果这一层足够，则暂不引入其他来源，避免状态膨胀和额外 CRUD

## 新增：2026-03-27 LLM 提示词命名统一为 V3 前缀

- 当前代码中仍在实际调用的非 `V3-` 提示词文件，已统一改名并同步更新引用：
  - `V3-semantic_expand_action_terms_task.txt`
  - `V3-semantic_expand_action_sfx_task.txt`
  - `V3-director_final_task.txt`
  - `V3-director_final_task_retry.txt`
  - `V3-music_teaching_task.txt`
  - `V3-production_task.txt`

- 原则：
  - 只统一“当前代码里仍在实际调用”的提示词文件命名
  - 未被系统实际调用的旧文件，不强行改名，避免无意义改动

## 新增：2026-03-27 动作图谱推荐等待时长优化

- 用户反馈：
  - 用户端执行 `动作图谱推荐` 时等待明显偏长
  - 追溯后发现主要慢点在：
    - 对每个动作词分别调用一次 `语义扩展 LLM`
    - 再分别调用一次 `直达/整体音效建议 LLM`
  - 当动作词数量变多时，请求时长会成倍增加

- 已做优化：
  - 在 `backend/app/services/action_sfx_graph.py`
  - 新增单次请求的 LLM 扩展预算：
    - `MAX_LLM_EXPAND_VERBS_PER_REQUEST = 5`
  - 缩减单次建议返回数量：
    - `SEMANTIC_SUGGESTION_LIMIT = 4`
    - `SFX_SUGGESTION_LIMIT = 4`
  - 只在当前节点本身的已维护语义/音效较少时，才继续请求 LLM 扩展

- 当前目标：
  - 优先保证用户端等待时间明显下降
  - 已有图谱维护较完整的动作词，不再重复浪费 LLM 扩展调用

## 新增：2026-03-27 动作分析主链路 UAT 交付文档

- 新增完整复测文档：
  - `docs/ACTION_MODULE_UAT_CHECKLIST_V1.md`
- 用途：
  - 作为当前动作分析主链路的测试工程文档与交付资产
  - 按“用户视角 / 运营视角 / 操作步骤 / 期待值”完整覆盖：
    - 动作提取
    - 动作图谱推荐
    - 问题词替代
    - 元数据与赛道分配
    - 通用元数据层
    - 赛道特化维护区
    - 动作补充单
    - 用户投稿音效
    - 运营行为审计

## 新增：2026-03-27 音乐匹配加入 LLM 增强解释

- 保留现有 `音乐是否适合作品` 的规则评分，不改基础判定逻辑。
- 新增提示词：
  - `backend/app/prompts/V3-music_match_task.txt`
- 新增后端能力：
  - `backend/app/services/music_match.py` 增加 LLM 增强解释构建
  - `backend/app/services/llm.py` 新增 `music_match` kind 的 prompt chain、结构校验和输出长度约束
  - `POST /api/analysis/<project_id>/music-match` 支持返回 `llm_review`
- 新增前端对比区：
  - 用户端 `音乐是否适合作品` 页面新增 `LLM增强解读（对比查看）`
- 行为审计补充：
  - `music_match` 行为日志开始写入 `llm_trace_digest`，便于在后台查看模型调用与耗时

## 新增：2026-03-27 音乐分析改为真实音频证据驱动

- 用户反馈：
  - `乐段拆解（导演可读）` 中的内容高度雷同
  - DeepSeek 输出在 `主层次`、`配器推测` 上重复明显
  - 与此前智谱效果相比，差异化不足

- 已确认根因：
  - `backend/app/services/audio_analysis.py` 之前传给 LLM 的 `audio_features` 基本是固定值：
    - `estimated_bpm = 120.0`
    - `tags = ['电影感', '史诗感', '战斗推进']`
    - `markers = _fallback_markers(duration_sec)`（按固定比例切分）
  - 这会导致 LLM 无论面对哪首音乐，都拿到近似同一份输入证据，自然输出高度雷同

- 已完成修复：
  - `backend/app/services/audio_analysis.py`
  - 新增基于真实音频文件的证据抽取，依赖本机可用的 `ffprobe / ffmpeg`
  - 当前会实际提取：
    - 音频元数据：`codec_name / channels / sample_rate / bit_rate / duration_sec`
    - 静音断点：`silence_profile`
    - 活跃段分布：`active_segments`
    - 响度信息：`mean_volume_db / max_volume_db / dynamic_range_db`
    - 动态锚点：`markers` 改为依据活跃段和静音断点生成，而不是固定比例
    - 标签：由动态范围、平均响度、停顿占比、时长等规则推导

- 提示词同步增强：
  - `backend/app/prompts/V3-music_analysis_task.txt`
  - `backend/app/prompts/V3-music_analysis_task_retry.txt`

## 新增：2026-03-30 用户投稿音效文件存储规范

- 业务目标：
  - 用户上传“更合适音效”后，审核通过不仅奖励用户，还要反哺后续推荐
  - 但用户投稿素材不能与系统官方素材混存，避免素材池交叉混乱

- 当前正式口径：
  - 用户原始投稿文件与系统采纳后的素材文件，分开存储
  - 审核通过后，不做“硬替换官方素材”，而是纳入单独的“用户更优推荐”素材池
  - 后续推荐排序时，用户更优推荐素材以更高优先级参与
  - 用户端展示时，仅标记来源：
    - `由用户更优推荐`
  - 用户端素材文件名展示时，去掉内部维护后缀：
    - `（整体-玄幻）`
    - `（直达-玄幻）`
    - 其他同类 `（整体/直达-赛道）`
    - 这类信息继续保留在系统内部用于区分直达/整体与赛道归属，不再面向用户展示
  - 不暴露投稿用户手机号或其他隐私信息

- 文件路径规范：
  - 用户原始投稿文件：
    - `backend/uploads/<user_phone>/<yyyy>/<mm>/<dd>/user_sfx_submission/<file>`
  - 审核通过后纳入系统候选池的采纳文件：
    - `backend/assets/sfx_user/<normalized_label>__userbetter_<submission_id>.<ext>`
  - 系统官方素材继续保留在：
    - `backend/assets/sfx`

- 两类文件的职责边界：
  - `backend/uploads/.../user_sfx_submission/`
    - 用于原始投稿留档、运营试听、问题追溯
    - 属于用户提交原始件，不直接作为系统官方素材
  - `backend/assets/sfx_user/`
    - 用于已审核通过、已纳入推荐排序的“用户更优推荐”素材
    - 与官方素材池物理隔离，便于后续排序、清理、迁移、回滚

- 审核通过后的系统行为：
  - 用户永久增加 `+3` 下载次数
  - 投稿音效复制进入 `backend/assets/sfx_user`
  - 记录采纳后的：
    - `adopted_file_name`
    - `adopted_file_path`
    - `adopted_source_label = 由用户更优推荐`
  - 后续动作图谱推荐、素材排序会优先考虑这条素材

- 驳回后的系统行为：
  - 原始投稿文件可删除
  - 如果此前已进入 `assets/sfx_user`，对应采纳文件也一并删除
  - 后台审核记录保留，状态记为：
    - `已驳回`

- 运营侧意义：
  - 可以在文件系统中稳定追踪：
    - 这条稿件的原始投稿位置
    - 这条稿件是否已进入用户更优推荐池
  - 便于：
    - 下载试听
    - 审核复核
    - 问题排查
    - 文件迁移
  - 新要求模型：
    - 必须优先利用 `feature_evidence`
    - 当静音断点/活跃段明显变化时，`sections` 的 `main_layers / instrument_guess` 要体现乐段差异
    - 即使证据不足，也尽量避免整段复用同样的“主层次/配器推测”

- 当前预期：
  - `乐段拆解（导演可读）` 不再因固定输入而整篇套话
  - DeepSeek 输出会更多体现：
    - 不同乐段的结构差异
    - 抽空、回落、再推进的实际位置
    - 主层次和配器推测的变化

- 新增一层稳定性审计：
  - `backend/app/services/llm.py`
    - 已加入音频分析结果的“质量闸门”，即使首轮 JSON 合法，也会检查：
      - `main_layers` 是否过于重复
      - `instrument_guess` 是否缺少差异
      - `entry/exit` 建议是否过弱
    - 若质量不足，会自动进入 `V3-music_analysis_task_retry.txt`
  - `backend/app/main.py`
    - 行为日志聚合已补充：
      - `quality_valid`
      - `quality_reason`
  - `frontend/admin.html`
    - `运营行为审计 -> 查询行为日志`
    - 现在除了 `LLM 摘要 / LLM 明细` 外，还会直接显示：
      - 结构校验是否通过
      - 质量校验是否通过
      - 若触发重试，显示具体原因

## 新增：2026-03-27 晚间复测收口与当前本地运行口径

- 当前本地默认端口：
  - 前端：`5200`
  - 后端：`8010`

- 当前用户端测试登录口径：
  - 已去掉“发送验证码”按钮
  - 测试阶段可使用任意手机号 + 固定验证码 `111111` 登录
  - 后端 `auth/login` 已放开 `111111` 作为测试登录验证码

- 当前后端稳定性关键修复：
  - `backend/app/services/llm.py`
  - 对于音乐分析链路中 `deepseek` 的 DNS/网络不可达错误（`URLError`, 如 `nodename nor servname provided`）
  - 已改成“快速降级到规则版”，不再把首轮 prompt + retry prompt 的多轮失败重试全部跑满
  - 本地单跑验证结果：
    - 同一首 `project_1.mp3`
    - 在 LLM 网络不可达时，约 `0.2s` 即返回 `rules-only`

- 当前服务启动口径（经人工拉起验证）：
  - 后端：
    - `cd "/Users/demo/Documents/New project/musicForArt/backend"`
    - `source .venv/bin/activate`
    - `python -m app.main`
  - 前端：
    - `cd "/Users/demo/Documents/New project/musicForArt/frontend"`
    - `python3 -m http.server 5200 --bind 127.0.0.1`
  - 当前一次人工验证结果：
    - `http://127.0.0.1:8010/health` 返回 `ok: true`
    - `POST /api/auth/login` 使用 `15914141177 + 111111` 返回成功 token

- 用户端“判断是否适合作品”页最新收口：
  - 去掉：
    - `文本分析标题`
    - 重复的 `关键结论`
  - 保留：
    - `评分结果`
  - `增强视角` 已做内容承载美化，但不压缩内容
  - `乐段拆解（导演可读）` 已取消卡片式压缩 UI，恢复为完整文字字段展示，保留 LLM 原始分析力度

- 当前复测建议：
  - 若再次怀疑后端不稳定，优先以前台方式运行后端，不要 `Ctrl+Z`
  - 停止服务使用：
    - `Ctrl+C`
    - 或 `pkill -f "python -m app.main"`

## 新增：2026-03-28 动作补充单与结构维护闭环

- 动作补充单运营已新增：
  - `转去维护节点`
  - 可选目标：
    - `通用元数据`
    - `赛道特化`
  - 该动作的业务含义：
    - 当补充单当前层级判断不对时，先打开对应维护节点处理
    - 不是直接完成补充单，不是直接删除补充单

- 当前闭环逻辑：
  1. 在动作补充单运营里点击 `转去维护节点`
  2. 系统打开对应维护节点：
     - 通用：`维护节点：词`
     - 赛道：`维护节点：赛道::词`
  3. 此时补充单仍保留在动作补充单运营里，不会凭空消失
  4. 只有当运营在维护节点里真正点了：
     - `保存通用元数据`
     - 或 `保存赛道特化`
  5. 系统才会同步重算这条补充单的 `补充版本`
     - 例如：从 `玄幻版` 改成 `通用版`

- 重要修复：
  - 旧实现里，`转去维护节点` 会直接调用 `action-graph/node-layer`
  - 这会导致：
    - 词在未保存前就提前写进图谱
    - 强刷后在 `结构维护：元数据与赛道分配` 里出现脏数据
    - 但补充单版本仍停留在旧层级
  - 现已修成：
    - `转去维护节点` 只打开维护态
    - 不提前落库

- 脏数据处理记录：
  - `召唤法阵` 曾因旧逻辑被提前写入通用元数据
  - 当前已从：
    - `backend/assets/sfx/action_graph.json`
    中清除
  - 当前核验结果：
    - `common_has False`
    - `genre_has False`

- 动作补充单表头语义已收口：
  - `赛道` 改成 `原始命中赛道`
  - 目的：
    - 区分“最初命中上下文”与“当前补充版本”
  - 例如一条单可以同时显示：
    - `原始命中赛道：玄幻`
    - `维护节点：取出`
    - `补充版本：通用版`

- 动作提取增强：
  - `backend/app/services/action_verbs.py`
  - 已补上“已维护动作词回流到动作提取”的逻辑
  - 现在会在 LLM 动作提取后，再扫描：
    - 通用元数据词
    - 当前赛道词
    - 已维护的语义扩展词
  - 只要这些词在文本里出现，就补回动作提取结果
  - 目的：
    - 解决已维护词（如 `挥剑劈砍`）无法稳定再次被动作提取命中的问题

## 新增：2026-03-30 认证口径调整

- 用户认证当前口径已改成：
  - 登录：`手机号 + 密码`
  - 注册：`手机号 + UID（纯数字）+ 密码 + 验证码`
  - 忘记密码：`手机号 + 验证码 + 新密码`
  - 修改 UID：`已登录 + 手机号验证码确认 + 新 UID（纯数字）`

- 本轮明确收口：
  - 登录 **不再强制 UID**
  - 注册 **强制填写 UID**
  - UID **只做纯数字校验**
  - UID **不做唯一性校验**
  - 业务原因：
    - 手机号仍作为安全身份
    - UID 主要用于后续作品展示与甲方联系
    - 避免因用户乱填 UID 造成真实 UID 用户无法注册

- 后端已新增并落地：
  - `user_account.password_hash`
  - `POST /api/auth/register`
  - `POST /api/auth/reset-password`
  - `POST /api/auth/change-uid`
  - `POST /api/auth/login`
    - 支持密码登录
    - 不再要求登录时必须提交 UID

- 前端用户页认证入口已同步调整：
  - 登录区新增密码输入框
  - 恢复验证码发送按钮，供注册 / 忘记密码 / 修改 UID 使用
  - 保留 UID 输入框，但只在注册和修改 UID 时作为必填使用
  - 新增按钮：
    - `注册`
    - `忘记密码`
    - `修改UID`
  - 登录提示已改成：
    - 登录不强制 UID
    - 注册强制 UID（纯数字）
    - 忘记密码走手机号验证码重置
    - 修改 UID 也需手机号验证码确认本人

## 新增：2026-03-30 UID 推荐体系与动态奖励

- 推荐码口径已从“手机号”切换为“UID”
  - 注册页 `推荐码` 现在填写推荐人的 `UID`
  - 推荐关系后端也按 `uid` 解析，不再按手机号解析
  - 自己不能用自己的 UID 做推荐码

- 推荐奖励已做成后端可配置：
  - 配置入口：
    - 后台 `用户与测试期管理 -> 推荐奖励设置`
  - 后端接口：
    - `GET /api/admin/referral-reward-settings`
    - `POST /api/admin/referral-reward-settings`
  - 当前默认值：
    - 每成功推荐 1 位新用户：`+15` 次音效下载次数
    - 每成功推荐 1 位新用户：`+5000` 字文字数量
  - 奖励发放方式：
    - 写入 `sfx_download_pack_balance`
    - 写入 `text_char_pack_balance`
  - 特点：
    - 可累计
    - 不直接改用户基础级别额度
    - 运营可在后台自由调整

- 用户端推荐短链已落地：
  - 登录后会显示：
    - 我的推荐码（UID）
    - 推荐短链
  - 短链格式当前为：
    - `/?ru=<uid>`
  - 页面支持一键复制推荐短链
  - 打开短链后，注册页会自动带入推荐 UID

- 推荐奖励的发放时机：
  - 首次完成注册 / 激活时
  - 若填写了有效推荐 UID，系统会：
    - 记录 `referred_by_user_id`
    - 记录 `referred_by_phone`
    - 记录 `referred_by_uid`
    - 给邀请人发放可配置奖励
  - 同一被邀请用户不会重复给奖励

- 用户端今日用量展示已同步增强：
  - 文本分析字符显示：
    - 基础剩余
    - 奖励文字包
    - 当前总可用
  - 音效下载数量显示：
    - 基础剩余
    - 永久奖励包
    - 当前总可用

- 用户侧认证已重新收口为双入口：
  - `index.html`
    - 继续保留原本流程：
      - 密码登录
      - 注册
      - 忘记密码
      - 修改 UID
  - `user_center.html`
    - 单独使用“验证码即登录，首次即注册”的轻量入口
    - 首次验证码登录时需补填 UID（纯数字）
    - 后续验证码登录直接成功
  - 为支持该差异化流程，后端新增：
    - `POST /api/auth/code-login-or-register`
    - 专供 `user_center.html` 使用

## 新增：2026-03-30 动作分析资产 Excel 导出（第一期）

- 已将老师提供的 Excel 模板落地到项目内：
  - `backend/assets/templates/action_asset_template.xlsx`
- 当前不再依赖下载目录里的临时文件路径，后续模板演进可直接在项目内维护

- `导出与汇总 -> 拉取完整报告`
  - 已改为直接下载 Excel 资产文件
  - 调用后端导出类型：
    - `type=action_asset_xlsx`

- 后端导出入口：
  - `GET /api/analysis/<project_id>/export?type=action_asset_xlsx`
  - 文件位置：
    - `backend/exports/<project>_action_asset_<timestamp>.xlsx`

- 当前第一期只填“动作分析模块已成熟”的内容，不强行填满整张表
  - 列映射口径：
    - `日期`：项目创建日期
    - `名称`：项目标题
    - `类型`：`<赛道>·动作分析资产`
    - `序号`：动作候选顺序
    - `分时`：来自文本演绎时间轴的起止时间
    - `镜头场景`：动作原句片段
    - `音乐运用`：融合节点 + 混音建议 + 音乐标签
    - `背景元素`：第一期留空，待场景模块完成后补
    - `特征元素`：第一期留空，待场景模块完成后补
    - `细节元素`：第一期留空，待场景模块完成后补
    - `动作`：动作提取词本身
    - `情绪心理`：基于句段关键词的情绪归纳
    - `武器/法术`：句段里的法术/武器/能量关键词
    - `特效运用`：围绕当前动作的特效建议
    - `效果器 / 其他运用`：第一期先留空
    - `备注`：`系统自动填充（动作分析一期）`

- 数据来源：
  - `action_verb_analysis.result_json`
  - `narration_analysis.timeline_json`
  - `fusion_plan.cue_sheet_json`
  - `audio_analysis.tags_json`

- 第一期开启原则：
  - 只填真正稳定、对创作者有帮助的列
  - 没有成熟依据的列宁可留空，不凑字
  - 第二期再接入场景模块结果，继续丰富：
    - `场景搭建`
    - 更完整的 `镜头场景`
    - 更完整的 `特效运用 / 效果器 / 其他运用`

## 新增：2026-03-31 用户端素材命名与推荐阈值下一阶段约定

- 用户端素材文件名展示口径已收口：
  - 对用户前台，不再展示内部维护后缀：
    - `（整体-玄幻）`
    - `（直达-玄幻）`
    - 以及同类 `（整体/直达-赛道）`
  - 这些信息继续保留在系统内部，服务于：
    - 直达/整体音效区分
    - 赛道归属
    - 后台维护与匹配逻辑
  - 用户端最终展示仅保留：
    - 可读主名称
    - 来源标记，如 `由用户更优推荐`

- 用户端动作图谱推荐已去掉：
  - `展开另外 N 条候选`
  - 当前前台只展示每个音效词的最佳素材，避免低优先级候选干扰用户判断

- 推荐阈值系统进入下一优先阶段：
  - 初始建议阈值：
    - `0.18`
  - 该值不应写死，后续需要升级为：
    - 系统动态分析建议阈值
    - 运营可手动设置最终生效阈值
  - 第一优先入口放在：
    - `运营行为审计 -> 优化建议`
  - 该区域后续需要增加：
    - 当前生效阈值
    - 系统建议阈值
    - 参考周期
    - 阈值建议原因
    - 运营手动修正入口

- 阈值建议后续将逐步接入的参考维度：
  - 语义推荐词命中质量
  - 图谱命中层级（通用/赛道/保底/用户更优推荐）
  - 音效素材历史命中率
  - 音效下载次数
  - 用户更优推荐素材的被采纳与被下载表现
  - 运营维护与纠偏记录

- 当前共识：
  - 推荐阈值不再理解为固定常量
  - 而是“系统先建议、运营可随时调整”的动态值
  - 后续迭代重点是不断丰富参与阈值决策的数据维度，并在优化建议中完整展示参考依据

## 新增：2026-03-31 动态阈值第一阶段已落地

- 后台入口：
  - `运营行为审计 -> 优化建议`
  - 已新增 `推荐阈值建议` 区块

- 第一阶段已实现：
  - 当前生效阈值展示
  - 系统建议阈值展示
  - 参考周期展示（当前按最近 N 天）
  - 参考维度摘要展示
  - 建议原因展示
  - 运营手动保存当前生效阈值入口

- 当前生效阈值系统设置键：
  - `action_sfx_effective_threshold`
  - 默认值：
    - `0.18`

- 第一阶段系统建议阈值的参考维度：
  - 最近周期 `search_sfx` 样本数
  - 完全无命中次数与占比
  - 低分命中次数与占比（按当前生效阈值判断）
  - 平均最高分
  - 系统官方素材总数
  - 用户更优推荐素材总数
  - 已采纳投稿数
  - 用户更优推荐总下载次数

- 第一阶段策略定位：
  - 先做“系统建议 + 运营手动决定”
  - 暂不做全自动自学习改阈值
  - 先把阈值真正作用在：
    - `动作图谱推荐` 的素材展示过滤

- 当前动作图谱推荐生效规则：
  - 后端会读取 `action_sfx_effective_threshold`
  - 低于阈值的素材结果不再返回给前台展示
  - 这一步只过滤素材展示，不改变图谱词本身

- 行为日志补充：
  - `action_sfx_graph` 的操作记录中，已追加：
    - `effective_threshold`
    - `filtered_asset_count`
  - 便于后续继续做阈值质量复盘

## 新增：2026-04-01 后台结构维护与删除链路收口

- 后台 `结构维护：元数据与赛道分配` 最近完成的关键收口：
  - “问题词替代设置”拆分为两条明确链路：
    - `保存问题词替代设置`
      - 只保存 fallback 替代规则
      - 只刷新保底监控与现有规则列表
      - 不再自动跳转到维护节点
    - `保存并进入维护节点`
      - 保存 fallback 替代规则
      - 将目标词同步到 `元数据与赛道分配`
      - 第二个下拉框以 `（未保存）` 形式显示该词
      - 自动进入后续维护区
  - 上述“保存并进入维护节点”链路已增加串行 loading：
    - 同一时间只允许处理一条问题词替代流
    - loading 期间同区其它保存 / 忽略 / 去维护按钮会暂时锁定
  - 两个按钮的运营解释已加 `?`
    - 只在第一条数据展示一次，后续同类按钮不重复出现

- 后台 `元数据与赛道分配` 区域交互收口：
  - 第二个下拉框与维护节点标题已做“最后一次选择生效”保护
    - 防止异步回写导致“前面选 A，后面展示 B”
  - 选择节点后会出现明确 loading 遮罩
    - 不再只是静默禁用下拉框
  - `通用元数据层` 和 `赛道特化维护区` 中已去掉无用搜索框
  - `补入通用元数据` 已增加维护节点 loading
    - 维护节点本体会切成“加载中卡片”
    - 同时显示外层遮罩

- 后台“赛道使用控制”口径已重新命名，避免误解成删除通用词本体：
  - 原 `从赛道删除与恢复` 改为：
    - `赛道使用控制`
  - 通用元数据视角下按钮改为：
    - `停止在所选赛道使用`
    - `恢复到所选赛道使用`
  - 赛道视角下按钮改为：
    - `停止当前赛道使用`
    - `恢复当前赛道使用`
  - `停止在所选赛道使用` 后新增 `?`
    - 明确说明：保留通用元数据本体，只切断所选赛道的引用

- 后台删除链路已补回显式入口：
  - 维护节点新增：
    - `删除当前维护节点`
  - 支持三种明确路径：
    - 删除当前通用元数据节点
    - 删除通用元数据并保留所选赛道特化
    - 删除当前赛道特化节点
  - `删除通用元数据并保留所选赛道特化` 后新增 `?`
    - 说明这条链路是“降级删除”
    - 先从“通用 + 赛道”降到“只保留赛道”
    - 再从“只保留赛道”降到“完全删除节点”
    - 以正式删除方式尽量避免幽灵数据
  - 删除成功后：
    - 第二个下拉框不再把刚删掉的词重新显示为 `（未保存）`
    - 而是回到 `请选择动作词`
  - 删除成功后不再自动刷新 `节点工作台`
    - 避免删除动作把无关工作台内容强行展开

- 后台 `节点工作台` 当前已对运营隐藏：
  - 原因：
    - 该区域更偏图谱排查 / 调试，不属于日常运营主链路
  - 处理方式：
    - 前端隐藏
    - 代码未删除，保留后续复用可能
  - 顶部快捷入口 `结构维护：元数据与赛道分配`
    - 已改为直接跳到真正的维护区，而不是跳到 `节点工作台`

- 后台删除与引用池命名已收口：
  - `赛道删除池`
    - 改为 `通用词停止在赛道引用`
  - `删除后命中复核`
    - 改为 `停止引用后命中复核`
  - `通用词停止在赛道引用`
    - 当前分页已调整为 `3` 条 / 页

- 后台存在过一条已确认的幽灵数据：
  - `悬疑 -> 装备外骨骼`
  - 真实状态：
    - `common` 不存在
    - `genres['悬疑']` 不存在
    - 但 `_meta.genre_assignments['悬疑']` 里残留了 `装备外骨骼`
  - 已处理：
    - 清除该条残留 assignment
    - 修复删除通用层时的清理逻辑
      - 以后删除通用层节点时，会同步清掉各赛道中的残留：
        - `genre_assignments`
        - `inheritance_blocks`
    - 目标：
      - 避免再生成这类“下拉框里有词、但节点本体不存在”的幽灵数据

- 后台 `高级入口：直接输入节点Key`
  - 已对运营移除
  - 内部隐藏字段仍保留，便于代码复用

## 新增：2026-04-01 用户推荐 UID 与前台调试字段修复

- 推荐体系口径已继续收口：
  - `我的推荐码（UID）`
    - 不再把手机号当成 UID 展示
  - 推荐短链：
    - 只在存在真实 UID 时才带上 UID 参数
  - 后端不再用手机号默认回填 UID
  - 如果旧账号存在 `UID = 手机号` 这类历史占位值：
    - 前台会把它当成无效推荐码处理
    - 不再展示为分享 UID

- 用户端前台调试字段暴露开关已收口：
  - 数据库清空后，`frontend_debug_expose_enabled` 不再因开发环境默认值自动开启
  - 当前默认值已改为关闭
  - 背景：
    - 曾出现清空数据库后，用户端重新暴露：
      - `当前展示来源`
      - `提示词文件`
      - `赛道`
      - `实际调用模型`
      - `Contract`
    - 现已修复为：
      - 默认关闭
      - 需要时由后台显式开启

## 下一阶段明确约定：系统健康自检

- 由于未来线上系统无法依赖人工逐条排查，需要把“系统自己检查自己”的能力做成后台内建能力。
- 已达成共识：
  - 先做第一版 `动作图谱健康自检`
  - 后台增加：
    - 手动触发入口
    - 自检结果展示区
    - 异常项明细与建议动作
  - 第一批重点检查项：
    - 幽灵节点 / 残留赛道引用
    - 残留 `inheritance_blocks`
    - 空节点
    - 删除池异常
    - 成长性指标（新增词数、空节点数、删除池增长等）

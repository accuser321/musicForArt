# MusicForArt 项目唤醒与当前状态

最后更新时间：2026-03-21

## 新线程唤醒指令
请先读取并严格按此文件恢复上下文：
`/Users/demo/Documents/New project/musicForArt/PROJECT_WAKEUP.md`

然后先执行：
1. 总结当前用户端、运营端、Neo4j 三条主链路；
2. 检查并启动 Neo4j、后端、前端；
3. 优先继续当前未收尾的核心 bug 与业务功能；
4. 不做无关重构。

## 项目路径与端口
- 项目根目录：`/Users/demo/Documents/New project/musicForArt`
- 后端：`/Users/demo/Documents/New project/musicForArt/backend`
- 前端：`/Users/demo/Documents/New project/musicForArt/frontend`
- 用户端：`http://127.0.0.1:5178`
- 后台：`http://127.0.0.1:5178/admin.html`
- 后端：`http://127.0.0.1:8090`
- Neo4j HTTP：`http://127.0.0.1:7474`
- Neo4j Bolt：`127.0.0.1:7687`

## 当前系统分层

### 用户端
- 手机号 + 验证码模拟登录
- 当前已升级为测试期用户体系：
  - 用户首页标题改为 `有声书创作台`
  - 非登录状态下，所有主功能按钮都做前端禁用和后端强校验
  - 测试期间默认启用“邀请码激活制”
  - 登录区新增：
    - 邀请码（测试期必填）
    - UID（可选）
    - 推荐码（可填写 UID / 手机号 / 为空）
- 用户首页现有主功能均要求登录成功后才能执行，旧 session 也会被邀请码测试制复核
- 创建项目
- 用户端功能区已重排为：
  - 基础准备
  - 动作模块
  - 场景模块
  - 文本与执行
  - 导出与汇总
- 关键文案已调整为：
  - `文本演绎分析（含时间轴）`
  - `生成后期执行单`
- 人物动作提取
- 动作图谱推荐
- 场景搭建分析
- 场景音效推荐
- 场景补充单
- 动作补充单
- 音乐分析页误混入动作提取区块的问题已修：
  - 只有动作结构结果才会渲染动作面板
- 动作提取分段明细（测试态）已增加：
  - 该段缩略原文
  - Contract
  - Fallback
- 后期执行单页已去掉：
  - `对白/音乐` 固定比例展示
  - `推荐音效` 展示
- 用户端已隐藏 `Qwen` 切换入口：
  - 当前前台只显示默认 `DeepSeek`
  - Qwen 接线代码保留，未来可直接恢复 UI
- 用户端已新增“今日用量”面板：
  - 文本分析字符：已用 / 限额 / 剩余
  - 音效下载数量：已用 / 限额 / 剩余
  - 当前账号 / 用户级别 / 统计日期
- 用户端已新增前台热度榜区域：
  - 动作音效热度榜
  - 场景搭建热度榜
  - 支持按 `过去1天 / 过去10天 / 过去30天 / 过去3个月` 切换
  - 两个区域并排展示，移动端自动改为纵向
- 文本演绎分析与生成后期执行单结果页，已去掉无关的动作提取卡片：
  - `人物动作判定规则`
  - `提取结果概览`
  - `候选动词数`
  - `需做动作音效`
  - `功能标题`
  - `人物动作动词提取`
- 动作图谱推荐已修复两个前端体验问题：
  - 空结果也能正常渲染，不再报错
  - 单个音效下载后页面不跳走，仍停留在当前推荐页继续浏览
- 单个音效下载与 `SFX ZIP` 导出后，会立即刷新“今日用量”中的下载计数
- 下载音效时不再在右侧结果区显示“正在执行：下载 xxx.mp3”

### 运营端
- 手机号 + 验证码模拟登录
- 用户体系新增后台能力（当前先以 API 为主，后续再做后台页面）：
  - 测试期开关：
    - `GET/POST /api/admin/beta-access`
  - 邀请码列表：
    - `GET /api/admin/invite-codes`
  - 用户列表已扩展字段：
    - `uid`
    - `invite_activated`
    - `invite_code_used`
    - `referred_by_phone`
    - `referred_by_uid`
    - `referral_user_count`
- `admin.html` 当前已补到可运营使用：
  - 用户列表按每页 `6` 个分页
  - 可查看指定日期的文本已用 / 下载已用 / 剩余 / 邀请激活状态
  - 可修改前台等级默认额度
  - 可按手机号手动重置当日用量
- 动作后台已新增“前台排行榜管理”：
  - 动作榜按真实下载统计生成
  - 支持时间范围切换：`1d / 10d / 30d / 90d`
  - 支持手动覆盖：
    - 前台展示名
    - 前台副标题
    - 前台显示数量
    - 前台排序位
    - 是否展示
  - 支持调整用户端：
    - 动作榜标题
    - 场景榜标题
    - 动作榜 / 场景榜左右位置
- 场景后台已复用同一套“前台排行榜管理”能力：
  - 场景榜按真实下载统计生成
  - 与动作后台共享前台区域顺序与标题配置
- `运营行为审计` 已改成运营可读视图：
  - 行为总览
  - 行为分布
  - 最近行为时间线
  - 原始 JSON 折叠区
- 行为分布当前显示：
  - 中文业务功能名
  - 英文动作码默认折叠，点击展开 / 再点回缩
- 最近行为时间线当前规则：
  - 时间格式统一显示为 `YYYY-MM-DD HH:MM:SS`
  - 每页显示 `4` 条
  - 支持上下页翻页
- `运营漏斗` 当前明确解释为：
  - `AI 推理链路漏斗`
  - 数据源来自 `ReasoningLog`
  - 不是用户增长漏斗
- `优化建议` 当前明确解释为：
  - `图谱与素材补库建议`
  - 数据源来自 `ReasoningLog`
- 动作补充单运营
- 动作图谱 Neo4j
- 父节点浏览器
- 父节点详情：列表视图 / 图谱视图
- 场景后台独立入口：
  - `/scene.html`（已具备手机号登录、场景补充单筛选、摘要统计、卡片/表格视图、逐词上传入库、通知用户；后续继续扩成场景图谱维护与场景模板后台）

### 图谱层
- 本地动作图谱 JSON
- Neo4j 同步
- Neo4j 边已显式拆分：
  - `HAS_DIRECT_SFX_CHILD`
  - `HAS_COMPOSITE_SFX_CHILD`

## 关键业务规则

### 测试期邀请码 / 推荐码规则
当前用户体系已切到“测试期邀请码激活制”：

1. 系统默认生成 `200` 个邀请码并写入数据库
2. 默认测试期开关为开启：
   - `beta_invite_only_enabled = true`
3. 测试期间：
   - 无邀请码的用户即使拿到验证码，也不能完成激活登录
   - 邀请码一旦激活成功，立即失效
4. 测试期开关关闭后：
   - 用户可正常注册/登录使用
   - 不再强制要求邀请码
5. 推荐码支持三种输入：
   - UID
   - 手机号
   - 留空
6. 推荐关系写入用户表：
   - `referred_by_user_id`
   - `referred_by_phone`
   - `referred_by_uid`
   - `referral_input`
7. 种子用户的拉新人数通过 `referral_user_count` 统计

### 用户级别与后台角色
当前已拆成两条维度，避免同一个手机号“既是后台管理员，又是前台种子用户/普通用户”时相互冲突：

1. 后台角色 `ops_role_code`
   - `00`：超级管理员
   - `11`：管理员
   - `22`：预留，不作为后台角色主用
   - `33`：普通后台无权限

2. 前台用户级别 `user_tier_code`
   - `22`：种子用户（测试期间可正常使用用户端功能）
   - `33`：普通用户（正式上线后的正常用户级别）

3. 当前超级管理员手机号
   - `15914141177`
   - 会在系统启动时自动写成：
     - `ops_role_code = 00`
     - `is_admin = 1`
     - `invite_activated = 1`

4. 同一手机号允许：
   - 后台角色是 `00/11`
   - 同时前台用户级别仍然是 `22/33`

5. 当前动作后台 `admin.html` 已新增：
   - 测试期开关
   - 邀请码池查看
   - 用户 UID
   - 后台角色码
   - 前台用户级别码

### 每日额度与去重计数规则
当前测试期已接入真实额度规则：

1. 种子用户 `22`
   - 每日文本分析总字符数：`10000`
   - 每日音效下载总数：`200`
2. 普通用户 `33`
   - 每日文本分析总字符数：`5000`
   - 每日音效下载总数：`100`
3. 授权用户 / 管理员
   - 当前默认不扣每日额度
4. 文本额度已真实接入这些链路：
   - `text_analysis`
   - `action_verb_analysis`
   - `scene_building_analysis`
   - `text_narration_analysis`
5. 下载额度已真实接入这些链路：
   - 单个音效下载 `/api/sfx/file`
   - `SFX ZIP` 导出下载
6. 同一用户、同一天、完全相同文本内容：
   - 只在第一次计入字符额度
   - 后续重复提交相同文本不再重复扣字符
7. 后台可按手机号手动重置指定日期额度：
   - `POST /api/admin/users/reset-usage`

### 前台热度榜规则
当前已新增“用户端热度榜”功能，供用户直接查看最近下载热度：

1. 动作榜与场景榜都支持时间维度：
   - `过去1天`
   - `过去10天`
   - `过去30天`
   - `过去3个月`
2. 动作榜当前统计：
   - 下载最多的动作音效 Top10
   - 下载最多的通用音效 Top10
   - 下载最多的玄幻赛道音效 Top10
   - 下载最多的言情赛道音效 Top10
   - 下载最多的科幻赛道音效 Top10
   - 下载最多的悬疑赛道音效 Top10
3. 场景榜复用同样的统计结构，但数据只来自场景音效下载
4. 当前不把场景榜混进动作后台逻辑里，而是动作后台 / 场景后台共用一套排行榜接口
5. 排行榜默认以真实下载数据为准，但后台允许对单条展示做手动覆盖
6. 当前排行榜下载数据源：
   - 动作音效下载：`action_sfx_asset_download`
   - 场景音效下载：`scene_sfx_asset_download`
7. 用户端展示读取：
   - `GET /api/home/leaderboards`
8. 后台管理读取：
   - `GET /api/admin/leaderboards/config`
   - `POST /api/admin/leaderboards/layout`
   - `POST /api/admin/leaderboards/override`

### 双核心模块方向
当前系统后续不再只围绕“动作分析”扩展，而是明确转向两个并列核心模块：

1. 动作分析
2. 场景搭建

规则：

1. 场景搭建不是动作图谱推荐的附属区块
2. 场景搭建必须作为用户端独立模块存在
3. 两个模块后续可以在结果页做联动，但不混模块
4. 当动作分析与场景搭建都稳定后，后续再进入第三阶段：
   - 导演思维融合
   - 近景 / 中景 / 远景
   - 混响 / 延迟 / 空间调度等创作建议

注意：

当前阶段，场景搭建不承担导演思维分析，不替用户做创作决策，只针对文本内容做场景识别、场景构成、图谱命中、素材召回与下载支持。

### 模块解耦约束
从当前开始，必须遵守：

1. 动作分析后台继续保留在 `admin.html`
2. 场景搭建后台独立建设在 `scene.html`
3. 场景模块后端服务、数据表、Neo4j 关系均单独建设
4. 不再把 scene 逻辑继续堆进 action 的后台页面与业务表
5. 第三模块“导演思维融合”未来作为上层整合，不反向要求底层耦合

### 命名规则
统一展示名：
- `{音效名}（{类型}-{赛道}）`

例子：
- `吐（组合-玄幻）`
- `手势摩擦声（直达-玄幻）`

### 音效分类
- `直达音效`：如 `摩擦声`、`挥手声`
- `组合音效`：如 `掐诀`、`吐`

### 同一素材可跨赛道复用
- 物理文件不要求重复存两份
- 业务关系允许同时挂到多个赛道
- 用户下载时仍按赛道展示不同 display name

### 状态逻辑
业务状态：
- `pending`：待合并
- `partial`：部分补齐
- `ready_to_notify`：已补齐待通知
- `merged`：全补齐视图

提醒状态：
- `未通知`
- `已通知`

注意：
- `notified` 不是业务状态终点
- 当前 UI 视图要求：
  - 已通知的数据不再出现在“已补齐待通知”视图中

## 当前已完成功能

### 用户端
- 动作图谱推荐支持：
  - 列表视图
  - 图谱视图
- 已去掉“报告模式”入口，用户端不再区分该模式
- “为什么推荐这些音效”解释区
- 命中素材表只保留表格展示
- `待补子级音效` 使用 `? / ✓` 状态展示
- 人物动作提取表格已去掉“判定原因”
- 用户端首页已增加场景后台独立入口提示
- 用户端首页已增加场景搭建独立主链路按钮：
  - 场景搭建分析
  - 场景音效推荐
  - 场景补充单
- 用户端下载行为已改为“静默下载”：
  - 不再覆盖当前结果页
  - 不再额外显示下载中的执行日志
- 用户端结果区已增加“今日用量”可视面板，便于复测额度逻辑

### 运营端
- 动作补充单按父节点分组
- 逐词上传
- 动作图谱维护：
  - 两级下拉选择
  - 通用层 / 赛道层维护
  - 通用层继承屏蔽 / 恢复继承
  - 继承屏蔽池 / 屏蔽后命中复核
  - 节点级清理
  - 通用层与赛道层双向迁移
- 场景后台独立壳子已建立：
  - `/Users/demo/Documents/New project/musicForArt/frontend/scene.html`
- 场景图谱主数据已建立：
  - `/Users/demo/Documents/New project/musicForArt/backend/assets/scene/scene_graph.json`
- 场景图谱管理服务已建立：
  - `/Users/demo/Documents/New project/musicForArt/backend/app/services/scene_graph_manage.py`
- 场景图谱 Neo4j 骨架已建立：
  - `/Users/demo/Documents/New project/musicForArt/backend/app/services/scene_graph_neo4j.py`
- 顶部通知目标改为下拉选择（手机号 + 补充单 ID）
- 用户与测试期管理已支持：
  - 前台等级与后台角色分离
  - 每日文本 / 下载额度编辑
  - 指定日期用量查看
  - 按手机号手动重置当日用量
  - 用户列表每页 6 条分页
- 运营行为审计已支持：
  - 行为总览卡
  - 中文业务功能分布
  - 最近行为时间线分页
  - 原始 JSON 折叠查看
- Neo4j 父节点浏览器：
  - 筛选
  - 排序
  - 摘要卡
  - 今日优先补充/通知
  - 列表 / 图谱双视图
- 父节点详情支持：
  - 子级关系
  - 已补 / 待补
  - 补充单状态
  - 节点内通知

### Neo4j
- 本地 Neo4j 已安装并可运行
- ActionNode 已写入业务状态：
  - `supplement_item_count`
  - `covered_count`
  - `pending_count`
  - `completion_ratio`
  - `ready_to_notify_count`
  - `notified_count`

## 当前最关键的已修 bug

### 1. 已通知仍出现在 ready_to_notify
已修：
- `ready_to_notify` 视图只显示：
  - 业务状态为 `ready_to_notify`
  - 且 `notified_at` 为空
- 已通知只出现在：
  - `全部状态`
  - `已通知用户`

### 2. 用户端命中素材但 `待补组合音效` 仍是问号
已修：
- 用户端 `? / ✓` 改为读取全局覆盖结果
- 不再只看当前一次补充单

### 3. 第二次同文复测时历史已补素材失效
已修：
- 核心原因：之前只按当前 supplement item 看素材，未按父节点全局复用
- 现在改成：
  - 按 `赛道 + target_head` 生成父节点 key
  - 从历史 `ActionSupplementAsset` 全局聚合已入库素材
  - 用户端、运营端、Neo4j 三边统一读这套覆盖结果

例子：
- `玄幻::吐` 现在能全局读到历史 `吐.mp3`
- 第二次同文复测不应再把 `吐（组合）` 变回问号

### 4. 同音效词跨父节点复用
已修：
- 如果 `玄幻::飞身` 已补过 `快速移动声`
- 而 `玄幻::躲闪` 也挂了 `快速移动声`
- 那么 `躲闪` 现在也应直接命中这份素材

### 5. 无明确音效词的动作自动生成保底组合音效
已修：
- 对 `骑`、`挥`、`操纵` 这类前台已展示但图谱里暂无 `sfx_terms` 的动作
- 系统自动生成保底组合音效：
  - `骑（组合）`
  - `挥（组合）`
  - `操纵（组合）`
- 这样用户端有可下载/可匹配路径，运营端也有上传入口

### 6. 通知下拉框独立于主列表状态
已修：
- 顶部通知下拉框不再跟随 `pending / merged / 全部状态` 变化
- 只展示：
  - `ready_to_notify`
  - 且 `未通知`
  的单条补充单
- 同时保留表格勾选批量通知

## 当前需要继续重点盯的链路
如果用户继续反馈“第二次复测仍失效”，优先检查：
1. `action_sfx_graph.py`
   - `load_action_node_coverage`
   - `build_action_sfx_recommendation`
2. `main.py`
   - `ops_action_supplements`
   - `ops_action_supplements_merge`
   - `analyze_action_graph_draft_api`
3. `action_graph_neo4j.py`
   - `_business_stats_by_node_key`

## 新增：场景搭建模块的大框架

### 一、产品定位
场景搭建与动作分析同级，都是用户端核心功能，目标是解决有声书行业中的“声音世界搭建”痛点。

动作分析解决：

1. 人物做了什么
2. 需要哪些动作音效

场景搭建解决：

1. 这一段文本属于什么场景
2. 场景由哪些可听见的元素构成
3. 场景应该命中哪些场景音效
4. 当前有哪些素材可直接下载
5. 还缺哪些场景音效素材

### 二、明确边界
场景搭建当前只做：

1. 文本场景识别
2. 场景构成拆解
3. 场景图谱命中
4. 场景音效召回与下载
5. 场景补充缺口生成

场景搭建当前不做：

1. 导演思维分析
2. 镜头语言分析
3. 近景 / 中景 / 远景的创作决策
4. 混响、延迟、空间设计等创作者能力替代

这些属于后续“第三模块：导演思维融合”阶段再做。

### 三、系统主链路
场景搭建建议沿用当前动作分析的主架构：

1. 用户上传文本
2. LLM 产出场景结论
3. 系统基于结论命中 Neo4j / 场景图谱
4. 命中场景关联词
5. 命中场景音效素材
6. 输出可下载结果与缺口
7. 缺口进入后续运营补充链路

### 四、场景搭建模块的核心输出
用户端建议最少包含：

1. 场景主识别
2. 场景构成要素
3. 场景关联词
4. 场景音效来源（可下载）
5. 待补场景音效

### 五、最重要的设计原则：场景可复制性
场景搭建与动作分析不同，它天然更强调“可复用的大场景集合”。

原因：

1. 场景文本写法会变化
2. 但声音制作上会收敛到稳定的大场景类型
3. 所以系统后续必须逐步沉淀可复用场景库

例如：

1. 深夜山林
2. 雨夜街巷
3. 客栈大堂
4. 宫廷长廊
5. 战场边缘

这意味着场景搭建模块后续必须建设：

1. 通用场景集合
2. 赛道场景集合
3. 场景构成词集合
4. 场景音效大集合

目标：

1. 新文本优先命中已有大场景
2. 不要每次从零生成全新场景
3. 只有明显不属于现有集合时，才新增候选

### 六、Neo4j / 图谱层建议方向
场景搭建图谱后续建议包含：

1. 场景主节点
2. 场景构成词
3. 直达场景音效
4. 组合场景音效
5. 场景模板 / 场景集合层

场景模板 / 场景集合层是关键，它决定系统的复用性与稳定性。

### 七、提示词来源
场景搭建模块的 LLM 提示词，不由当前开发阶段拍脑袋编写。

后续以用户提供的“有声书老师教学逐字稿”为主输入，由系统从逐字稿中提炼：

1. 场景识别提示词
2. 场景构成拆解提示词
3. 场景图谱命中所需结构化字段

### 八、开发优先级
当前优先级是：

1. 先沉淀模块框架与数据结构
2. 再从老师逐字稿提炼提示词
3. 再开始场景搭建模块开发

不要直接跳过框架设计开始写代码。

### 九、当前已起的后端骨架
当前 scene 模块后端已经起壳：

1. 场景分析服务：
   - `/Users/demo/Documents/New project/musicForArt/backend/app/services/scene_building.py`
2. 场景音效推荐服务：
   - `/Users/demo/Documents/New project/musicForArt/backend/app/services/scene_sfx_graph.py`
3. Prompt：
   - `/Users/demo/Documents/New project/musicForArt/backend/app/prompts/V3-scene_building_task.txt`
   - `/Users/demo/Documents/New project/musicForArt/backend/app/prompts/V3-scene_building_task_retry.txt`
4. 场景分析接口：
   - `POST /api/analysis/<project_id>/scene-building`
5. 场景音效推荐接口：
   - `POST /api/analysis/<project_id>/scene-sfx`
6. 场景补充单生成接口：
  - `POST /api/analysis/<project_id>/scene-supplements`
7. 场景补充单运营查询接口：
  - `GET /api/ops/scene-supplements`
8. 场景补充单运营动作接口：
  - `POST /api/ops/scene-supplements/merge`
  - `POST /api/ops/scene-supplements/notify`
9. 场景图谱维护接口：
  - `GET /api/scene-graph/catalog`
  - `GET /api/scene-graph/node-layers`
  - `POST /api/scene-graph/node-layer`
  - `GET /api/scene-graph/collections`
  - `GET /api/scene-graph/templates`
  - `POST /api/scene-graph/template`
  - `GET /api/scene-graph/neo4j-status`
  - `POST /api/scene-graph/neo4j-sync`

### 十、当前已起的数据表
当前 scene 模块已新增独立表：

1. `scene_analysis`
2. `scene_supplement_task`
3. `scene_supplement_asset`

说明：

1. 这三张表与 action 模块分开
2. 当前先满足独立链路起壳
3. 后续再补 scene 侧完整运营功能、入库与通知

## 关键文件
- 后端主入口：
  - `/Users/demo/Documents/New project/musicForArt/backend/app/main.py`
- 动作图谱推荐：
  - `/Users/demo/Documents/New project/musicForArt/backend/app/services/action_sfx_graph.py`
- Neo4j 图谱服务：
  - `/Users/demo/Documents/New project/musicForArt/backend/app/services/action_graph_neo4j.py`
- 用户端页面：
  - `/Users/demo/Documents/New project/musicForArt/frontend/index.html`
- 运营后台页面：
  - `/Users/demo/Documents/New project/musicForArt/frontend/admin.html`
- 场景搭建策略文档：
  - `/Users/demo/Documents/New project/musicForArt/docs/SCENE_BUILDING_MODULE_STRATEGY.md`
- 场景搭建输出结构文档：
  - `/Users/demo/Documents/New project/musicForArt/docs/SCENE_BUILDING_OUTPUT_SCHEMA.md`
- 场景搭建 Prompt v1：
  - `/Users/demo/Documents/New project/musicForArt/docs/SCENE_BUILDING_PROMPT_V1.md`
- 场景模块解耦方案：
  - `/Users/demo/Documents/New project/musicForArt/docs/SCENE_MODULE_DECOUPLING_PLAN.md`
- 动作图谱数据：
  - `/Users/demo/Documents/New project/musicForArt/backend/assets/sfx/action_graph.json`
- 数据库：
  - `/Users/demo/Documents/New project/musicForArt/backend/music_for_art.db`

## 本地文件存储
当前开发环境音效统一存储目录：
- `/Users/demo/Documents/New project/musicForArt/backend/assets/sfx/`

当前测试数据清理策略：
- 保留 `user_account`
- 清空业务表：
  - `projects`
  - `audio_analysis`
  - `text_analysis`
  - `narration_analysis`
  - `fusion_plan`
  - `action_supplement_task`
  - `action_supplement_asset`
  - `user_operation_log`
  - `auth_code`
  - `auth_session`
- 清理测试音效文件，仅保留：
  - `README.md`
  - `semantic_lexicon.json`
  - `action_graph.json`

上线建议：
- 音效文件：腾讯云 COS
- 业务元数据：MySQL
- 图谱关系：Neo4j
- 本地磁盘：仅临时缓存

## Neo4j 本地运行
Neo4j 目录：
- `/Users/demo/Documents/New project/musicForArt/.local/neo4j/neo4j-install/neo4j/2026.02.2/libexec`

JRE 目录：
- `/Users/demo/Documents/New project/musicForArt/.local/jdks/zulu21-jre/zulu-21.jre/Contents/Home`

启动：
```bash
cd "/Users/demo/Documents/New project/musicForArt/.local/neo4j/neo4j-install/neo4j/2026.02.2/libexec"
export JAVA_HOME="/Users/demo/Documents/New project/musicForArt/.local/jdks/zulu21-jre/zulu-21.jre/Contents/Home"
./bin/neo4j start
./bin/neo4j status
```

## 标准启动命令

### 启动后端
```bash
cd "/Users/demo/Documents/New project/musicForArt/backend"
source .venv/bin/activate
pkill -f "python -m app.main" || true
nohup python -m app.main >/tmp/musicforart-backend.log 2>&1 &
sleep 3
curl -sS http://127.0.0.1:8090/health
```

### 启动前端
```bash
cd "/Users/demo/Documents/New project/musicForArt/frontend"
pkill -f "python3 -m http.server 5178" || true
nohup python3 -m http.server 5178 --bind 127.0.0.1 >/tmp/musicforart-frontend.log 2>&1 &
sleep 2
curl -sS http://127.0.0.1:5178/ | head -n 5
curl -sS http://127.0.0.1:5178/admin.html | head -n 5
```

### 验证 Neo4j
```bash
curl -sS http://127.0.0.1:8090/api/action-graph/neo4j-status
```

期待：
- `neo4j_configured: true`
- `neo4j_connected: true`

## 排障命令
```bash
tail -n 120 /tmp/musicforart-backend.log
tail -n 120 /tmp/musicforart-frontend.log
lsof -nP -iTCP:8090 -sTCP:LISTEN
lsof -nP -iTCP:5178 -sTCP:LISTEN
```

## 当前协作方式
- 用户通常会批量测试后统一反馈
- 回答时优先：
  1. 明确是否已修
  2. 说明根因
  3. 给最短部署命令
  4. 给复测步骤和期待值

## Scene 模块最新进展
- `scene` 已确立为与动作分析并列的独立核心模块，不再继续耦合进 `admin.html`
- 独立后台页：
  - `/Users/demo/Documents/New project/musicForArt/frontend/scene.html`
- 独立后端服务：
  - `scene_building.py`
  - `scene_sfx_graph.py`
  - `scene_graph_manage.py`
  - `scene_graph_neo4j.py`
- 独立主数据：
  - `/Users/demo/Documents/New project/musicForArt/backend/assets/scene/scene_graph.json`

### Scene 已落地链路
- 用户端：
  - `场景搭建分析`
  - `场景音效推荐`
  - `场景补充单`
- 后端接口：
  - `POST /api/analysis/<project_id>/scene-building`
  - `POST /api/analysis/<project_id>/scene-sfx`
  - `POST /api/analysis/<project_id>/scene-supplements`
  - `GET /api/ops/scene-supplements`
  - `POST /api/ops/scene-supplements/merge`
  - `POST /api/ops/scene-supplements/notify`

### Scene 图谱维护
- 已支持：
  - `GET /api/scene-graph/catalog`
  - `GET /api/scene-graph/node-layers`
  - `POST /api/scene-graph/node-layer`
  - `POST /api/scene-graph/delete-node`
  - `GET /api/scene-graph/collections`
  - `GET /api/scene-graph/templates`
  - `POST /api/scene-graph/template`
  - `GET /api/scene-graph/neo4j-status`
  - `POST /api/scene-graph/neo4j-sync`
  - `POST /api/scene-graph/promote`
  - `POST /api/scene-graph/demote`
- 节点级双向迁移已接通：
  - 赛道层节点 -> 通用层
  - 通用层节点 -> 目标赛道层
- `scene.html` 中已新增“节点级迁移”操作区：
  - `迁移到通用层，并保留赛道层`
  - `迁移到通用层，并从赛道层移除`
  - `迁移到赛道层，并保留通用层`
  - `迁移到赛道层，并从通用层移除`
- `scene.html` 中已新增“删除当前层节点”操作：
  - 通用层视角删除时，会明确提醒会影响所有继承该通用层的赛道
  - 赛道层视角删除时，只删除当前赛道下的真实节点
- `scene.html` 中已新增“新建空白场景节点”入口：
  - 可直接创建通用层或赛道层的空白节点
  - 运营填写内容后即可保存当前层，无需先等目录里已有节点
  - 图谱编辑区的两个写操作按钮现在也是“状态联动”的：
    - `新建空白节点`
      - 未输入节点名时禁用，并显示 `请先输入节点名`
      - 输入节点名后自动恢复可点击
    - `保存当前层`
      - 未选择节点、也未创建空白节点时禁用，并显示 `请先选择或新建节点`
      - 一旦已选节点或已创建空白节点，就自动恢复为 `保存当前层`
- 场景节点下拉目录现在会直接展示：
  - `通用+赛道`
  - `赛道真实节点`
  - `通用继承`
  - 以及模板名、场景集合名
- 目录下方会显示当前层/赛道的节点数量摘要，帮助运营在加载前先判断目录状态
- `scene.html` 已新增“节点来源解释”卡：
  - 会直接解释当前节点是 `通用层真实节点`、`赛道真实节点`、`通用层继承` 还是 `通用层 + 赛道层`
  - 同时给运营明确的维护提示，减少把“继承节点”误当成“赛道真实节点”的风险
- `scene_graph_manage.py` 的节点详情现在会返回 `inheritance_scope`
  - `inheriting_genres`
  - `real_genre_nodes`
- `scene.html` 的来源解释卡现在会进一步显示“影响范围”
  - 删除通用层节点前，能直接看到还会影响哪些赛道继承
  - 删除赛道层真实节点时，也会说明只影响当前赛道
- `scene.html` 的节点级迁移 / 删除区，现在也补成“状态联动”
  - 初始未加载节点时：
    - 迁移按钮统一显示 `请先加载节点`
    - 删除按钮统一显示 `请先加载节点`
  - 通用层视角：
    - 上迁按钮会禁用并显示 `通用层视角不可上迁`
    - 下迁按钮按通用层真实节点是否存在决定是否可用
  - 赛道层视角：
    - 下迁按钮会禁用并显示 `赛道层视角不可下迁`
    - 上迁按钮按赛道真实节点是否存在决定是否可用
  - 删除当前层节点后：
    - 迁移 / 删除区会自动回退到“请先重新加载场景节点”的空状态
- `scene.html` 已新增“节点摘要”区：
  - 汇总模板、场景集合、来源状态、影响继承赛道、已存在真实赛道节点
  - 让运营在顶部就能快速判断当前节点的大致维护状态

### Scene 集合维护
- `scene.html` 中的大场景集合不再只是预览，已支持：
  - 修改集合名称
  - 清空当前集合引用
- 后端接口：
  - `POST /api/scene-graph/collection-rename`
  - `POST /api/scene-graph/collection-delete`
- 当前规则：
  - 集合改名会同步更新引用它的节点与模板
  - 清空集合引用只会移除集合名，不会删除节点或模板本身
- `scene.html` 的节点维护现在已支持“模板 -> 场景集合”轻自动联动：
  - 节点模板命中已有模板名时，会自动带出该模板所属的场景集合
  - 保存节点时，如果集合为空，也会优先回填模板关联集合
- `scene.html` 现在会在集合/模板操作区直接提示影响范围：
  - 集合选择后，会提示当前集合引用了多少通用层节点、多少赛道层节点
  - 模板选择后，会提示当前模板被多少个节点引用
  - 让运营在改名、删除、清空引用前，就能明确知道会影响“节点 / 模板 / 两者”
- `scene.html` 的集合维护区，现在也补成“状态联动”：
  - 未选择集合时：
    - `修改集合名称` 显示 `请先选择集合`
    - `清空当前集合引用` 显示 `请先选择集合`
  - 已选集合但未输入新名称时：
    - `修改集合名称` 显示 `请先输入新名称`
  - 只有“已选集合 + 已填新名称”时，才恢复为真正的改名按钮

### Scene 模板维护
- 已支持：
  - 保存模板
  - 删除当前模板
- 模板删除不会自动删除已引用该模板的场景节点，但会返回受影响节点数量，供运营复核
- 模板维护区现在也补成“状态联动”：
  - 未输入模板名称时：
    - `保存模板` 显示 `请先输入模板名称`
  - 未选择模板、且当前也没有有效模板名称时：
    - `删除当前模板` 显示 `请先选择模板`
  - 进入有效状态后，按钮再恢复为 `保存模板 / 删除当前模板`
- 模板列表现在会直接展示：
  - `引用节点数`
  - `引用节点`
- 运营在选择模板时，会收到“当前模板已被多少个节点引用”的即时提示

### Scene 推荐展示
- 用户端场景模块现在会显示更明确的图谱来源：
  - `来源类型`
  - `模板命中`
  - `场景集合`
  - `含保底词`
- 用户端 `index.html` 的 scene 卡片现在增加了专用来源标签行：
  - `来源`
  - `模板`
  - `集合`
  - `含保底词`
- 目标是让场景搭建结果在视觉上更像独立核心模块，而不是动作分析旁边的一段普通文字
- 用户端 scene 总览现在还会直接统计：
  - 场景切换点
  - 模板命中场景数
  - 通用层命中场景数
  - 赛道层命中场景数
  - 保底生成场景数
  - 已有素材场景数（scene-sfx）
- 用户端第 `4.6` 步“场景补充单”现在也已有独立面板展示：
  - 场景补充单总览
  - 场景补充单明细
  - 第 4 / 4.5 / 4.6 三段关系说明
  - `4.6` 面板里现在还直接给了后续处理入口提示：
    - `打开 scene 后台`
    - 并明确说明后续应进入：
      - 场景补充单运营
      - 场景图谱维护
  - 三段关系说明里也补了“用户端到后台的闭环”：
    - 用户端负责识别、推荐、写入补充单
    - scene 后台负责补素材、改图谱、调模板与集合
  - 当前又补了一层“结果层级”：
    - `结果层级：可转后台补库`
    - `结果层级：待复核`
    - `结果层级：处理中`
    - `结果层级：已补齐`
  - 总览也新增了：
    - `可直接转后台补库`
    - `建议先复核图谱`
  - 目的是让用户在进入 scene 后台前，就能先判断：
    - 这条补充单是不是已经可以直接交给后台补素材
    - 还是应先回到 scene 图谱检查推荐方向
- 用户端按钮区已补 scene 模块链路提示：
  - 先 `4) 场景搭建分析`
  - 再 `4.5) 场景音效推荐`
  - 最后 `4.6) 场景补充单`
- 用户端 scene 三步按钮已改成“状态联动”：
  - `4) 场景搭建分析`：未创建项目时禁用
  - `4.5) 场景音效推荐`：只有完成 `4)` 后才解锁
  - `4.6) 场景补充单`：只有完成 `4.5)` 后才解锁
  - 按钮文案也会联动变化：
    - 未解锁时显示 `待第4步 / 待第4.5步`
    - 已完成一轮后显示 `可重跑`
  - 按钮下方提示会根据当前进度动态切换，不再主要依赖点击后报错来引导用户下一步
  - 新建项目、重做 `4)`、重做 `4.5)`、重做 `4.6)` 时，现在都会先清理对应的旧结果状态：
    - 重做 `4)`：会先清空 `4 / 4.5 / 4.6` 的旧结果
    - 重做 `4.5)`：会先清空 `4.5 / 4.6` 的旧结果
    - 重做 `4.6)`：会先清空 `4.6` 的旧结果
  - 目的：避免第 `4)` 或 `4.5)` 已经开始重跑时，页面还残留上一轮的下游结果，造成“看起来像还能沿用”的误解
  - 现在重跑时还会先给出明确的“重跑中占位提示”：
    - 重跑 `4)`：提示已清空上一轮的场景分析、场景音效推荐、场景补充单结果
    - 重跑 `4.5)`：提示已清空上一轮的场景音效推荐、场景补充单结果
    - 重跑 `4.6)`：提示已清空上一轮的场景补充单结果
  - 目的：让用户在请求返回前，能明确知道当前是“正在重跑该步骤”，而不是误以为页面短暂空白是异常
- 用户端 `scene` 三段结果又补了一轮“阶段感”：
  - `4) 场景搭建分析`：顶部会显示“当前阶段：场景识别”
  - `4.5) 场景音效推荐`：顶部会显示“当前阶段：图谱命中与素材召回”
  - `4.6) 场景补充单`：顶部会显示“当前阶段：缺口写入补充单”
  - 每段总览下方都补了很短的“这一步在回答什么 / 下一步去哪里”的说明，降低三段结果之间的跳跃感
- 用户端 `scene` 结果又补了两类风险提醒：
  - `保底生成提醒`
    - 当场景主要依赖系统保底结果、而非稳定图谱命中时，单独拎出来提示
  - `缺口提醒`
    - 当场景音效推荐仍存在明显缺口时，单独提示优先涉及哪些场景
  - 目的是让用户更容易区分：
    - 哪些是相对稳定的图谱结果
    - 哪些仍需要继续维护 scene 图谱或写入场景补充单
- 用户端 `scene` 卡片现在也已补“建议下一步”：
  - `4) 场景搭建分析` 的每个场景卡片，会根据：
    - 通用层命中
    - 赛道层命中
    - 模板命中
    - 是否保底
    给出下一步建议
  - `4.5) 场景音效推荐` 的每个场景卡片，会根据：
    - 是否已有素材
    - 是否仍有缺口
    - 是否同时存在保底
    给出下一步建议
  - 目标：让用户在看到单个场景时，就知道该去 scene 图谱维护、继续做音效推荐，还是写入场景补充单
- 用户端 `scene` 结果页现在还补了统一的“结果层级”：
  - `4) 场景搭建分析`
    - 每个场景卡片都会显示：
      - `结果层级：稳定图谱`
      - `结果层级：通用图谱`
      - `结果层级：模板过渡`
      - `结果层级：保底结果`
      - `结果层级：待完善`
    - 总览也新增了 `结果较稳定场景`
  - `4.5) 场景音效推荐`
    - 每个场景卡片都会显示：
      - `结果层级：可直接使用`
      - `结果层级：部分命中`
      - `结果层级：待补素材`
      - `结果层级：保底待补`
      - `结果层级：待确认`
    - 总览也新增了 `可直接使用场景`
  - 这层的作用，是把“来源标签 / 风险提醒 / 下一步建议”再往前收口成一套统一口径，让用户和运营更容易判断当前结果是否已经够稳定、该继续下载使用，还是应优先回到 scene 图谱维护
- 当前设计原则：
  - 场景分析与动作分析分开建设
  - 未来第三模块再做导演思维层的上层融合

### Scene 后台文案
- `scene.html` 已做一轮文案降噪：
  - 顶部与模块说明改短
  - “调试输出”改成“请求回显”
  - 多处开发态说明已压成更产品化的短句
- `scene.html` 已新增快捷导航：
  - 场景补充单
  - 场景图谱
  - 场景集合
  - 场景模板
  - Scene Neo4j
  - 请求回显
- “请求回显”已改成默认折叠，降低长页面滚动干扰
- `scene.html` 的 `Scene Neo4j` 区，现在也补成“状态联动”：
  - 未登录时：
    - `查看 Neo4j 状态` 显示 `请先登录后查看`
    - `同步场景图谱到 Neo4j` 显示 `请先登录后同步`
  - 已登录但还没读取状态时：
    - 同步按钮显示 `先查看 Neo4j 状态`
  - 读取状态后：
    - 若未配置：按钮显示 `Neo4j 未配置` 并禁用
    - 若已配置但未连通：按钮显示 `Neo4j 未连接，仍尝试同步`
    - 若已配置且连通：按钮显示 `同步场景图谱到 Neo4j`
  - 区块下方提示文案也会随状态同步变化，不再只依赖点击后的返回信息
- `scene.html` 的顶部登录区 / 场景补充单加载区，现在也补成“状态联动”：
  - 登录区：
    - 未输入有效手机号时，`发送验证码` 显示 `请先输入手机号`
    - 未输入有效手机号或验证码时，`登录后台` 显示 `请先输入验证码`
    - 输入满足条件后，按钮才恢复为 `发送验证码 / 登录后台`
  - 场景补充单运营区：
    - 未登录时，`加载场景补充单` 显示 `请先登录后加载`
    - 未输入天数时，会显示 `请先输入天数`
    - `清空当前筛选` 在未登录时也会禁用，并显示 `请先登录后操作`
    - 正在拉取补充单列表时，`加载场景补充单` 会显示 `加载中...`
- `scene.html` 现在还补了一层“函数入口登录兜底”：
  - 即使绕过按钮状态，下面这些核心加载函数也会先拦住未登录调用：
    - `loadSceneSupplements`
    - `loadSceneGraphCatalog`
    - `loadSceneGraphTopPriority`
    - `loadSceneGraphNode`
    - `loadSceneCollections`
    - `loadSceneTemplates`
    - `loadSceneTermSuggestions`
    - `loadSceneNeo4jStatus`
    - `syncSceneNeo4j`
  - 目的不是重复提示，而是避免在未登录状态下直接打出一串 `401`，把真正问题变成“页面坏了”的错觉
- `scene.html` 的写操作函数，也已补“函数入口登录兜底”：
  - 即使绕过按钮禁用，下面这些写操作也会先在函数入口拦住未登录调用，并给出明确提示：
    - `renameSceneCollection`
    - `deleteSceneCollection`
    - `saveSceneTemplate`
    - `deleteSceneTemplate`
    - `openSceneGraphFromSupplement`
    - `saveSceneGraphLayer`
    - `promoteSceneNode`
    - `demoteSceneNode`
    - `deleteSceneCurrentLayerNode`
    - `mergeSceneAsset`
    - `notifySceneSupplement`
  - 目的同样是减少未登录状态下的误操作和 `401` 噪音，让运营看到的是“请先登录后操作”，而不是误以为页面或接口损坏
- `scene.html` 的高频异步动作，现已补“请求中防连点”：
  - 登录区：
    - `发送验证码` 请求中会显示 `发送中...`
    - `登录后台` 请求中会显示 `登录中...`
  - 集合维护区：
    - 改名请求中会显示 `修改中...`
    - 清空集合引用请求中会显示 `清空中...`
  - 模板维护区：
    - 保存模板请求中会显示 `保存中...`
    - 删除模板请求中会显示 `删除中...`
  - 图谱维护区：
    - `保存当前层` 请求中会显示 `保存中...`
    - 节点级上迁 / 下迁请求中会显示 `迁移中...`
    - `删除当前层节点` 请求中会显示 `删除中...`
    - `加载当前最高优先节点` 请求中会显示 `加载中...`
    - `加载场景节点` 请求中会显示 `加载中...`
  - 场景补充单卡片区：
    - `上传并合并入库` 请求中会显示 `上传中...`
    - `标记已通知` 请求中会显示 `通知中...`
    - `查看该场景图谱 / 去图谱维护` 请求中会显示 `定位中...`
  - 请求完成后，按钮会回到当前应有的状态联动文案，减少网络慢时的重复点击和重复提交
- `scene.html` 的按钮旁提示（`inline-status`）现在也已统一状态样式：
  - 进行中：暖色提示
  - 成功完成：绿色提示
  - 失败 / 缺条件：红色提示
  - 普通说明：默认灰色
  - 这层不是新业务逻辑，而是把保存、迁移、删除、上传、通知这些动作的就地反馈统一成更容易扫读的视觉语言，方便后续集中复测时快速识别哪里成功、哪里失败、哪里还在处理中
- `scene.html` 的场景补充单卡片按钮，现在也已做“未登录状态显式表达”：
  - `查看该场景图谱 / 去图谱维护`
    - 未登录时：按钮直接显示 `请先登录后定位`
  - `上传并合并入库`
    - 未登录时：按钮直接显示 `请先登录后上传`
    - 同时函数层 `refreshSceneMergeState` 也会直接给出 `请先登录后上传并合并`
  - `通知用户`
    - 未登录时：按钮直接显示 `请先登录后通知`
    - 提示文案也会同步解释必须先登录 scene 后台

### Scene 图谱维护体验
- `scene.html` 的图谱维护表单已补“实时标签预览”：
  - 背景元素
  - 特征元素
  - 细节元素
  - 支撑场景音效
  - 补充层次音效
- 每个字段旁边都已加“清空”按钮
- 当前策略是不推翻文本输入，而是在文本输入基础上补实时拆词预览，降低运营对纯文本表单的阅读负担
- `scene.html` 现已支持“快速加入常用项”：
  - 根据当前模板 / 场景集合，从已有 scene 图谱中聚合同模板、同集合的常用元素与音效词
  - 点击即可加入对应字段
- 后端接口：
  - `GET /api/scene-graph/term-suggestions`
- `scene.html` 已把“场景补充单运营”与“场景图谱维护”接通：
  - 每条场景补充单卡片新增“查看该场景图谱”
  - 表格视图新增“去图谱维护”
  - 跳转逻辑：
    - 优先定位当前赛道真实节点
    - 其次回到通用层真实节点
    - 若图谱中尚无现成节点，则自动定位到对应赛道的新建节点入口
  - 运营提示也已同步补到卡片中：建议先核对场景图谱，再决定是直接补素材，还是先调整场景推荐
- `scene.html` 还补了图谱维护 -> 补充单的反向提示：
  - 当前节点若已有关联场景补充单，会在“相关场景补充单”区显示条数、待补词数、可通知数
  - 并提供“回到场景补充单区查看”按钮
- `scene` 场景补充单现在也已接入“来源驱动的素材版本选择”：
  - 后端 `/api/ops/scene-supplements` 已返回：
    - `sfx_term_items`
    - `missing_sfx_term_items`
    - 每个词的 `source / source_label / default_asset_scope / scope_hint`
  - 前端 `scene.html` 会把场景音效词、待补场景音效直接标出：
    - 通用层
    - 赛道层
    - 通用+赛道
    - 未标注
  - 上传卡片会根据来源自动默认：
    - 只来自通用层 -> 默认通用版
    - 只来自赛道层 -> 默认当前赛道版
    - 通用+赛道 -> 不自动替运营决定，明确提示先选素材版本
- `scene.html` 的上传入库交互已进一步收口为“防误操作模式”：
  - 上传按钮默认禁用
  - 必须同时满足：
    - 已选场景音效词
    - 已选音效文件
    - 若来源为通用+赛道 / 未标注，还必须已明确选择素材版本
  - 前端不再只是提示，而是直接阻止误传；后端 `mergeSceneAsset()` 也补了 `asset_scope` 必填校验
- `scene` 补充单上传还补了一层“减点击”：
  - 如果一条场景补充单当前只剩 1 个待补词
  - 前端会自动帮运营选中这个词
  - 并继续按来源规则自动同步素材版本提示与按钮状态
- `scene` 场景补充单卡片又做了一轮收口：
  - 待补场景音效区现在会显示“来源摘要”，快速汇总当前补充单里：
    - 通用层词数
    - 赛道层词数
    - 通用+赛道词数
    - 未标注词数
  - 现在又补了统一的“补充单层级”：
    - `补充单层级：已补齐`
    - `补充单层级：优先补素材`
    - `补充单层级：先复核图谱`
    - `补充单层级：先判定版本`
    - `补充单层级：继续补素材`
  - 卡片里除了来源摘要，还会直接显示 `当前建议`
  - 表格视图也新增了 `补充单层级` 列
  - 现在表格视图中的补充单层级也改成了和卡片视图一致的彩色标签
  - 目的：让运营不只是看到词和来源，还能更快判断这条单是应直接补素材，还是先回图谱核对，或者先判断通用/赛道版本
  - 后台顶部统计卡现在也已和这套口径打通：
    - `优先补素材`
    - `先复核图谱`
    - `先判定版本`
    - `继续补素材 / 已补齐`
  - 这样 scene 用户端 `4.6` 和 scene 后台补充单总览，就开始共享同一套“该先做什么”的判断语言
  - 若当前补充单已经没有待补词，则不再继续显示上传入口
  - 改为提示运营：
    - 可以直接检查通知状态
    - 或回到场景图谱继续优化推荐
  - 通知按钮现在也按状态分层：
    - 已通知：按钮显示 `已通知用户`，禁用
    - 可通知：按钮显示 `标记已通知`
    - 未达到条件：按钮显示 `暂不可通知`，禁用
  - 卡片右侧提示也会同步显示：
    - `当前可通知用户`
    - `当前未达到可通知状态`
    - 或具体的 `已通知时间`
- `scene.html` 的场景补充单运营区已补“仅看仍有待补词的补充单”：
  - 这是前端轻筛选，只影响当前页面展示
  - 不会修改补充单状态
  - 开启后，统计卡片、卡片视图、表格视图、原始数据视图都会同步切到过滤后的结果
  - 这层筛选现在还会影响场景图谱目录中的“补充单热度”统计口径
  - 也就是：如果当前只看仍有待补词的补充单，图谱目录里的 `补充单:N / 待补:M` 也会同步只按这批可见补充单来算
- `scene.html` 的场景补充单列表现在又补了一层“运营优先级排序 + 层级筛选”：
  - 列表会优先按 `补充单层级` 自动排序：
    - `先复核图谱`
    - `先判定版本`
    - `优先补素材`
    - `继续补素材`
    - `已补齐`
  - 同层级内再按：
    - `待补词数` 从多到少
    - `ready_to_notify 且未通知` 优先
    - 最后按场景名稳定排序
  - 目的：让最值得先处理、最容易出错的场景补充单自然浮到前面
  - 同时补充单工具栏新增了 `补充单层级` 筛选：
    - `全部层级`
    - `先复核图谱`
    - `先判定版本`
    - `优先补素材`
    - `继续补素材`
    - `已补齐`
  - 筛选摘要下方还新增了 `补充单层级` 快筛按钮：
    - `全部层级`
    - `先复核图谱`
    - `先判定版本`
    - `优先补素材`
    - `继续补素材`
    - `已补齐`
  - 快筛按钮现在会直接显示当前数量，如：
    - `先复核图谱 (3)`
    - `优先补素材 (5)`
  - 若某个层级当前数量为 `0`，对应按钮会直接置灰禁用
  - 目的：让运营不必每次都打开下拉框，可直接先看分布，再一键切到最常处理的层级
  - 这样运营除了按状态、天数、是否仍待补来筛，还能直接只看“先回图谱”或“先判版本”的单
  - 工具栏下方现在还会显示 `当前筛选摘要`：
    - 最近多少天
    - 当前状态筛选
    - 当前补充单层级筛选
    - 是否仅看仍有待补词
    - 当前展示模式
  - 并直接提醒：当前补充单列表已按处理优先级自动排序
  - 如果当前筛选后为空：
    - 页面会直接提示“当前勾选了仅看仍有待补词的补充单，所以列表为空”
    - 避免运营把空白区域误判成接口失败
- `scene.html` 的场景图谱目录选择层已接入“补充单热度”：
  - 场景节点下拉项现在会追加：
    - `补充单:N`
    - `待补:M`
  - 下拉框下方的目录提示也会显示：
    - 当前目录里“含补充单节点”数量
  - 目的是让运营在还没加载节点前，就能优先看到哪些场景已产生真实补充需求
- 这层目录逻辑又补了一次优先级排序：
  - 场景节点下拉现在会优先按：
    - 待补词数
    - 补充单数
    - 场景名
    排序
  - 也就是“补充压力越大”的节点会自然排在更前面
  - 目录提示文案里也会明确说明：当前已按待补优先级排序
- `scene.html` 图谱目录又补了一个轻筛选：
  - `仅看有补充单节点`
  - 适合运营在补充单压力较大时，先只看已产生真实补充需求的场景节点
  - 切换筛选时会尽量保留当前已选节点；目录提示也会显示“当前仅显示含补充单节点”
  - 如果该筛选下没有节点：
    - `sceneGraphQuickHint` 会直接提示当前没有“含补充单”的场景节点
    - 并建议取消该筛选后查看完整目录
- `scene.html` 图谱目录现已支持“搜索场景节点”：
  - 可按：
    - 场景名
    - 模板名
    - 场景集合名
    进行模糊筛选
  - 这层搜索与“仅看有补充单节点”“按待补优先级排序”可叠加使用
  - 目录提示会显示“当前已按关键词筛选”
  - 若关键词筛选结果为空：
    - `sceneGraphQuickHint` 会明确提示“当前关键词下没有匹配的场景节点”
- `scene.html` 图谱目录还补了一个快捷动作：
  - `加载当前最高优先节点`
  - 它会直接加载当前筛选结果里排在第一位的节点
  - 旁边会显示一行轻提示：
    - 当前最高优先节点是谁
    - 这条节点当前对应多少补充单 / 多少待补词
  - 图谱区两个核心按钮现在也是“状态感知”的：
    - 没有筛出节点时：
      - `加载当前最高优先节点` 会禁用并显示 `当前没有优先节点`
      - `加载场景节点` 会禁用并显示 `请先选择场景节点`
    - 一旦选中节点或筛出优先节点，按钮会自动恢复可点击状态
- `scene.html` 现已在两个长列表区都补了“清空当前筛选”：
  - 场景补充单运营：
    - 恢复 `最近 30 天 / 全部状态 / 卡片视图 / 不勾选仅看待补`
  - 场景图谱维护：
    - 清空关键词搜索
    - 取消“仅看有补充单节点”
  - 这两个“清空当前筛选”按钮现在也补成“状态联动”：
    - 如果当前已经是默认筛选口径，会禁用并显示 `当前无需清空`
    - 只有真的存在非默认筛选时，才恢复为 `清空当前筛选`
  - 这两个动作都只影响当前页面展示，不修改任何业务数据
- `scene.html` 这层联动又补稳了一次：
  - 每次刷新场景补充单列表后，会立即重绘场景图谱目录
  - 避免补充单数量 / 待补词数量变化后，节点下拉仍显示旧热度
- `scene` 补充单表格视图现在也与卡片视图对齐：
  - 场景音效词、待补词都直接显示来源标签
  - 新增“来源摘要”列
  - 避免运营在表格视图下丢失通用层 / 赛道层 / 通用+赛道的信息
- 上线前图谱策略已新增一条明确原则：
  - `action_graph.json` 与 `scene_graph.json` 不应完全等待用户文本被动生长
  - 应先带一版“行业基础种子图谱”上线
  - 目的：
    - 让系统一开始就具备基础命中能力
    - 让运营可先围绕基础种子补素材
    - 再用真实用户文本、补充单和图谱维护持续把图谱养大
- 本轮已开始执行第一批基础种子扩充：
  - 动作图谱：
    - 通用层动作由 `24` 个提升到 `37` 个
    - 新增高复用通用动作词：
      - `走`
      - `快走`
      - `慢走`
      - `跳`
      - `爬`
      - `飞`
      - `摩擦`
      - `蹲下`
      - `抬手`
      - `放下`
      - `挥手`
      - `触碰`
      - `拾起`
  - 场景图谱：
    - 通用场景由 `3` 个提升到 `12` 个
    - 场景模板由 `4` 个提升到 `16` 个
    - 新增通用场景包括：
      - `街道`
      - `雨夜街巷`
      - `房间`
      - `卧室`
      - `走廊`
      - `客栈`
      - `市集`
      - `山林`
      - `河边`
    - 同时新增少量赛道样板场景：
      - `玄幻::宗门大殿`
      - `言情::闺阁深夜`
      - `悬疑::废弃走廊`
      - `科幻::舰桥`
- 这批预置种子与当前系统能力是匹配的：
  - 通用层可后续迁到赛道层
  - 赛道层也可后续再提升回通用层
  - 所以先以“高复用通用层”为主上线，是合理且低风险的
- 后台也已把这条策略变成可见信息：
  - `admin.html` 的动作图谱维护区新增了“基础种子摘要”
    - 会显示当前动作通用层基础种子规模
    - 并直接提示上线前建议优先补哪类高复用基础动作素材
    - 现在还新增了“建议优先补库”快捷入口，可直接跳到：
      - `走`
      - `快走`
      - `跳`
      - `爬`
      - `飞`
      - `抓`
      - `推`
      - `拉`
      - `摩擦`
      - `蹲下`
    - 这些快捷入口现在还会直接显示覆盖状态：
      - `未建补充单`（含义：待用户命中验证）
      - `待补 N`
      - `已覆盖 N`
    - 这些快捷入口现在还会直接显示“优先补库原因”：
      - 例如：
        - `基础位移动作，复用频率高`
        - `近景互动动作，覆盖面很广`
        - `布料/表面类通用动效，高复用`
    - 这些动作种子快捷入口现在还会按当前覆盖状态自动重排：
      - `待补` 优先
      - `未建补充单` 其次
      - `已覆盖` 靠后
    - 这批优先动作种子现在还会直接显示覆盖摘要：
      - 优先种子里 `待补` 有多少个
      - `待用户命中验证` 有多少个
      - `已覆盖` 有多少个
    - 动作后台当前还新增了一行轻量状态总览卡：
      - `待补`
      - `待验证`
      - `已覆盖`
      让运营先看基础种子的整体补库压力，再决定点哪一个入口
    - 每个优先动作种子条目现在还会直接给出“下一步动作”：
      - 已有补充单：`先去补充单补素材`
      - 未建补充单：`先等待真实用户文本命中验证`
    - `未建补充单` 在动作种子语义里，当前已明确解释为：
      - 不是异常数据
      - 而是“上线前已预置、但还缺真实用户命中验证”的基础种子
    - 已有补充单的优先动作种子，现在可直接点 `去补充`
    - 从动作种子入口点进图谱维护后，当前维护反馈里还会直接显示：
      - `建议下一步：先去补充单补素材`
      - 或 `建议下一步：先等待真实用户文本命中验证`
    - 动作后台种子摘要区当前还会直接提示：
      - `待用户命中验证` 不是异常
      - 而是上线前已预置、等待真实文本触发的基础动作种子
  - `scene.html` 的场景图谱维护区新增了“场景基础种子摘要”
    - 会显示当前通用场景、模板、赛道样板规模
    - 并直接提示上线前建议优先补哪类高复用基础场景素材
    - 现在还新增了“建议优先补库”快捷入口，可直接跳到：
      - `街道`
      - `房间`
      - `卧室`
      - `走廊`
      - `海边`
      - `山林`
      - `雨夜街巷`
    - 这些快捷入口现在也会直接显示覆盖状态：
      - `未建补充单`（含义：待用户命中验证）
      - `待补 N`
      - `已覆盖 N`
    - 这些快捷入口现在还会直接显示：
      - `支撑音效项数`
      - `细节音效项数`
    - 这批优先场景种子现在还会直接显示覆盖摘要：
      - 优先种子里 `待补` 有多少个
      - `待用户命中验证` 有多少个
      - `已覆盖` 有多少个
    - 场景后台当前也新增了一行轻量状态总览卡：
      - `待补`
      - `待验证`
      - `已覆盖`
      让运营先看基础场景种子的整体补库压力，再决定点哪一个入口
    - 每个优先场景种子条目现在还会直接给出“下一步动作”：
      - 已有补充单：`先去补充单补素材`
      - 未建补充单：`先等待真实用户文本命中验证`
    - `未建补充单` 在场景种子语义里，当前也已明确解释为：
      - 不是异常数据
      - 而是“上线前已预置、但还缺真实用户命中验证”的基础场景种子
    - 已有补充单的优先场景种子，现在可直接点 `去补充单`
    - 从场景种子入口点进图谱维护后，保存状态旁会直接显示：
      - `建议下一步：先去补充单补支撑场景音效，再回看细节音效`
      - 或 `建议下一步：先等待真实用户文本命中验证`
    - 场景图谱维护里的“相关场景补充单”区块，现在也会直接给出：
      - 当前节点更适合先补素材
      - 还是已可回补充单区通知用户
    - 场景节点摘要与来源解释里也新增了“补库顺序提示”：
      - 先补支撑场景音效
      - 再根据场景是否已经站住，决定是否继续补细节音效
    - 这些场景种子快捷入口现在也会按当前覆盖状态自动重排：
      - `待补` 优先
      - `未建补充单` 其次
      - `已覆盖` 靠后
    - 场景后台种子摘要区当前也会直接提示：
      - `待用户命中验证` 不是异常
      - 而是上线前已预置、等待真实文本触发的基础场景种子
  - 目的：让运营上线前不必自己猜“先补什么”，而是有一个稳定的基础补库方向

## 新增：2026-03-20 场景搭建分析稳定性修复

- `scene-building` 近期出现过真实 500：
  - 根因不是前端，而是后端写库时把 `result['report_json'] = result` 自引用
  - 已修复为可序列化副本，避免 `Circular reference detected`

- `scene-building` 的 `Contract 不通过` 已明确定位：
  - 不是 JSON 解析失败
  - 而是 LLM 已返回 JSON，但顶层字段不稳定，缺过 `title`
  - 主 prompt 与 retry prompt 现已补强：
    - 强制顶层必须包含 `title / scene_items / summary / graph_model`
    - `title` 固定输出为 `场景搭建分析`

- `scene-building` 现已补上长文本分段逻辑，参考动作分析：
  - `SINGLE_PASS_SAFE_CHARS = 420`
  - `SEGMENT_TARGET_CHARS = 360`
  - `SEGMENT_MAX_CHARS = 420`
  - 长文本会先分段，再合并 `scene_items / summary / key_points / risks`

- `supporting_sfx_terms / detail_sfx_terms` 的 prompt 规则也已更新：
  - `supporting_sfx_terms` 用于“让听众立刻知道场景”
  - `detail_sfx_terms` 用于“让场景更丰满”，不能替代支撑音效
  - 若支撑音效已有 `2-3` 个明显标志声，细节音效仍建议补 `2-5` 个
  - 若支撑音效少于 `2` 个、无法明确场景，细节音效可补 `3-8` 个
  - 目标不是节约 token，而是给用户更宽的可选择空间

## 新增：2026-03-20 前端体验收口（用户端主题 / 文档锚点 / scene 补充单交互）

- 用户端 [index.html](/Users/demo/Documents/New%20project/musicForArt/frontend/index.html)
  - 曾临时切换到与 `scene.html` 接近的深蓝色 + 金色渐变主题
  - 现已按用户反馈回退到原来的红色系轻主题

- `scene.html`
  - “场景补充单运营”里的补充单卡片，不再只是静态长卡片
  - 卡片头现在可点击展开 / 收起详情，避免运营点击补充单时“看起来没有任何反应”
  - 卡片头新增了显式提示：`点击收起补充单详情 / 点击展开补充单详情`
  - 另修复了一个导致“统计有数但补充单列表空白”的真实 bug：
    - 原因：补充单渲染中使用了 `escAttr(...)`，但函数未定义
    - 结果：`renderSceneSupplements()` 在统计卡渲染完成后、列表写入前抛异常
    - 表现：顶部能看到补充单数/待补词数，下面没有任何补充单卡片
    - 现已补上 `escAttr()` 并补充 `scene-badge` 样式

- `scene.html` 的“本地文档锚点”
  - 已从写死的 `pre` 文本，改成动态文档清单
  - 每条会显示：
    - 本地绝对路径
    - 文档中文名称
    - 中文解释
  - 当前纳入 scene 相关文档：
    - `SCENE_BUILDING_MODULE_STRATEGY.md`
    - `SCENE_BUILDING_OUTPUT_SCHEMA.md`
    - `SCENE_BUILDING_PROMPT_V1.md`
    - `SCENE_MODULE_DECOUPLING_PLAN.md`
    - `OPS_ACTION_GRAPH_AND_SUPPLEMENTS_GUIDE.md`
    - `OPS_ACTION_GRAPH_AND_SUPPLEMENTS_QA.md`

- `admin.html`
  - 已复用“本地文档锚点”功能
  - 新增动作分析相关文档清单，同样展示：
    - 本地绝对路径
    - 文档中文名称
    - 中文解释

## 新增：2026-03-21 前端安全加固第一阶段

- 本轮已按“测试期可暴露调试、正式期收紧暴露面”的原则落地第一阶段安全加固：
  - 用户端默认不再长期裸露内部调试字段
  - 调试 JSON 改为受系统开关控制
  - 当前策略是：
    - 测试期可开启 `frontend_debug_expose_enabled`
    - 正式上线后应关闭该开关
    - 即使关闭，当前前端在结果异常时仍可临时显示一段折叠调试 JSON，便于复测定位 bug

- 后端已新增前端安全配置项：
  - `frontend_debug_expose_default`
  - `require_signed_downloads`
  - `download_token_ttl_sec`
  - `download_signing_key`

- 后端已新增前端安全设置接口：
  - `GET /api/admin/frontend-security`
  - `POST /api/admin/frontend-security`
  - 当前 `admin.html` 已补上“前端安全设置”卡片
    - 可直接查看：
      - 调试 JSON 是否开启
      - 当前环境是否要求签名下载
      - 下载 token 时效
    - 并可手动保存 `frontend_debug_expose_enabled`

- 用户端返回已开始最小化收口：
  - 用户端接口统一经过 `_user_json_response(...)`
  - 会递归清理不该继续暴露给普通用户的数据，例如：
    - 文件真实路径
    - 内部 `_source_path`
    - `asset_file_path`
    - `llm_trace`
    - `debug`
    - `raw_response`
    - `prompt_file`
    - `effective_prompt_file`
  - 目标是：
    - 普通用户端只拿到“展示必须字段”
    - 后台与排障链路再保留完整信息

- 下载安全已按“本地兼容 / 正式签名”分层实现：
  - 本地开发环境仍兼容现有下载方式，便于联调
  - 正式环境可通过 `require_signed_downloads=1` 切到签名下载
  - 当前签名下载机制：
    - 前端不必继续拿真实文件路径直接复用
    - 后端可下发短时效 `token`
    - `/api/sfx/file` 已支持通过 `token` 校验后下载
    - token 会绑定：
      - 目标文件
      - 过期时间
      - 当前登录手机号（若要求）

- 身份识别已开始切到“token/session 优先”：
  - 三个前端页面：
    - `index.html`
    - `admin.html`
    - `scene.html`
    当前都已去掉主动透传 `X-User-Phone`
  - 现在优先依赖：
    - `Authorization: Bearer <token>`
    - 服务端 session/token 解析出的当前用户
  - 这一步的意义是：
    - 降低前端伪造手机号请求的风险
    - 后续做额度、下载、审计绑定时更稳

- 运营端识别信息已补强：
  - `admin.html` 的用户列表不再只给单独手机号列
  - 现在显式展示：
    - `手机号`
    - `UID`
    - 前台等级
    - 是否授权不限额
  - “运营行为审计”的时间线卡片现在也会显示：
    - `UID`
    - `手机号`
  - 目的：方便运营在人工排查和白名单放开时做双重识别，不只靠手机号肉眼判断

- 关于系统唯一标识的当前口径：
  - 内部稳定主键：`user_account.id`
  - 外显识别：`uid`
  - 登录方式：`phone`
  - 原因：
    - 手机号适合登录，但不适合做永久业务主键
    - `id` 更稳定
    - `uid` 更适合后台与运营沟通时识别用户

## 新增：2026-03-21 用户端首页热榜压缩版

- 用户端首页热榜区域本轮已按“单屏尽量看全”重新收紧：
  - 动作榜与场景榜全部从 `Top10` 改为 `Top5`
  - 两大区块继续并排展示
  - 每个区块内 6 个榜单按双列排布，目标是在常见桌面视口下尽量接近三行看全

- 排榜内容已进一步做减法：
  - 去掉榜单行内的附说明，例如动作来源词、场景来源词
  - 去掉区块顶部的“过去10天 · N 次下载”
  - 去掉“按真实下载统计展示……”这类解释性文案
  - 现在榜单行只保留：
    - 排名
    - 音效名
    - 下载次数

- 排榜样式已继续压缩：
  - 时间切换按钮：
    - 更小字号
    - 更小内边距
    - 更低高度
  - 榜单卡片：
    - 更小标题区
    - 更小 badge
    - 更小列表项高度
    - 更窄数值列
  - 榜单边界统一：
    - 有数据的每一行也统一改成虚线边框
    - 无数据空态同样保持虚线边框，视觉节奏一致

- 用户端页头也同步压缩了一轮：
  - `有声书声音设计台` 标题高度缩小
  - 原来独立一整排的能力芯片已取消
  - 改成嵌入式功能提示：
    - `文本`
    - `动作音效`
    - `场景搭建`
    - `音乐分析`
    - `后期执行单`
  - 这些嵌入式芯片已做过基线和字号对齐修正，避免比正文明显偏小
